module.exports = [
"[externals]/next/dist/compiled/next-server/app-route-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-route-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-route-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/@opentelemetry/api [external] (next/dist/compiled/@opentelemetry/api, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/@opentelemetry/api", () => require("next/dist/compiled/@opentelemetry/api"));

module.exports = mod;
}),
"[externals]/next/dist/compiled/next-server/app-page-turbo.runtime.dev.js [external] (next/dist/compiled/next-server/app-page-turbo.runtime.dev.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js", () => require("next/dist/compiled/next-server/app-page-turbo.runtime.dev.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-unit-async-storage.external.js [external] (next/dist/server/app-render/work-unit-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-unit-async-storage.external.js", () => require("next/dist/server/app-render/work-unit-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/work-async-storage.external.js [external] (next/dist/server/app-render/work-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/work-async-storage.external.js", () => require("next/dist/server/app-render/work-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/shared/lib/no-fallback-error.external.js [external] (next/dist/shared/lib/no-fallback-error.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/shared/lib/no-fallback-error.external.js", () => require("next/dist/shared/lib/no-fallback-error.external.js"));

module.exports = mod;
}),
"[externals]/module [external] (module, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("module", () => require("module"));

module.exports = mod;
}),
"[externals]/path [external] (path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("path", () => require("path"));

module.exports = mod;
}),
"[project]/convex/_generated/api.js [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/* eslint-disable */ /**
 * Generated `api` utility.
 *
 * THIS CODE IS AUTOMATICALLY GENERATED.
 *
 * To regenerate, run `npx convex dev`.
 * @module
 */ __turbopack_context__.s([
    "api",
    ()=>api,
    "components",
    ()=>components,
    "internal",
    ()=>internal
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/server/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/server/api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$components$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/server/components/index.js [app-route] (ecmascript) <locals>");
;
const api = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["anyApi"];
const internal = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["anyApi"];
const components = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$server$2f$components$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["componentsGeneric"])();
}),
"[externals]/node:crypto [external] (node:crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:crypto", () => require("node:crypto"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/action-async-storage.external.js [external] (next/dist/server/app-render/action-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/action-async-storage.external.js", () => require("next/dist/server/app-render/action-async-storage.external.js"));

module.exports = mod;
}),
"[externals]/next/dist/server/app-render/after-task-async-storage.external.js [external] (next/dist/server/app-render/after-task-async-storage.external.js, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("next/dist/server/app-render/after-task-async-storage.external.js", () => require("next/dist/server/app-render/after-task-async-storage.external.js"));

module.exports = mod;
}),
"[project]/lib/convex/client.ts [app-route] (ecmascript) <locals>", ((__turbopack_context__) => {
"use strict";

/**
 * Convex Client for Server-Side Operations
 *
 * This replaces Upstash Redis for workflow storage
 */ __turbopack_context__.s([
    "getAuthenticatedConvexClient",
    ()=>getAuthenticatedConvexClient,
    "getConvexClient",
    ()=>getConvexClient,
    "isConvexConfigured",
    ()=>isConvexConfigured
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$index$2d$node$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/index-node.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/http_client.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$clerk$2f$nextjs$2f$dist$2f$esm$2f$app$2d$router$2f$server$2f$auth$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@clerk/nextjs/dist/esm/app-router/server/auth.js [app-route] (ecmascript)");
;
;
;
let convexClient = null;
function getConvexClient() {
    if (!convexClient) {
        const url = ("TURBOPACK compile-time value", "https://sensible-ermine-579.convex.cloud");
        if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
        ;
        try {
            convexClient = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ConvexHttpClient"](url);
        } catch (error) {
            console.error('Failed to initialize Convex client:', error);
            throw new Error(`Convex client initialization failed: ${error instanceof Error ? error.message : 'Unknown error'}`);
        }
    }
    return convexClient;
}
async function getAuthenticatedConvexClient() {
    const url = ("TURBOPACK compile-time value", "https://sensible-ermine-579.convex.cloud");
    if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
    ;
    const client = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ConvexHttpClient"](url);
    try {
        // Get Clerk auth token
        const { getToken } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$clerk$2f$nextjs$2f$dist$2f$esm$2f$app$2d$router$2f$server$2f$auth$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["auth"])();
        const token = await getToken({
            template: "convex"
        });
        // Set the authentication token
        if (token) {
            client.setAuth(token);
        } else {
            console.warn('No Clerk token available - using unauthenticated client');
        }
    } catch (error) {
        console.error('Failed to get Clerk token:', error);
    // Continue with unauthenticated client instead of throwing
    // This allows the app to function even if Clerk auth fails
    }
    return client;
}
function isConvexConfigured() {
    return !!("TURBOPACK compile-time value", "https://sensible-ermine-579.convex.cloud");
}
;
}),
"[externals]/@langchain/langgraph [external] (@langchain/langgraph, esm_import)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

const mod = await __turbopack_context__.y("@langchain/langgraph");

__turbopack_context__.n(mod);
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, true);}),
"[externals]/crypto [external] (crypto, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("crypto", () => require("crypto"));

module.exports = mod;
}),
"[project]/lib/workflow/safe-expression-evaluator.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Safe expression evaluator for if-else conditions and set-state values
 *
 * SECURITY FIX: Migrated from expr-eval to mathjs
 * - expr-eval has critical vulnerabilities (CVE-2024-29415): prototype pollution, code injection
 * - mathjs provides a secure, sandboxed evaluation environment
 * - No access to dangerous functions or global objects
 * - Protection against prototype pollution
 */ __turbopack_context__.s([
    "getExpressionVariables",
    ()=>getExpressionVariables,
    "safeCompare",
    ()=>safeCompare,
    "safeEvaluate",
    ()=>safeEvaluate,
    "validateExpression",
    ()=>validateExpression
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$mathjs$2f$lib$2f$esm$2f$core$2f$create$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/mathjs/lib/esm/core/create.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$mathjs$2f$lib$2f$esm$2f$entry$2f$allFactoriesAny$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/mathjs/lib/esm/entry/allFactoriesAny.js [app-route] (ecmascript)");
;
// Create a restricted mathjs instance
const math = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$mathjs$2f$lib$2f$esm$2f$core$2f$create$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["create"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$mathjs$2f$lib$2f$esm$2f$entry$2f$allFactoriesAny$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["all"], {});
function safeEvaluate(expression, context) {
    if (!expression || expression.trim() === '') {
        throw new Error('Expression cannot be empty');
    }
    // Maximum expression length to prevent DoS
    const MAX_LENGTH = 1000;
    if (expression.length > MAX_LENGTH) {
        throw new Error(`Expression too long (max ${MAX_LENGTH} characters)`);
    }
    try {
        // Create a clean scope with only safe variables
        // This prevents prototype pollution attacks
        const cleanScope = Object.create(null);
        // Copy context variables to clean scope
        for (const [key, value] of Object.entries(context)){
            // Block dangerous property names
            if (key === '__proto__' || key === 'constructor' || key === 'prototype') {
                continue;
            }
            cleanScope[key] = value;
        }
        // Add custom safe functions to the scope
        cleanScope.toLowerCase = (str)=>String(str).toLowerCase();
        cleanScope.toUpperCase = (str)=>String(str).toUpperCase();
        cleanScope.trim = (str)=>String(str).trim();
        cleanScope.includes = (str, search)=>String(str).includes(String(search));
        cleanScope.startsWith = (str, search)=>String(str).startsWith(String(search));
        cleanScope.endsWith = (str, search)=>String(str).endsWith(String(search));
        cleanScope.length = (arr)=>Array.isArray(arr) ? arr.length : String(arr).length;
        cleanScope.isNull = (val)=>val === null || val === undefined;
        cleanScope.isNumber = (val)=>typeof val === 'number' && !isNaN(val);
        cleanScope.isString = (val)=>typeof val === 'string';
        cleanScope.isBoolean = (val)=>typeof val === 'boolean';
        cleanScope.isArray = (val)=>Array.isArray(val);
        // Evaluate the expression using mathjs
        const result = math.evaluate(expression, cleanScope);
        return result;
    } catch (error) {
        // Provide helpful error message
        const errorMessage = error instanceof Error ? error.message : 'Unknown error';
        throw new Error(`Expression evaluation failed: ${errorMessage}`);
    }
}
function validateExpression(expression) {
    try {
        // Try to parse the expression (but don't evaluate)
        math.parse(expression);
        return {
            valid: true
        };
    } catch (error) {
        return {
            valid: false,
            error: error instanceof Error ? error.message : 'Invalid expression'
        };
    }
}
function getExpressionVariables(expression) {
    try {
        const node = math.parse(expression);
        const variables = new Set();
        // Traverse the AST to find all symbol nodes (variables)
        node.traverse((node)=>{
            if (node.type === 'SymbolNode') {
                variables.add(node.name);
            }
        });
        return Array.from(variables);
    } catch  {
        return [];
    }
}
function safeCompare(left, operator, right, options) {
    let leftVal = left;
    let rightVal = right;
    // String normalization if requested
    if (options?.trim && typeof leftVal === 'string') {
        leftVal = leftVal.trim();
    }
    if (options?.trim && typeof rightVal === 'string') {
        rightVal = rightVal.trim();
    }
    if (options?.caseInsensitive && typeof leftVal === 'string') {
        leftVal = leftVal.toLowerCase();
    }
    if (options?.caseInsensitive && typeof rightVal === 'string') {
        rightVal = rightVal.toLowerCase();
    }
    switch(operator){
        case '==':
            return leftVal == rightVal; // eslint-disable-line eqeqeq
        case '===':
            return leftVal === rightVal;
        case '!=':
            return leftVal != rightVal; // eslint-disable-line eqeqeq
        case '!==':
            return leftVal !== rightVal;
        case '>':
            return leftVal > rightVal;
        case '<':
            return leftVal < rightVal;
        case '>=':
            return leftVal >= rightVal;
        case '<=':
            return leftVal <= rightVal;
        default:
            throw new Error(`Unknown operator: ${operator}`);
    }
}
}),
"[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "evaluateExpression",
    ()=>evaluateExpression,
    "extractVariableReferences",
    ()=>extractVariableReferences,
    "getAvailableVariables",
    ()=>getAvailableVariables,
    "substituteVariables",
    ()=>substituteVariables,
    "validateVariableReferences",
    ()=>validateVariableReferences
]);
/**
 * Check if a property name is a prototype pollution vector
 * Blocks: __proto__, constructor, prototype
 */ function isPrototypePollutionKey(key) {
    const dangerousKeys = [
        '__proto__',
        'constructor',
        'prototype'
    ];
    return dangerousKeys.includes(key.toLowerCase());
}
function substituteVariables(text, state) {
    if (!text) return text;
    // Find all {{variable}} patterns
    const pattern = /\{\{([^}]+)\}\}/g;
    return text.replace(pattern, (match, expression)=>{
        try {
            // Clean up the expression
            const cleanExpr = expression.trim();
            // Evaluate the expression against state
            const value = evaluateExpression(cleanExpr, state);
            // Convert value to string
            if (value === null || value === undefined) {
                return match; // Keep original if not found
            }
            if (typeof value === 'object') {
                // Special handling for file objects - use content/text instead of JSON
                if (value.storageId) {
                    if (value.content) {
                        console.log(`[VarSub] Substituting file content for ${cleanExpr}, content length: ${value.content.length}`);
                        return value.content;
                    } else if (value.text) {
                        console.log(`[VarSub] Substituting file text for ${cleanExpr}, text length: ${value.text.length}`);
                        return value.text;
                    } else {
                        console.warn(`[VarSub] File object ${cleanExpr} has no content or text property, returning JSON`);
                        return JSON.stringify(value);
                    }
                }
                // For non-file objects, return JSON
                return JSON.stringify(value);
            }
            return String(value);
        } catch (e) {
            console.warn(`Failed to substitute variable: ${expression}`, e);
            return match; // Keep original on error
        }
    });
}
function evaluateExpression(expression, state) {
    // Support both patterns:
    // 1. Full: "state.variables.node_1.price"
    // 2. Simple: "node_1.price" (auto-adds state.variables prefix)
    let normalizedExpr = expression;
    // If expression doesn't start with "state", assume it's a simple node reference
    if (!expression.startsWith('state.')) {
        // Check if it's a known shorthand
        if (expression === 'input' || expression.startsWith('input.')) {
            // Support both input.query (maps to state.variables.input.input.query) and input directly
            // Try nested first (for JSON inputs), then direct
            normalizedExpr = `state.variables.${expression}`;
        } else if (expression === 'lastOutput' || expression.startsWith('lastOutput.')) {
            normalizedExpr = `state.variables.${expression}`;
        } else {
            // Assume it's a node reference like "scrape_website.markdown"
            normalizedExpr = `state.variables.${expression}`;
        }
    }
    // Parse dot notation path
    const parts = normalizedExpr.split('.');
    let current = {
        state
    };
    for (const part of parts){
        if (current === null || current === undefined) {
            return undefined;
        }
        // SECURITY: Prevent prototype pollution attacks
        // Block access to __proto__, constructor, prototype
        if (isPrototypePollutionKey(part)) {
            console.warn(`Blocked prototype pollution attempt: ${part}`);
            return undefined;
        }
        // Handle array indexing like items[0]
        const arrayMatch = part.match(/^(\w+)\[(\d+)\]$/);
        if (arrayMatch) {
            const [, arrayName, index] = arrayMatch;
            // Validate array name doesn't contain prototype pollution
            if (isPrototypePollutionKey(arrayName)) {
                console.warn(`Blocked prototype pollution attempt in array: ${arrayName}`);
                return undefined;
            }
            current = current[arrayName]?.[parseInt(index)];
        } else {
            // Use hasOwnProperty check for additional safety
            if (typeof current === 'object' && current !== null) {
                if (Object.prototype.hasOwnProperty.call(current, part)) {
                    current = current[part];
                } else {
                    return undefined;
                }
            } else {
                return undefined;
            }
        }
    }
    // Special handling for input.X pattern - try multiple resolution strategies
    if (current === undefined && normalizedExpr.startsWith('state.variables.input.')) {
        const inputPath = normalizedExpr.replace('state.variables.input.', '');
        // Strategy 1: Try nested .input.input.X (for JSON inputs)
        const nestedValue = state.variables?.input?.input?.[inputPath];
        if (nestedValue !== undefined) {
            return nestedValue;
        }
        // Strategy 2: Try direct variable access (e.g., state.variables.query when input.query is requested)
        if (state.variables?.[inputPath] !== undefined) {
            return state.variables[inputPath];
        }
        // Strategy 3: Try traversing the full nested path manually
        const nestedPath = `state.variables.input.input.${inputPath}`;
        const nestedParts = nestedPath.split('.');
        let nestedCurrent = {
            state
        };
        for (const part of nestedParts){
            if (nestedCurrent === null || nestedCurrent === undefined) break;
            nestedCurrent = nestedCurrent[part];
        }
        if (nestedCurrent !== undefined) {
            return nestedCurrent;
        }
    }
    return current;
}
function extractVariableReferences(text) {
    if (!text) return [];
    const pattern = /\{\{([^}]+)\}\}/g;
    const matches = [];
    let match;
    while((match = pattern.exec(text)) !== null){
        matches.push(match[1].trim());
    }
    return matches;
}
function validateVariableReferences(text, state) {
    const references = extractVariableReferences(text);
    const missing = [];
    for (const ref of references){
        try {
            const value = evaluateExpression(ref, state);
            if (value === undefined) {
                missing.push(ref);
            }
        } catch (e) {
            missing.push(ref);
        }
    }
    return {
        valid: missing.length === 0,
        missing
    };
}
function getAvailableVariables(state) {
    const variables = [];
    // Add top-level variables
    variables.push({
        path: 'state.variables.input',
        value: state.variables.input,
        type: typeof state.variables.input
    });
    variables.push({
        path: 'state.variables.lastOutput',
        value: state.variables.lastOutput,
        type: typeof state.variables.lastOutput
    });
    // Add all custom variables
    Object.keys(state.variables).forEach((key)=>{
        if (key !== 'input' && key !== 'lastOutput') {
            const value = state.variables[key];
            variables.push({
                path: `state.variables.${key}`,
                value,
                type: typeof value
            });
            // If it's an object, add nested properties
            if (value && typeof value === 'object' && !Array.isArray(value)) {
                Object.keys(value).forEach((nestedKey)=>{
                    variables.push({
                        path: `state.variables.${key}.${nestedKey}`,
                        value: value[nestedKey],
                        type: typeof value[nestedKey]
                    });
                });
            }
        }
    });
    return variables;
}
}),
"[project]/lib/mcp/resolver.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * MCP Resolver
 * Fetches MCP configurations from Convex at runtime
 * This ensures executors always use the latest configuration
 */ __turbopack_context__.s([
    "migrateMCPData",
    ()=>migrateMCPData,
    "resolveMCPServer",
    ()=>resolveMCPServer,
    "resolveMCPServers",
    ()=>resolveMCPServers
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$index$2d$node$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/index-node.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/http_client.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
;
;
// Create a Convex client for server-side usage
const getConvexClient = ()=>{
    const url = ("TURBOPACK compile-time value", "https://sensible-ermine-579.convex.cloud");
    if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
    ;
    return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ConvexHttpClient"](url);
};
async function resolveMCPServers(serverIds) {
    if (!serverIds || serverIds.length === 0) {
        return [];
    }
    // Handle legacy format (full config objects instead of IDs)
    if (typeof serverIds[0] === 'object') {
        console.log('✅ Using legacy mcpTools format (already resolved)');
        return serverIds; // Already resolved
    }
    const convex = getConvexClient();
    if (!convex) {
        console.warn('Convex not configured, returning empty MCP servers');
        return [];
    }
    try {
        // Fetch server configurations from Convex
        const servers = await convex.query(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].mcpServers.getMCPServersByIds, {
            ids: serverIds
        });
        // Transform to the format expected by executors
        return servers.filter((s)=>!!s).map((server)=>({
                name: server.name,
                url: server.url,
                description: server.description,
                authType: server.authType,
                accessToken: server.accessToken,
                availableTools: server.tools || [],
                headers: server.headers
            }));
    } catch (error) {
        console.error('Error resolving MCP servers:', error);
        return [];
    }
}
async function resolveMCPServer(serverId) {
    if (!serverId) {
        return null;
    }
    const convex = getConvexClient();
    if (!convex) {
        console.warn('Convex not configured, cannot resolve MCP server');
        return null;
    }
    try {
        const server = await convex.query(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].mcpServers.getMCPServer, {
            id: serverId
        });
        if (!server) {
            return null;
        }
        return {
            name: server.name,
            url: server.url,
            description: server.description,
            authType: server.authType,
            accessToken: server.accessToken,
            availableTools: server.tools || [],
            headers: server.headers
        };
    } catch (error) {
        console.error('Error resolving MCP server:', error);
        return null;
    }
}
function migrateMCPData(data) {
    // If using old format with full configs, return as-is for backward compatibility
    if (data.mcpTools && Array.isArray(data.mcpTools)) {
        console.warn('Node using legacy mcpTools format. Consider migrating to mcpServerIds.');
        return data;
    }
    // New format uses mcpServerIds
    return data;
}
}),
"[externals]/node:module [external] (node:module, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:module", () => require("node:module"));

module.exports = mod;
}),
"[externals]/node:stream [external] (node:stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:stream", () => require("node:stream"));

module.exports = mod;
}),
"[externals]/buffer [external] (buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("buffer", () => require("buffer"));

module.exports = mod;
}),
"[externals]/string_decoder [external] (string_decoder, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("string_decoder", () => require("string_decoder"));

module.exports = mod;
}),
"[externals]/stream [external] (stream, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("stream", () => require("stream"));

module.exports = mod;
}),
"[externals]/node:assert [external] (node:assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:assert", () => require("node:assert"));

module.exports = mod;
}),
"[externals]/node:net [external] (node:net, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:net", () => require("node:net"));

module.exports = mod;
}),
"[externals]/node:http [external] (node:http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http", () => require("node:http"));

module.exports = mod;
}),
"[externals]/node:querystring [external] (node:querystring, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:querystring", () => require("node:querystring"));

module.exports = mod;
}),
"[externals]/node:events [external] (node:events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:events", () => require("node:events"));

module.exports = mod;
}),
"[externals]/node:diagnostics_channel [external] (node:diagnostics_channel, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:diagnostics_channel", () => require("node:diagnostics_channel"));

module.exports = mod;
}),
"[externals]/node:util [external] (node:util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util", () => require("node:util"));

module.exports = mod;
}),
"[externals]/node:tls [external] (node:tls, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:tls", () => require("node:tls"));

module.exports = mod;
}),
"[externals]/node:buffer [external] (node:buffer, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:buffer", () => require("node:buffer"));

module.exports = mod;
}),
"[externals]/node:zlib [external] (node:zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:zlib", () => require("node:zlib"));

module.exports = mod;
}),
"[externals]/node:perf_hooks [external] (node:perf_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:perf_hooks", () => require("node:perf_hooks"));

module.exports = mod;
}),
"[externals]/node:util/types [external] (node:util/types, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:util/types", () => require("node:util/types"));

module.exports = mod;
}),
"[externals]/node:worker_threads [external] (node:worker_threads, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:worker_threads", () => require("node:worker_threads"));

module.exports = mod;
}),
"[externals]/node:http2 [external] (node:http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:http2", () => require("node:http2"));

module.exports = mod;
}),
"[externals]/node:async_hooks [external] (node:async_hooks, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:async_hooks", () => require("node:async_hooks"));

module.exports = mod;
}),
"[externals]/node:console [external] (node:console, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:console", () => require("node:console"));

module.exports = mod;
}),
"[externals]/node:fs/promises [external] (node:fs/promises, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:fs/promises", () => require("node:fs/promises"));

module.exports = mod;
}),
"[externals]/node:path [external] (node:path, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:path", () => require("node:path"));

module.exports = mod;
}),
"[externals]/node:timers [external] (node:timers, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:timers", () => require("node:timers"));

module.exports = mod;
}),
"[externals]/node:dns [external] (node:dns, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:dns", () => require("node:dns"));

module.exports = mod;
}),
"[project]/lib/workflow/executors/tool-utils.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Tool Utilities
 * Centralized utilities for safe tool result handling
 * This ensures all tools work consistently regardless of response format
 */ /**
 * Safely parse JSON with automatic fallback to raw content
 * This prevents JSON parsing errors from breaking workflows
 */ __turbopack_context__.s([
    "formatToolResultForLLM",
    ()=>formatToolResultForLLM,
    "normalizeToolResult",
    ()=>normalizeToolResult,
    "parseToolCallResult",
    ()=>parseToolCallResult,
    "safeJsonParse",
    ()=>safeJsonParse,
    "truncateContent",
    ()=>truncateContent,
    "validateToolResult",
    ()=>validateToolResult,
    "wrapToolFunction",
    ()=>wrapToolFunction
]);
function safeJsonParse(content, fallbackToRaw = true) {
    if (!content) {
        return ''; // Return empty string without type assertion
    }
    try {
        return JSON.parse(content);
    } catch (e) {
        if (fallbackToRaw) {
            // If not valid JSON, return the raw content
            return content; // Return string without type assertion
        }
        throw new Error(`Invalid JSON: ${e instanceof Error ? e.message : 'Parse error'}`);
    }
}
function normalizeToolResult(result, options = {}) {
    const { preferJson = false, includeRaw = false } = options;
    // Handle null/undefined
    if (result === null || result === undefined) {
        return {
            data: result,
            isJson: false
        };
    }
    // Handle non-string primitives (numbers, booleans)
    if (typeof result !== 'string' && typeof result !== 'object') {
        return {
            data: result,
            isJson: false
        };
    }
    // Handle objects (already parsed)
    if (typeof result === 'object') {
        let raw;
        if (includeRaw) {
            try {
                raw = JSON.stringify(result);
            } catch (e) {
                // Handle circular references or other serialization errors
                raw = '[Object - Could not serialize: circular reference detected]';
                console.warn('[normalizeToolResult] Circular reference detected in tool result');
            }
        }
        return {
            data: result,
            isJson: true,
            ...raw && {
                raw
            }
        };
    }
    // Handle strings - try to parse as JSON if preferJson is true
    if (preferJson) {
        const parsed = safeJsonParse(result, false);
        if (typeof parsed !== 'string') {
            return {
                data: parsed,
                isJson: true,
                ...includeRaw && {
                    raw: result
                }
            };
        }
    }
    // Return as plain text
    return {
        data: result,
        isJson: false,
        ...includeRaw && {
            raw: result
        }
    };
}
function formatToolResultForLLM(result) {
    if (result.error) {
        return `Error: ${result.error}`;
    }
    const { data, isJson } = result;
    // Handle null/undefined
    if (data === null) return 'null';
    if (data === undefined) return 'undefined';
    // Handle primitives
    if (typeof data === 'string') return data;
    if (typeof data === 'number') return String(data);
    if (typeof data === 'boolean') return String(data);
    // Handle objects/arrays - stringify with pretty formatting
    if (isJson) {
        try {
            return JSON.stringify(data, null, 2);
        } catch (e) {
            // Handle circular references
            if (e instanceof Error && e.message.includes('circular')) {
                console.warn('[formatToolResultForLLM] Circular reference detected, using fallback');
                // Try to extract useful information without full serialization
                if (Array.isArray(data)) {
                    return `[Array with ${data.length} items - contains circular reference]`;
                }
                if (typeof data === 'object' && data !== null) {
                    const keys = Object.keys(data);
                    return `{Object with keys: ${keys.join(', ')} - contains circular reference}`;
                }
            }
            // Fallback to string representation
            return String(data);
        }
    }
    // Fallback
    return String(data);
}
function truncateContent(content, maxTokens = 15000) {
    if (!content) return '';
    const maxLength = maxTokens * 4;
    if (content.length <= maxLength) return content;
    return content.substring(0, maxLength) + `\n...[Content truncated. Original size: ${content.length} chars. Showing first ${maxLength} chars]`;
}
function wrapToolFunction(toolFn, options = {}) {
    const { name = 'unknown', normalize = true, maxSize = 50 * 1024 } = options;
    return async (input)=>{
        try {
            const result = await toolFn(input);
            // Convert result to string to check size
            let stringResult;
            if (!normalize) {
                stringResult = typeof result === 'string' ? result : JSON.stringify(result);
            } else {
                const normalized = normalizeToolResult(result, {
                    includeRaw: false
                });
                stringResult = formatToolResultForLLM(normalized);
            }
            // Check response size and truncate if necessary
            const sizeInBytes = Buffer.byteLength(stringResult, 'utf8');
            if (sizeInBytes > maxSize) {
                const sizeKB = (sizeInBytes / 1024).toFixed(2);
                const maxKB = (maxSize / 1024).toFixed(2);
                console.warn(`[Tool: ${name}] Response large: ${sizeKB}KB (max: ${maxKB}KB). Truncating...`);
                // Truncate to maxSize
                // Note: Buffer.byteLength is accurate but substring works on chars.
                // We'll approximate by chars for simplicity/speed.
                return stringResult.substring(0, maxSize) + `\n...[Content truncated. Original size: ${sizeKB}KB. Showing first ${maxKB}KB]`;
            }
            return stringResult;
        } catch (error) {
            const errorMessage = error instanceof Error ? error.message : 'Unknown error';
            const errorStack = error instanceof Error ? error.stack : undefined;
            console.error(`[Tool: ${name}] Error:`, {
                message: errorMessage,
                stack: errorStack,
                input: typeof input === 'string' ? input.substring(0, 100) : '[complex input]'
            });
            // Return structured error for better handling
            return JSON.stringify({
                error: true,
                message: errorMessage,
                tool: name,
                timestamp: new Date().toISOString()
            });
        }
    };
}
function parseToolCallResult(toolResult) {
    if (!toolResult) return null;
    // If it's already an object, return it
    if (typeof toolResult === 'object') {
        return toolResult;
    }
    // If it's a string, try to parse it
    if (typeof toolResult === 'string') {
        return safeJsonParse(toolResult, true);
    }
    // Return as-is for other types
    return toolResult;
}
function validateToolResult(result, options = {}) {
    const { requiredFields = [], expectedType } = options;
    if (result === null || result === undefined) {
        return {
            valid: false,
            error: 'Result is null or undefined'
        };
    }
    // Check expected type
    if (expectedType) {
        const actualType = Array.isArray(result) ? 'array' : typeof result;
        if (actualType !== expectedType) {
            return {
                valid: false,
                error: `Expected type ${expectedType}, got ${actualType}`
            };
        }
    }
    // Check required fields (for objects)
    if (requiredFields.length > 0 && typeof result === 'object' && result !== null) {
        for (const field of requiredFields){
            // Check if field exists
            if (!(field in result)) {
                return {
                    valid: false,
                    error: `Missing required field: ${field}`
                };
            }
            // Check if field has a value (not null or undefined)
            const value = result[field];
            if (value === null || value === undefined) {
                return {
                    valid: false,
                    error: `Required field '${field}' is null or undefined`
                };
            }
            // Check if string fields are not empty
            if (typeof value === 'string' && value.trim() === '') {
                return {
                    valid: false,
                    error: `Required field '${field}' is an empty string`
                };
            }
        }
    }
    return {
        valid: true
    };
}
}),
"[project]/lib/workflow/executors/tool-factory.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ToolFactory",
    ()=>ToolFactory
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$community$2f$dist$2f$retrievers$2f$tavily_search_api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@langchain/community/dist/retrievers/tavily_search_api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$community$2f$dist$2f$tools$2f$serper$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@langchain/community/dist/tools/serper.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$community$2f$dist$2f$tools$2f$serpapi$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@langchain/community/dist/tools/serpapi.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$tools$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@langchain/core/tools.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@langchain/core/dist/tools/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/index.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/cheerio/dist/esm/load-parse.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/tool-utils.ts [app-route] (ecmascript)");
;
;
;
;
;
;
class ToolFactory {
    static async createTool(toolConfig, apiKeys) {
        // Handle both 'id' and 'toolId' properties (UI sends 'toolId', but we expect 'id')
        const id = toolConfig.id || toolConfig.toolId;
        if (!id) {
            console.error('[ToolFactory] No id or toolId found in toolConfig:', toolConfig);
            return null;
        }
        switch(id){
            case "tavily-search":
                if (!apiKeys.tavily) {
                    return null;
                }
                // Tavily is a retriever, wrap it as a tool
                const retriever = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$community$2f$dist$2f$retrievers$2f$tavily_search_api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["TavilySearchAPIRetriever"]({
                    apiKey: apiKeys.tavily,
                    k: toolConfig.maxResults || 5
                });
                return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DynamicTool"]({
                    name: "tavily_search",
                    description: "Search the web using Tavily API. Input should be a search query string.",
                    func: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["wrapToolFunction"])(async (query)=>{
                        const docs = await retriever.invoke(query);
                        return docs.map((doc)=>doc.pageContent).join("\n\n");
                    }, {
                        name: "tavily_search"
                    })
                });
            case "serper-search":
                if (!apiKeys.serper) {
                    return null;
                }
                console.log('[ToolFactory] Creating serper_search tool');
                // Wrap LangChain's Serper tool for consistent error handling
                const serperTool = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$community$2f$dist$2f$tools$2f$serper$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["Serper"](apiKeys.serper);
                return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DynamicTool"]({
                    name: "serper_search",
                    description: "Search the web using Serper (Google Search API). Input should be a search query string.",
                    func: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["wrapToolFunction"])(async (input)=>{
                        return await serperTool.call(input);
                    }, {
                        name: "serper_search"
                    })
                });
            case "serpapi-search":
                if (!apiKeys.serpapi) {
                    console.warn('[ToolFactory] Missing SerpAPI key');
                    return null;
                }
                // Wrap LangChain's SerpAPI tool for consistent error handling
                const serpApiTool = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$community$2f$dist$2f$tools$2f$serpapi$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["SerpAPI"](apiKeys.serpapi);
                return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DynamicTool"]({
                    name: serpApiTool.name,
                    description: serpApiTool.description,
                    func: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["wrapToolFunction"])(async (input)=>{
                        return await serpApiTool.call(input);
                    }, {
                        name: "serpapi_search"
                    })
                });
            case "scraperapi":
                if (!apiKeys.scraperapi) return null;
                return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DynamicTool"]({
                    name: "scraperapi",
                    description: "Scrape a webpage using ScraperAPI to handle CAPTCHAs and IP rotation.",
                    func: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["wrapToolFunction"])(async (url)=>{
                        const params = new URLSearchParams({
                            api_key: apiKeys.scraperapi,
                            url: url,
                            render: toolConfig.renderJS ? "true" : "false"
                        });
                        const response = await fetch(`https://api.scraperapi.com/?${params.toString()}`);
                        return await response.text();
                    }, {
                        name: "scraperapi"
                    })
                });
            case "browserless":
                if (!apiKeys.browserless) return null;
                return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DynamicTool"]({
                    name: "browserless",
                    description: "Advanced web scraping using Browserless (Headless Chrome/Playwright). Supports JavaScript execution, screenshots, PDF generation, and custom selectors. Input should be a valid URL.",
                    func: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["wrapToolFunction"])(async (url)=>{
                        const { waitForSelector, executeScript, screenshot, pdf, timeout = 30000 } = toolConfig;
                        // Build the request payload for Browserless
                        const payload = {
                            url,
                            gotoOptions: {
                                waitUntil: 'networkidle2',
                                timeout
                            }
                        };
                        // Add wait for selector if specified
                        if (waitForSelector) {
                            payload.waitForSelector = {
                                selector: waitForSelector,
                                timeout
                            };
                        }
                        // Add JavaScript execution if specified
                        if (executeScript) {
                            payload.waitForFunction = {
                                fn: executeScript,
                                timeout
                            };
                        }
                        const results = {
                            url,
                            timestamp: new Date().toISOString()
                        };
                        // Handle screenshot request
                        if (screenshot) {
                            const screenshotResponse = await fetch(`https://chrome.browserless.io/screenshot?token=${apiKeys.browserless}`, {
                                method: "POST",
                                headers: {
                                    "Content-Type": "application/json"
                                },
                                body: JSON.stringify({
                                    ...payload,
                                    options: {
                                        fullPage: true,
                                        type: 'png'
                                    }
                                })
                            });
                            if (screenshotResponse.ok) {
                                const buffer = await screenshotResponse.arrayBuffer();
                                const base64 = Buffer.from(buffer).toString('base64');
                                results.screenshot = `data:image/png;base64,${base64}`;
                                results.screenshotSize = buffer.byteLength;
                            } else {
                                results.screenshotError = `Failed to capture screenshot: ${screenshotResponse.status}`;
                            }
                        }
                        // Handle PDF request
                        if (pdf) {
                            const pdfResponse = await fetch(`https://chrome.browserless.io/pdf?token=${apiKeys.browserless}`, {
                                method: "POST",
                                headers: {
                                    "Content-Type": "application/json"
                                },
                                body: JSON.stringify({
                                    ...payload,
                                    options: {
                                        printBackground: true,
                                        format: 'A4'
                                    }
                                })
                            });
                            if (pdfResponse.ok) {
                                const buffer = await pdfResponse.arrayBuffer();
                                const base64 = Buffer.from(buffer).toString('base64');
                                results.pdf = `data:application/pdf;base64,${base64}`;
                                results.pdfSize = buffer.byteLength;
                            } else {
                                results.pdfError = `Failed to generate PDF: ${pdfResponse.status}`;
                            }
                        }
                        // Always fetch the content
                        const contentResponse = await fetch(`https://chrome.browserless.io/content?token=${apiKeys.browserless}`, {
                            method: "POST",
                            headers: {
                                "Content-Type": "application/json"
                            },
                            body: JSON.stringify(payload)
                        });
                        if (contentResponse.ok) {
                            results.content = await contentResponse.text();
                        } else {
                            results.contentError = `Failed to fetch content: ${contentResponse.status}`;
                        }
                        // Return structured results
                        return results;
                    }, {
                        name: "browserless"
                    })
                });
            case "content-extractor":
                return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DynamicTool"]({
                    name: "content-extractor",
                    description: "Extract the main text content from a webpage HTML.",
                    func: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["wrapToolFunction"])(async (html)=>{
                        const $ = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$cheerio$2f$dist$2f$esm$2f$load$2d$parse$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["load"](html);
                        // Remove scripts, styles, and other non-content elements
                        $('script, style, nav, footer, header, aside, iframe, noscript').remove();
                        // Get text
                        let text = $('body').text();
                        // Safety check: ensure text is a string
                        if (!text || typeof text !== 'string') {
                            console.warn('[content-extractor] Failed to extract text from body, returning empty string');
                            return '';
                        }
                        // Clean up whitespace
                        text = text.replace(/\s+/g, ' ').trim();
                        return text;
                    }, {
                        name: "content-extractor"
                    })
                });
            case "firecrawl":
                if (!apiKeys.firecrawl) {
                    console.warn('[ToolFactory] Missing Firecrawl API key');
                    return null;
                }
                return new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$tools$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DynamicTool"]({
                    name: "firecrawl_scrape",
                    description: "Scrape and extract content from any website URL using Firecrawl. Returns clean, LLM-ready markdown content including main text, links, and images. Input should be a valid URL (e.g., 'https://example.com').",
                    func: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["wrapToolFunction"])(async (url)=>{
                        const mode = toolConfig.mode || 'scrape';
                        if (mode === 'scrape') {
                            // Single page scrape using Firecrawl v1 API
                            const response = await fetch('https://api.firecrawl.dev/v1/scrape', {
                                method: 'POST',
                                headers: {
                                    'Content-Type': 'application/json',
                                    'Authorization': `Bearer ${apiKeys.firecrawl}`
                                },
                                body: JSON.stringify({
                                    url: url,
                                    formats: [
                                        'markdown',
                                        'html'
                                    ]
                                })
                            });
                            if (!response.ok) {
                                const error = await response.text();
                                throw new Error(`Firecrawl API error: ${response.status} - ${error}`);
                            }
                            const result = await response.json();
                            if (!result.success) {
                                throw new Error(`Firecrawl scrape failed: ${result.error || 'Unknown error'}`);
                            }
                            // Return markdown content (best for LLMs)
                            return {
                                url: url,
                                markdown: result.data?.markdown || result.data?.content || '',
                                title: result.data?.metadata?.title || 'Unknown',
                                description: result.data?.metadata?.description || ''
                            };
                        } else {
                            // Crawl entire site using Firecrawl v1 API
                            const response = await fetch('https://api.firecrawl.dev/v1/crawl', {
                                method: 'POST',
                                headers: {
                                    'Content-Type': 'application/json',
                                    'Authorization': `Bearer ${apiKeys.firecrawl}`
                                },
                                body: JSON.stringify({
                                    url: url,
                                    limit: toolConfig.maxPages || 10,
                                    scrapeOptions: {
                                        formats: [
                                            'markdown'
                                        ]
                                    }
                                })
                            });
                            if (!response.ok) {
                                const error = await response.text();
                                throw new Error(`Firecrawl API error: ${response.status} - ${error}`);
                            }
                            const result = await response.json();
                            if (!result.success) {
                                throw new Error(`Firecrawl crawl failed: ${result.error || 'Unknown error'}`);
                            }
                            // Return array of pages
                            return result.data?.map((page)=>({
                                    url: page.url,
                                    markdown: page.markdown || page.content,
                                    title: page.metadata?.title || 'Unknown'
                                })) || [];
                        }
                    }, {
                        name: "firecrawl_scrape"
                    })
                });
            default:
                console.warn(`Unknown tool ID: ${id}`);
                return null;
        }
    }
}
}),
"[externals]/node:fs [external] (node:fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:fs", () => require("node:fs"));

module.exports = mod;
}),
"[externals]/node:process [external] (node:process, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:process", () => require("node:process"));

module.exports = mod;
}),
"[project]/lib/workflow/pdf-utils.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * PDF text extraction using pdf2json
 * This is a pure Node.js library without browser dependencies
 */ __turbopack_context__.s([
    "handler",
    ()=>handler
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$pdf2json$2f$dist$2f$pdfparser$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/pdf2json/dist/pdfparser.js [app-route] (ecmascript)");
;
async function handler(buffer) {
    try {
        console.log("[PDF] Starting PDF text extraction with pdf2json...");
        console.log("[PDF] Buffer size:", buffer?.length, "bytes");
        // Validate buffer
        if (!Buffer.isBuffer(buffer) || buffer.length === 0) {
            return `[PDF error: Invalid buffer provided]`;
        }
        return new Promise((resolve, reject)=>{
            const pdfParser = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$pdf2json$2f$dist$2f$pdfparser$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"]();
            // Set up event handlers
            pdfParser.on("pdfParser_dataError", (errData)=>{
                console.error("[PDF] Parse error:", errData.parserError);
                resolve(`[PDF content could not be extracted: ${errData.parserError}]`);
            });
            pdfParser.on("pdfParser_dataReady", (pdfData)=>{
                try {
                    // Extract text from all pages
                    let fullText = '';
                    // pdfData.Pages is an array of page objects
                    if (pdfData && pdfData.Pages && Array.isArray(pdfData.Pages)) {
                        for (const page of pdfData.Pages){
                            if (page.Texts && Array.isArray(page.Texts)) {
                                for (const textItem of page.Texts){
                                    if (textItem.R && Array.isArray(textItem.R)) {
                                        for (const run of textItem.R){
                                            if (run.T) {
                                                // Decode URI-encoded text
                                                try {
                                                    fullText += decodeURIComponent(run.T) + ' ';
                                                } catch (e) {
                                                    // Fallback for malformed URIs
                                                    fullText += run.T + ' ';
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                            fullText += '\n\n'; // Add page break
                        }
                    }
                    fullText = fullText.trim();
                    console.log(`[PDF] ✅ Successfully extracted ${fullText.length} characters from ${pdfData.Pages?.length || 0} pages`);
                    if (fullText.length === 0) {
                        resolve('[PDF appears to be empty or contains only images]');
                    } else {
                        resolve(fullText);
                    }
                } catch (error) {
                    console.error("[PDF] Error extracting text:", error);
                    resolve(`[PDF text extraction failed: ${error instanceof Error ? error.message : 'Unknown error'}]`);
                }
            });
            // Parse the buffer
            pdfParser.parseBuffer(buffer);
        });
    } catch (error) {
        console.error("[PDF] Error in PDF handler:", error);
        const errorMsg = error instanceof Error ? error.message : 'Unknown error';
        return `[PDF processing failed: ${errorMsg}]`;
    }
}
}),
"[project]/lib/workflow/file-utils.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "prefetchFileContents",
    ()=>prefetchFileContents
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2f$client$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/lib/convex/client.ts [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$pdf$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/pdf-utils.ts [app-route] (ecmascript)");
;
;
;
;
/**
 * Check if a file is a PDF based on MIME type or filename
 */ function isPdfFile(contentType, filename) {
    if (contentType && contentType.includes('pdf')) {
        return true;
    }
    if (filename && filename.toLowerCase().endsWith('.pdf')) {
        return true;
    }
    return false;
}
async function prefetchFileContents(text, state) {
    console.log('[FileUtils] ========== prefetchFileContents called ==========');
    console.log('[FileUtils] Text to scan:', text?.substring(0, 300));
    if (!text) {
        console.log('[FileUtils] No text provided, returning early');
        return;
    }
    const references = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["extractVariableReferences"])(text);
    console.log('[FileUtils] Found variable references:', references);
    const convex = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2f$client$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getConvexClient"])();
    // Track unique file IDs to fetch
    const filesToFetch = new Map();
    for (const ref of references){
        try {
            const value = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["evaluateExpression"])(ref, state);
            console.log(`[FileUtils] Evaluating "${ref}":`, {
                type: typeof value,
                isObject: typeof value === 'object',
                hasStorageId: !!value?.storageId,
                hasContent: !!value?.content,
                storageId: value?.storageId
            });
            // Check if it's a file object (has storageId) and missing content
            if (value && typeof value === 'object' && value.storageId && typeof value.storageId === 'string' && !value.content) {
                console.log(`[FileUtils] 🎯 Found file to fetch: ${ref} with storageId: ${value.storageId}`);
                // Use the storageId as the key to avoid duplicates
                if (!filesToFetch.has(value.storageId)) {
                    filesToFetch.set(value.storageId, {
                        storageId: value.storageId,
                        variablePath: ref
                    });
                }
            }
        } catch (e) {
            console.log(`[FileUtils] Error evaluating "${ref}":`, e);
        }
    }
    if (filesToFetch.size === 0) {
        console.log('[FileUtils] No files to fetch, returning early');
        return;
    }
    console.log(`[FileUtils] Prefetching content for ${filesToFetch.size} files...`);
    // Fetch all files in parallel
    await Promise.all(Array.from(filesToFetch.values()).map(async ({ storageId, variablePath })=>{
        try {
            // Get the file object from state to check for filename
            const fileObj = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["evaluateExpression"])(variablePath, state);
            const filename = fileObj?.originalFilename || fileObj?.name || null;
            // Get file metadata to determine file type
            let metadata;
            try {
                metadata = await convex.query(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].files.getFileMetadata, {
                    storageId
                });
                if (!metadata) {
                    console.error(`[FileUtils] ❌ No metadata found for storageId: ${storageId}`);
                    console.error(`[FileUtils] This likely means the file doesn't exist in Convex storage`);
                    return;
                }
                console.log(`[FileUtils] File metadata:`, {
                    contentType: metadata.contentType,
                    size: metadata.size,
                    sha256: metadata.sha256
                });
            } catch (err) {
                console.error(`[FileUtils] ❌ Error fetching metadata for ${storageId}:`, err);
                return;
            }
            const contentType = metadata?.contentType || null;
            // Get the download URL
            let url;
            try {
                url = await convex.query(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].files.getDownloadUrl, {
                    storageId
                });
                if (!url) {
                    console.error(`[FileUtils] ❌ No download URL returned for storageId: ${storageId}`);
                    console.error(`[FileUtils] This usually means the file exists but Convex couldn't generate a URL`);
                    return;
                }
                console.log(`[FileUtils] Download URL obtained (length: ${url.length})`);
            } catch (err) {
                console.error(`[FileUtils] ❌ Error getting download URL for ${storageId}:`, err);
                return;
            }
            // Fetch the content
            console.log(`[FileUtils] Fetching file content from URL...`);
            const response = await fetch(url);
            if (!response.ok) {
                console.error(`[FileUtils] ❌ Failed to fetch file content:`, {
                    status: response.status,
                    statusText: response.statusText,
                    url: url.substring(0, 100) + '...'
                });
                return;
            }
            console.log(`[FileUtils] ✅ File fetch successful, response size: ${response.headers.get('content-length') || 'unknown'}`);
            let content;
            // Check if this is a PDF file
            if (isPdfFile(contentType, filename)) {
                console.log(`[FileUtils] Detected PDF file: ${filename || storageId}`);
                // Fetch as array buffer for PDF processing
                const arrayBuffer = await response.arrayBuffer();
                const buffer = Buffer.from(arrayBuffer);
                // Extract text from PDF
                content = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$pdf$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["handler"])(buffer);
            // console.log(`[FileUtils] Extracted ${content.length} characters from PDF`);
            } else {
                // Read as text for non-PDF files
                content = await response.text();
            }
            // Inject content back into the state variable
            const value = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["evaluateExpression"])(variablePath, state);
            console.log(`[FileUtils] Variable path: ${variablePath}, value type: ${typeof value}, has storageId: ${!!value?.storageId}`);
            if (value && typeof value === 'object') {
                // Store both raw content and a convenient 'text' field
                value.content = content;
                // Some downstream code expects a 'text' property
                value.text = content;
                console.log(`[FileUtils] ✅ Injected ${content.length} characters into ${variablePath}`);
                console.log(`[FileUtils] Content preview: ${content.substring(0, 200)}...`);
                console.log(`[FileUtils] Verify - value.content exists: ${!!value.content}, length: ${value.content?.length}`);
                console.log(`[FileUtils] Verify - value.text exists: ${!!value.text}, length: ${value.text?.length}`);
                console.log(`[FileUtils] Full value keys:`, Object.keys(value));
            } else {
                console.error(`[FileUtils] ❌ Failed to inject content - value is not an object or is null`);
            }
        } catch (error) {
            console.error(`[FileUtils] Error fetching file ${storageId}:`, error);
        }
    }));
    console.log('[FileUtils] ========== prefetchFileContents completed ==========');
}
}),
"[project]/lib/workflow/executors/mcp-utils.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "convertMcpToOpenAiTool",
    ()=>convertMcpToOpenAiTool,
    "executeMcpTool",
    ()=>executeMcpTool,
    "fetchMcpTools",
    ()=>fetchMcpTools,
    "unwrapMCPResponse",
    ()=>unwrapMCPResponse
]);
function unwrapMCPResponse(response, serverName = 'MCP') {
    if (response && typeof response === 'object') {
        // Handle standard MCP result structure
        if (response.content && Array.isArray(response.content)) {
            // Extract text content from the array
            const textContent = response.content.filter((item)=>item.type === 'text').map((item)=>item.text).join('\n');
            if (textContent) return textContent;
            // If no text but has content, return the first item or the whole array
            return response.content.length === 1 ? response.content[0] : response.content;
        }
        // Handle direct result property (common in some implementations)
        if (response.result) {
            return unwrapMCPResponse(response.result, serverName);
        }
    }
    return response;
}
function convertMcpToOpenAiTool(mcp) {
    // Support both 'schema' and 'input_schema' (AdarshMCP uses input_schema)
    const schema = mcp.schema || mcp.input_schema || {};
    // Clean properties: remove unsupported fields like 'examples', 'default', 'arguments'
    // Google Gemini API only supports: type, description, enum, items, properties, required
    const cleanProperties = (props)=>{
        if (!props || typeof props !== 'object') return props;
        const cleaned = {};
        for (const [key, value] of Object.entries(props)){
            if (typeof value === 'object' && value !== null) {
                const propCopy = {};
                // Only keep standard JSON Schema fields
                if ('type' in value) propCopy.type = value.type;
                if ('description' in value) propCopy.description = value.description;
                if ('enum' in value) propCopy.enum = value.enum;
                if ('items' in value) propCopy.items = value.items;
                if ('properties' in value) propCopy.properties = cleanProperties(value.properties);
                if ('required' in value) propCopy.required = value.required;
                cleaned[key] = propCopy;
            } else {
                cleaned[key] = value;
            }
        }
        return cleaned;
    };
    const toolName = mcp.name || mcp.toolName || mcp.function && mcp.function.name || 'unknown_tool';
    if (toolName === 'unknown_tool') {
        console.warn('MCP tool has no name:', JSON.stringify(mcp));
    }
    // Force safe schema for Firecrawl tools to prevent validation errors
    if (toolName.includes('firecrawl')) {
        if (toolName.includes('scrape')) {
            schema.properties = {
                url: {
                    type: 'string',
                    description: 'The URL to scrape (e.g. https://example.com)'
                },
                formats: {
                    type: 'array',
                    items: {
                        type: 'string'
                    },
                    description: 'Formats to return (markdown, html, etc.)'
                }
            };
            schema.required = [
                'url'
            ];
        } else if (toolName.includes('crawl')) {
            schema.properties = {
                url: {
                    type: 'string',
                    description: 'The URL to start crawling from'
                },
                limit: {
                    type: 'number',
                    description: 'Maximum number of pages to crawl'
                },
                scrapeOptions: {
                    type: 'object',
                    properties: {
                        formats: {
                            type: 'array',
                            items: {
                                type: 'string'
                            }
                        }
                    }
                }
            };
            schema.required = [
                'url'
            ];
        } else if (toolName.includes('search')) {
            schema.properties = {
                query: {
                    type: 'string',
                    description: 'The search query (e.g. "Diageo competitors"). Do NOT pass a URL here.'
                }
            };
            schema.required = [
                'query'
            ];
        }
    } else if (Object.keys(schema).length === 0) {
    // ... existing fallback for other tools ...
    }
    return {
        type: "function",
        function: {
            name: toolName,
            description: mcp.description || 'No description',
            parameters: {
                type: "object",
                properties: cleanProperties(schema.properties || {}),
                required: schema.required || []
            }
        }
    };
}
async function executeMcpTool(mcpServer, toolName, args, apiKeys = {}) {
    // Fix: Firecrawl search tool robust input sanitization
    if (toolName.includes('firecrawl_search')) {
        if (!args) {
            console.log('[MCP] Input is null/undefined for firecrawl_search, defaulting to empty object');
            args = {};
        }
        if (typeof args === 'string') {
            console.log('[MCP] Converting string input to query object for firecrawl_search');
            args = {
                query: args
            };
        } else if (typeof args === 'object') {
            // Map aliases and fallback
            if (!args.query) {
                const mappedQuery = args.url || args.input || args.topic || args.q || args.search_query || args.search || args.text || args.content;
                if (mappedQuery) {
                    console.log('[MCP] Mapping alias to query for firecrawl_search:', mappedQuery);
                    args.query = mappedQuery;
                } else {
                    const firstStringKey = Object.keys(args).find((k)=>typeof args[k] === 'string');
                    if (firstStringKey) {
                        console.log(`[MCP] Fallback: using property '${firstStringKey}' as query`);
                        args.query = args[firstStringKey];
                    } else {
                        console.log('[MCP] Last resort: stringifying input object as query');
                        args.query = JSON.stringify(args);
                    }
                }
            }
            // Remove sources if present
            if (args.sources) {
                console.log(`[MCP] Removing invalid 'sources' parameter from ${toolName}`);
                delete args.sources;
            }
        }
    }
    // Replace URL placeholders
    const resolvedMcpUrl = mcpServer.url && mcpServer.url.includes('{FIRECRAWL_API_KEY}') ? mcpServer.url.replace('{FIRECRAWL_API_KEY}', encodeURIComponent(apiKeys.firecrawl || '')) : mcpServer.url || '';
    // Support both authToken and accessToken for backward compatibility
    let authToken = mcpServer.authToken || mcpServer.accessToken;
    // Auto-inject Firecrawl API key if missing and server is Firecrawl
    if (!authToken && (mcpServer.name?.toLowerCase().includes('firecrawl') || mcpServer.url?.includes('firecrawl'))) {
        authToken = apiKeys.firecrawl;
    }
    const mcpResponse = await fetch(resolvedMcpUrl, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json, text/event-stream',
            ...authToken && {
                'Authorization': `Bearer ${authToken}`
            }
        },
        body: JSON.stringify({
            jsonrpc: '2.0',
            id: Date.now(),
            method: 'tools/call',
            params: {
                name: toolName,
                arguments: args
            }
        })
    });
    const contentType = mcpResponse.headers.get('content-type');
    let result;
    if (contentType && contentType.includes('text/event-stream')) {
        const text = await mcpResponse.text();
        // Parse SSE events
        const lines = text.split('\n');
        for (const line of lines){
            if (line.startsWith('data: ')) {
                try {
                    const data = JSON.parse(line.slice(6));
                    if (data.result || data.error) {
                        result = data;
                        break;
                    }
                } catch (e) {
                // Ignore parse errors for intermediate lines
                }
            }
        }
        if (!result) {
            // Fallback: try to parse the whole text if it's not standard SSE but just JSON with wrong header
            try {
                result = JSON.parse(text);
            } catch (e) {
                throw new Error(`Failed to parse SSE response from ${mcpServer.name}. Raw text: ${text.substring(0, 500)}`);
            }
        }
    } else {
        result = await mcpResponse.json();
    }
    if (result && result.error) {
        throw new Error(`MCP error ${result.error.code}: ${result.error.message}`);
    }
    // Unwrap the response
    return unwrapMCPResponse(result, mcpServer.name);
}
async function fetchMcpTools(mcpServer, apiKeys = {}) {
    // Replace URL placeholders
    const resolvedMcpUrl = mcpServer.url && mcpServer.url.includes('{FIRECRAWL_API_KEY}') ? mcpServer.url.replace('{FIRECRAWL_API_KEY}', encodeURIComponent(apiKeys.firecrawl || '')) : mcpServer.url || '';
    // Support both authToken and accessToken for backward compatibility
    const authToken = mcpServer.authToken || mcpServer.accessToken;
    try {
        // Try JSON-RPC first (Standard MCP)
        const rpcResponse = await fetch(resolvedMcpUrl, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Accept': 'application/json, text/event-stream',
                ...authToken && {
                    'Authorization': `Bearer ${authToken}`
                }
            },
            body: JSON.stringify({
                jsonrpc: '2.0',
                id: Date.now(),
                method: 'tools/list',
                params: {}
            })
        });
        if (rpcResponse.ok) {
            const contentType = rpcResponse.headers.get('content-type');
            let data;
            if (contentType && contentType.includes('text/event-stream')) {
                const text = await rpcResponse.text();
                const lines = text.split('\n');
                for (const line of lines){
                    if (line.startsWith('data: ')) {
                        try {
                            const parsed = JSON.parse(line.slice(6));
                            if (parsed.result) {
                                data = parsed;
                                break;
                            }
                        } catch (e) {}
                    }
                }
            } else {
                data = await rpcResponse.json();
            }
            if (data && data.result && data.result.tools) {
                return data.result.tools;
            }
        }
        // Fallback to GET /tools/list (AdarshMCP specific?)
        const getUrl = resolvedMcpUrl.endsWith('/') ? `${resolvedMcpUrl}tools/list` : `${resolvedMcpUrl}/tools/list`;
        const getResponse = await fetch(getUrl, {
            headers: {
                'Accept': 'application/json, text/event-stream',
                ...authToken && {
                    'Authorization': `Bearer ${authToken}`
                }
            }
        });
        if (getResponse.ok) {
            const contentType = getResponse.headers.get('content-type');
            let data;
            if (contentType && contentType.includes('text/event-stream')) {
                const text = await getResponse.text();
                const lines = text.split('\n');
                for (const line of lines){
                    if (line.startsWith('data: ')) {
                        try {
                            const parsed = JSON.parse(line.slice(6));
                            if (parsed.tools || parsed.result && parsed.result.tools) {
                                data = parsed;
                                break;
                            }
                        } catch (e) {}
                    }
                }
            } else {
                data = await getResponse.json();
            }
            if (data) {
                if (data.tools) return data.tools;
                if (data.result && data.result.tools) return data.result.tools;
            }
        }
        console.warn(`Failed to fetch tools from ${mcpServer.name}`);
        return [];
    } catch (error) {
        console.error(`Error fetching tools from ${mcpServer.name}:`, error);
        return [];
    }
}
}),
"[project]/lib/config/llm-config.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * LLM Configuration
 *
 * Configure available LLM providers and models
 * API keys are still in .env.local for security
 */ __turbopack_context__.s([
    "formatModelId",
    ()=>formatModelId,
    "getAllModels",
    ()=>getAllModels,
    "getConfiguredProviders",
    ()=>getConfiguredProviders,
    "getDefaultModel",
    ()=>getDefaultModel,
    "getModelInfo",
    ()=>getModelInfo,
    "getModelsForProvider",
    ()=>getModelsForProvider,
    "isProviderConfigured",
    ()=>isProviderConfigured,
    "llmProviders",
    ()=>llmProviders
]);
const llmProviders = [
    {
        id: 'anthropic',
        name: 'Anthropic',
        envKey: 'ANTHROPIC_API_KEY',
        defaultModel: 'claude-sonnet-4-5-20250929',
        models: [
            {
                id: 'claude-sonnet-4-5-20250929',
                name: 'Claude Sonnet 4.5',
                provider: 'anthropic',
                contextWindow: 200000,
                inputCostPer1M: 3.00,
                outputCostPer1M: 15.00,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 8192,
                description: 'Latest Sonnet - balanced performance for most tasks'
            },
            {
                id: 'claude-opus-4-5-20251101',
                name: 'Claude Opus 4.5',
                provider: 'anthropic',
                contextWindow: 200000,
                inputCostPer1M: 15.00,
                outputCostPer1M: 75.00,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 8192,
                description: 'Most intelligent model - best for complex reasoning'
            },
            {
                id: 'claude-haiku-4-5-20251001',
                name: 'Claude Haiku 4.5',
                provider: 'anthropic',
                contextWindow: 200000,
                inputCostPer1M: 1.00,
                outputCostPer1M: 5.00,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 8192,
                description: 'Fastest and most cost-effective model'
            }
        ]
    },
    {
        id: 'openai',
        name: 'OpenAI',
        envKey: 'OPENAI_API_KEY',
        defaultModel: 'gpt-4o',
        models: [
            {
                id: 'gpt-4o',
                name: 'GPT-4o',
                provider: 'openai',
                contextWindow: 128000,
                inputCostPer1M: 2.50,
                outputCostPer1M: 10.00,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 16384,
                description: 'Multimodal flagship model with function calling'
            },
            {
                id: 'gpt-4o-mini',
                name: 'GPT-4o Mini',
                provider: 'openai',
                contextWindow: 128000,
                inputCostPer1M: 0.15,
                outputCostPer1M: 0.60,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 16384,
                description: 'Affordable and fast with function calling'
            }
        ]
    },
    {
        id: 'groq',
        name: 'Groq',
        envKey: 'GROQ_API_KEY',
        defaultModel: 'llama-3.3-70b-versatile',
        models: [
            {
                id: 'llama-3.3-70b-versatile',
                name: 'Llama 3.3 70B Versatile',
                provider: 'groq',
                contextWindow: 131072,
                inputCostPer1M: 0.59,
                outputCostPer1M: 0.79,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 32768,
                description: 'Most capable Llama model - excellent reasoning and instruction following'
            },
            {
                id: 'llama-3.1-8b-instant',
                name: 'Llama 3.1 8B Instant',
                provider: 'groq',
                contextWindow: 131072,
                inputCostPer1M: 0.05,
                outputCostPer1M: 0.08,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 131072,
                description: 'Ultra-fast and cost-effective - 560 tokens/second'
            },
            {
                id: 'openai/gpt-oss-120b',
                name: 'GPT OSS 120B',
                provider: 'groq',
                contextWindow: 131072,
                inputCostPer1M: 0.20,
                outputCostPer1M: 0.20,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 65536,
                description: 'Large Responses API model with extended output'
            },
            {
                id: 'openai/gpt-oss-20b',
                name: 'GPT OSS 20B',
                provider: 'groq',
                contextWindow: 131072,
                inputCostPer1M: 0.10,
                outputCostPer1M: 0.10,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 65536,
                description: 'Balanced model with high speed - 1000 tokens/second'
            }
        ]
    },
    {
        id: 'google',
        name: 'Google',
        envKey: 'GOOGLE_API_KEY',
        defaultModel: 'gemini-2.0-flash-exp',
        models: [
            {
                id: 'gemini-2.0-flash-exp',
                name: 'Gemini 2.0 Flash (Experimental)',
                provider: 'google',
                contextWindow: 1000000,
                inputCostPer1M: 0.00,
                outputCostPer1M: 0.00,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 8192,
                description: 'Free experimental preview - next-gen multimodal model'
            },
            {
                id: 'gemini-2.0-flash-001',
                name: 'Gemini 2.0 Flash',
                provider: 'google',
                contextWindow: 1000000,
                inputCostPer1M: 0.10,
                outputCostPer1M: 0.40,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 8192,
                description: 'Stable GA version - faster and more capable'
            },
            {
                id: 'gemini-2.0-flash-lite-preview-02-05',
                name: 'Gemini 2.0 Flash-Lite',
                provider: 'google',
                contextWindow: 1000000,
                inputCostPer1M: 0.075,
                outputCostPer1M: 0.30,
                supportsJSON: true,
                supportsMCP: true,
                maxTokens: 8192,
                description: 'Cost-effective version of Gemini 2.0 Flash'
            }
        ]
    }
];
function getDefaultModel(provider) {
    const config = llmProviders.find((p)=>p.id === provider);
    return config?.defaultModel || '';
}
function getModelsForProvider(provider) {
    const config = llmProviders.find((p)=>p.id === provider);
    return config?.models || [];
}
function getModelInfo(fullModelId) {
    const [provider, modelId] = fullModelId.split('/');
    const providerConfig = llmProviders.find((p)=>p.id === provider);
    if (!providerConfig) return null;
    return providerConfig.models.find((m)=>m.id === modelId) || null;
}
function formatModelId(provider, modelId) {
    return `${provider}/${modelId}`;
}
function getAllModels() {
    return llmProviders.flatMap((provider)=>provider.models.map((model)=>({
                ...model,
                fullId: `${provider.id}/${model.id}`
            })));
}
function isProviderConfigured(provider) {
    const config = llmProviders.find((p)=>p.id === provider);
    if (!config) return false;
    // This only works server-side
    if (typeof process === 'undefined') return false;
    return !!process.env[config.envKey];
}
function getConfiguredProviders() {
    return llmProviders.filter((p)=>isProviderConfigured(p.id)).map((p)=>p.id);
}
}),
"[project]/lib/api/models.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Model validation and configuration for LLM providers
 */ __turbopack_context__.s([
    "DEFAULT_MODELS",
    ()=>DEFAULT_MODELS,
    "SUPPORTED_MODELS",
    ()=>SUPPORTED_MODELS,
    "ensureOpenAIModel",
    ()=>ensureOpenAIModel,
    "getDefaultModel",
    ()=>getDefaultModel,
    "getModelString",
    ()=>getModelString,
    "isSupportedProvider",
    ()=>isSupportedProvider,
    "parseModelString",
    ()=>parseModelString,
    "validateModel",
    ()=>validateModel
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/config/llm-config.ts [app-route] (ecmascript)");
;
const SUPPORTED_MODELS = {
    openai: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'openai')?.models.map((m)=>m.id) || [],
    anthropic: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'anthropic')?.models.map((m)=>m.id) || [],
    groq: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'groq')?.models.map((m)=>m.id) || [],
    google: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'google')?.models.map((m)=>m.id) || []
};
const DEFAULT_MODELS = {
    openai: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'openai')?.defaultModel || 'gpt-4o',
    anthropic: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'anthropic')?.defaultModel || 'claude-sonnet-4-5-20250929',
    groq: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'groq')?.defaultModel || 'gpt-oss-120b',
    google: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$config$2f$llm$2d$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["llmProviders"].find((p)=>p.id === 'google')?.defaultModel || 'gemini-2.0-flash-exp'
};
function parseModelString(modelString) {
    if (!modelString) {
        return {
            provider: 'openai',
            modelName: DEFAULT_MODELS.openai
        };
    }
    if (modelString.includes('/')) {
        const [provider, modelName] = modelString.split('/', 2);
        // Validate provider
        if (provider !== 'openai' && provider !== 'anthropic' && provider !== 'groq' && provider !== 'google') {
            // Default to openai if provider is unknown
            return {
                provider: 'openai',
                modelName: DEFAULT_MODELS.openai
            };
        }
        return {
            provider: provider,
            modelName
        };
    }
    // No provider prefix, default to openai
    return {
        provider: 'openai',
        modelName: modelString
    };
}
function validateModel(modelString) {
    const { provider, modelName } = parseModelString(modelString);
    // Check if model is in supported list
    const supportedModels = SUPPORTED_MODELS[provider];
    const isValid = supportedModels.includes(modelName);
    if (!isValid) {
        return {
            provider,
            modelName,
            isValid: false,
            error: `Model '${modelName}' is not supported for provider '${provider}'. Supported models: ${supportedModels.join(', ')}`
        };
    }
    return {
        provider,
        modelName,
        isValid: true
    };
}
function getDefaultModel(provider) {
    return DEFAULT_MODELS[provider];
}
function isSupportedProvider(provider) {
    return provider === 'openai' || provider === 'anthropic' || provider === 'groq' || provider === 'google';
}
function getModelString(provider, modelName) {
    return `${provider}/${modelName}`;
}
function ensureOpenAIModel(modelString) {
    const { provider, modelName } = parseModelString(modelString);
    // If it's already an OpenAI model, return it
    if (provider === 'openai') {
        // Validate it's a supported OpenAI model
        const validation = validateModel(modelString);
        if (validation.isValid) {
            return modelName;
        }
        // Fall back to default if invalid
        return DEFAULT_MODELS.openai;
    }
    // For non-OpenAI providers, return default OpenAI model
    return DEFAULT_MODELS.openai;
}
}),
"[project]/lib/workflow/executors/agent.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeAgentNode",
    ()=>executeAgentNode
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mcp$2f$resolver$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/mcp/resolver.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$factory$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/tool-factory.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$utils$2f$function_calling$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@langchain/core/utils/function_calling.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$utils$2f$function_calling$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@langchain/core/dist/utils/function_calling.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/tool-utils.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$file$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/file-utils.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/mcp-utils.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/api/models.ts [app-route] (ecmascript)");
;
;
;
;
;
;
;
;
async function executeAgentNode(node, state, apiKeys) {
    const { data } = node;
    // Log available keys (security: only log names)
    console.log('[Agent] Available API Keys:', Object.keys(apiKeys || {}).filter((k)=>!!apiKeys[k]));
    console.log('[Agent] Selected Tools Config:', JSON.stringify(data.selectedTools || [], null, 2));
    try {
        // 1. Migrate data if using old format
        const migratedData = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mcp$2f$resolver$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["migrateMCPData"])(data);
        // 2. Resolve MCP server IDs to full configurations
        let mcpTools = migratedData.mcpTools || [];
        if (migratedData.mcpServerIds && migratedData.mcpServerIds.length > 0) {
            mcpTools = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mcp$2f$resolver$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveMCPServers"])(migratedData.mcpServerIds);
        }
        // Filter out invalid MCP tool entries
        mcpTools = mcpTools.filter((mcp)=>{
            if (!mcp) {
                console.warn('[Agent] Filtered out null/undefined MCP tool');
                return false;
            }
            if (!mcp.name && !mcp.toolName) {
                console.warn('[Agent] Filtered out MCP tool without name:', mcp);
                return false;
            }
            return true;
        });
        // Flatten tools from all MCP servers
        const flattenedMcpTools = (await Promise.all(mcpTools.map(async (server)=>{
            const url = server.url && server.url.includes('{FIRECRAWL_API_KEY}') ? server.url.replace('{FIRECRAWL_API_KEY}', encodeURIComponent(apiKeys?.firecrawl || '')) : server.url || '';
            if (server.name.toLowerCase().includes('firecrawl') && !apiKeys?.firecrawl) {
                console.warn(`[Tools] ⚠️ Firecrawl tool detected but no API key provided in apiKeys.firecrawl`);
            }
            let tools = server.availableTools || [];
            // Check if tools are just strings (names) or missing, and fetch if needed
            if (tools.length === 0 || tools.length > 0 && typeof tools[0] === 'string') {
                console.log(`[Tools] Fetching full tool definitions for ${server.name}...`);
                const fetchedTools = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["fetchMcpTools"])(server, apiKeys);
                console.log(`[Tools] Fetched ${fetchedTools.length} tools from ${server.name}`);
                if (fetchedTools.length > 0) {
                    tools = fetchedTools;
                } else {
                    console.warn(`[Tools] ⚠️ Failed to fetch tools from ${server.name} or no tools available.`);
                }
            }
            return tools.map((tool)=>{
                const toolObj = typeof tool === 'string' ? {
                    name: tool
                } : tool;
                return {
                    ...toolObj,
                    serverName: server.name,
                    serverUrl: url,
                    serverAuthToken: server.accessToken
                };
            });
        }))).flat();
        console.log(`[Tools] Total flattened MCP tools: ${flattenedMcpTools.length}`);
        // 3. Prefetch file contents
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$file$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["prefetchFileContents"])(migratedData.instructions || '', state);
        // 4. Substitute Variables
        const instructions = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(migratedData.instructions || '', state);
        // 5. Instantiate Standard Tools
        const standardTools = [];
        if (data.selectedTools && Array.isArray(data.selectedTools)) {
            for (const toolConfig of data.selectedTools){
                try {
                    const tool = await __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$factory$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ToolFactory"].createTool(toolConfig, apiKeys || {});
                    if (tool) {
                        standardTools.push(tool);
                    }
                } catch (error) {
                    const toolId = toolConfig.toolId || toolConfig.id;
                    console.error(`Failed to instantiate tool ${toolId}:`, error);
                }
            }
        }
        // Debug logging
        if ("TURBOPACK compile-time truthy", 1) {
            console.log(`[Tools] MCP tools count: ${mcpTools.length}`, mcpTools.map((m)=>({
                    name: m?.name,
                    hasUrl: !!m?.url
                })));
            console.log(`[Tools] Created ${standardTools.length} standard tools:`, standardTools.map((t)=>t?.name || 'unnamed'));
        }
        // 6. Validate API Keys
        if (!apiKeys) {
            throw new Error('API keys are required for server-side execution');
        }
        // 7. Mock Response Check (Server-side execution only)
        if (process.env.MOCK_AGENT_RESPONSE) {
            let mockConfig = process.env.MOCK_AGENT_RESPONSE;
            try {
                mockConfig = JSON.parse(process.env.MOCK_AGENT_RESPONSE);
            } catch (e) {
            // Keep raw string if parsing fails
            }
            let mockOutput = mockConfig;
            if (mockConfig && typeof mockConfig === 'object') {
                const nodeKey = node.id;
                const nodeName = node.data.nodeName;
                mockOutput = mockConfig[nodeKey] ?? (nodeName ? mockConfig[nodeName] : undefined) ?? mockConfig.default ?? mockOutput;
            }
            if (mockOutput !== undefined) {
                const mockChatUpdates = data.includeChatHistory ? [
                    {
                        role: 'user',
                        content: data.instructions || ''
                    },
                    {
                        role: 'assistant',
                        content: typeof mockOutput === 'string' ? mockOutput : JSON.stringify(mockOutput)
                    }
                ] : [];
                return {
                    __agentValue: mockOutput,
                    __agentToolCalls: [],
                    __chatHistoryUpdates: mockChatUpdates,
                    __variableUpdates: {
                        lastOutput: mockOutput
                    }
                };
            }
        }
        // 8. Prepare Execution Context
        const contextualPrompt = instructions;
        const messages = data.includeChatHistory && state.chatHistory.length > 0 ? [
            ...state.chatHistory,
            {
                role: 'user',
                content: contextualPrompt
            }
        ] : [
            {
                role: 'user',
                content: contextualPrompt
            }
        ];
        // Parse model string
        let modelString = data.model || `anthropic/${__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DEFAULT_MODELS"].anthropic}`;
        let provider;
        let modelName;
        if (modelString.includes('/')) {
            const firstSlashIndex = modelString.indexOf('/');
            provider = modelString.substring(0, firstSlashIndex);
            modelName = modelString.substring(firstSlashIndex + 1);
        } else {
            provider = 'openai';
            modelName = modelString;
        }
        let usage = {
            input_tokens: 0,
            output_tokens: 0,
            total_tokens: 0,
            prompt_tokens: 0,
            completion_tokens: 0
        };
        let toolCalls = [];
        let responseText = '';
        const hasMcpTools = mcpTools.length > 0;
        const maxTokens = data.tokenLimit || 4096;
        // 9. Execute based on Provider
        if (provider === 'anthropic' && apiKeys?.anthropic) {
            // --- Anthropic Implementation ---
            const Anthropic = (await __turbopack_context__.A("[project]/node_modules/@anthropic-ai/sdk/index.mjs [app-route] (ecmascript, async loader)")).default;
            const client = new Anthropic({
                apiKey: apiKeys.anthropic
            });
            if (hasMcpTools || standardTools.length > 0) {
                const arcadeTools = mcpTools.filter((mcp)=>mcp.name?.toLowerCase().includes('arcade'));
                const realMcpTools = mcpTools.filter((mcp)=>!mcp.name?.toLowerCase().includes('arcade'));
                if (arcadeTools.length > 0) {
                    console.warn('⚠️ Arcade tools detected in MCP config - these will be skipped');
                }
                const mcpServers = realMcpTools.map((mcp)=>{
                    const url = mcp.url && mcp.url.includes('{FIRECRAWL_API_KEY}') ? mcp.url.replace('{FIRECRAWL_API_KEY}', encodeURIComponent(apiKeys.firecrawl || '')) : mcp.url || '';
                    // Validate that we have required fields
                    if (!url || !mcp.name) {
                        console.warn(`⚠️ Skipping invalid MCP server: ${mcp.name || 'unnamed'} - missing URL or name`);
                        return null;
                    }
                    const server = {
                        type: 'url',
                        url: url,
                        name: mcp.name
                    };
                    // Only include authorization_token if it exists (some MCP servers don't require auth)
                    if (mcp.accessToken) {
                        server.authorization_token = mcp.accessToken;
                    }
                    return server;
                }).filter(Boolean).filter((s)=>!s.name.toLowerCase().includes('firecrawl'));
                let useManualToolCalling = false;
                let anthropicError = null;
                const manualMcpTools = flattenedMcpTools.filter((t)=>t.name.includes('firecrawl') || t.serverName?.toLowerCase().includes('firecrawl'));
                const finalTools = [
                    ...standardTools.map((t)=>{
                        const openAITool = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$utils$2f$function_calling$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToOpenAITool"])(t);
                        return {
                            name: openAITool.function.name,
                            description: openAITool.function.description,
                            input_schema: openAITool.function.parameters
                        };
                    }),
                    ...flattenedMcpTools.map((t)=>{
                        const openAITool = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertMcpToOpenAiTool"])(t);
                        return {
                            name: openAITool.function.name,
                            description: openAITool.function.description,
                            input_schema: openAITool.function.parameters
                        };
                    })
                ].filter((tool, index, self)=>index === self.findIndex((t)=>t.name === tool.name));
                console.log('[Agent] Final Tools passed to LLM:', JSON.stringify(finalTools.map((t)=>({
                        name: t.name,
                        description: t.description
                    })), null, 2));
                console.log('[Agent] MCP Servers configured:', mcpServers.length);
                console.log('[Agent] Standard Tools count:', standardTools.length);
                console.log('[Agent] MCP Tools count:', flattenedMcpTools.length);
                console.log('[Agent] Messages to LLM:', JSON.stringify(messages, null, 2));
                try {
                    const response = await client.beta.messages.create({
                        model: modelName,
                        max_tokens: maxTokens,
                        messages: messages,
                        mcp_servers: mcpServers.length > 0 ? mcpServers : undefined,
                        tools: finalTools,
                        betas: mcpServers.length > 0 ? [
                            'mcp-client-2025-04-04'
                        ] : undefined
                    });
                    console.log('[Agent] Response from LLM:', {
                        model: response.model,
                        stopReason: response.stop_reason,
                        contentTypes: response.content.map((c)=>c.type),
                        toolUsesCount: response.content.filter((c)=>c.type === 'tool_use' || c.type === 'mcp_tool_use').length,
                        textBlocksCount: response.content.filter((c)=>c.type === 'text').length
                    });
                    const toolUses = response.content.filter((item)=>item.type === 'tool_use' || item.type === 'mcp_tool_use');
                    const toolResults = response.content.filter((item)=>item.type === 'tool_result' || item.type === 'mcp_tool_result');
                    const textBlocks = response.content.filter((item)=>item.type === 'text');
                    responseText = textBlocks.map((item)=>item.text).join('\n');
                    usage = response.usage || {};
                    const manualToolUses = toolUses.filter((tu)=>standardTools.some((st)=>st.name === tu.name) || manualMcpTools.some((mt)=>(mt.name || mt.toolName) === tu.name));
                    let extraToolResults = [];
                    if (manualToolUses.length > 0) {
                        extraToolResults = await Promise.all(manualToolUses.map(async (tu)=>{
                            const tool = standardTools.find((t)=>t.name === tu.name);
                            if (tool) {
                                try {
                                    const result = await tool.invoke(tu.input);
                                    const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                    };
                                } catch (e) {
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: JSON.stringify({
                                            error: e instanceof Error ? e.message : 'Unknown error'
                                        }),
                                        is_error: true
                                    };
                                }
                            }
                            const mcpTool = manualMcpTools.find((t)=>(t.name || t.toolName) === tu.name);
                            if (mcpTool) {
                                try {
                                    const mcpServer = {
                                        name: mcpTool.serverName,
                                        url: mcpTool.serverUrl,
                                        authToken: mcpTool.serverAuthToken
                                    };
                                    console.log(`🔧 Manual MCP tool call: ${tu.name}`, {
                                        input: tu.input,
                                        inputType: typeof tu.input,
                                        schema: mcpTool.inputSchema
                                    });
                                    const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, tu.name, tu.input, apiKeys);
                                    const stringResult = JSON.stringify(result.result || result);
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                    };
                                } catch (e) {
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: JSON.stringify({
                                            error: e instanceof Error ? e.message : 'Unknown error'
                                        }),
                                        is_error: true
                                    };
                                }
                            }
                            return null;
                        }));
                        extraToolResults = extraToolResults.filter(Boolean);
                        if (extraToolResults.length > 0) {
                            // Implement agentic loop - continue until no more tool calls
                            let currentMessages = [
                                ...messages,
                                {
                                    role: 'assistant',
                                    content: response.content
                                },
                                {
                                    role: 'user',
                                    content: extraToolResults
                                }
                            ];
                            let currentResponse = response;
                            let iterations = 0;
                            const MAX_ITERATIONS = 10; // Prevent infinite loops
                            while(iterations < MAX_ITERATIONS){
                                const nextResponse = await client.beta.messages.create({
                                    model: modelName,
                                    max_tokens: maxTokens,
                                    messages: currentMessages,
                                    mcp_servers: mcpServers,
                                    tools: finalTools,
                                    betas: [
                                        'mcp-client-2025-04-04'
                                    ]
                                });
                                console.log(`[Agent] Agentic loop iteration ${iterations + 1}:`, {
                                    stopReason: nextResponse.stop_reason,
                                    contentTypes: nextResponse.content.map((c)=>c.type),
                                    toolUsesCount: nextResponse.content.filter((c)=>c.type === 'tool_use' || c.type === 'mcp_tool_use').length
                                });
                                // Accumulate usage
                                usage = {
                                    input_tokens: (usage.input_tokens || 0) + (nextResponse.usage?.input_tokens || 0),
                                    output_tokens: (usage.output_tokens || 0) + (nextResponse.usage?.output_tokens || 0),
                                    total_tokens: (usage.total_tokens || 0) + ((nextResponse.usage?.input_tokens || 0) + (nextResponse.usage?.output_tokens || 0))
                                };
                                const nextToolUses = nextResponse.content.filter((item)=>item.type === 'tool_use' || item.type === 'mcp_tool_use');
                                // If no more tool calls, we're done
                                if (nextToolUses.length === 0) {
                                    const finalTextBlocks = nextResponse.content.filter((item)=>item.type === 'text');
                                    responseText = finalTextBlocks.map((item)=>item.text).join('\n');
                                    currentResponse = nextResponse;
                                    break;
                                }
                                // Execute the next batch of tool calls
                                const nextManualToolUses = nextToolUses.filter((tu)=>standardTools.some((st)=>st.name === tu.name) || manualMcpTools.some((mt)=>(mt.name || mt.toolName) === tu.name));
                                const nextToolResults = await Promise.all(nextManualToolUses.map(async (tu)=>{
                                    const tool = standardTools.find((t)=>t.name === tu.name);
                                    if (tool) {
                                        try {
                                            const result = await tool.invoke(tu.input);
                                            const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                            return {
                                                type: 'tool_result',
                                                tool_use_id: tu.id,
                                                content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                            };
                                        } catch (e) {
                                            return {
                                                type: 'tool_result',
                                                tool_use_id: tu.id,
                                                content: JSON.stringify({
                                                    error: e instanceof Error ? e.message : 'Unknown error'
                                                }),
                                                is_error: true
                                            };
                                        }
                                    }
                                    const mcpTool = manualMcpTools.find((t)=>(t.name || t.toolName) === tu.name);
                                    if (mcpTool) {
                                        try {
                                            const mcpServer = {
                                                name: mcpTool.serverName,
                                                url: mcpTool.serverUrl,
                                                authToken: mcpTool.serverAuthToken
                                            };
                                            console.log(`🔧 Agentic loop MCP tool call: ${tu.name}`, {
                                                iteration: iterations + 1,
                                                input: tu.input
                                            });
                                            const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, tu.name, tu.input, apiKeys);
                                            const stringResult = JSON.stringify(result.result || result);
                                            return {
                                                type: 'tool_result',
                                                tool_use_id: tu.id,
                                                content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                            };
                                        } catch (e) {
                                            return {
                                                type: 'tool_result',
                                                tool_use_id: tu.id,
                                                content: JSON.stringify({
                                                    error: e instanceof Error ? e.message : 'Unknown error'
                                                }),
                                                is_error: true
                                            };
                                        }
                                    }
                                    return null;
                                }));
                                const filteredNextToolResults = nextToolResults.filter(Boolean);
                                // Accumulate all tool calls for tracking
                                toolCalls = [
                                    ...toolCalls,
                                    ...nextToolUses.map((item)=>{
                                        const toolCall = {
                                            type: item.type,
                                            name: item.name,
                                            server_name: item.server_name || 'MCP',
                                            arguments: item.input,
                                            tool_use_id: item.id
                                        };
                                        const result = filteredNextToolResults.find((tr)=>tr.tool_use_id === item.id);
                                        if (result) {
                                            toolCall.output = result.is_error ? {
                                                error: result.content
                                            } : Array.isArray(result.content) ? result.content[0]?.text || result.content : result.content;
                                        }
                                        return toolCall;
                                    })
                                ];
                                // Update message history for next iteration
                                currentMessages = [
                                    ...currentMessages,
                                    {
                                        role: 'assistant',
                                        content: nextResponse.content
                                    },
                                    {
                                        role: 'user',
                                        content: filteredNextToolResults
                                    }
                                ];
                                currentResponse = nextResponse;
                                iterations++;
                            }
                            if (iterations >= MAX_ITERATIONS) {
                                console.warn(`⚠️ Agent reached max iterations (${MAX_ITERATIONS}) - stopping agentic loop`);
                            }
                        } else {
                            // No manual tool execution needed - use the original tool results from MCP
                            toolCalls = toolUses.map((item, idx)=>{
                                const toolCall = {
                                    type: item.type,
                                    name: item.name,
                                    server_name: item.server_name || 'MCP',
                                    arguments: item.input,
                                    tool_use_id: item.id
                                };
                                const existingResult = toolResults.find((tr)=>tr.tool_use_id === item.id);
                                if (existingResult) {
                                    toolCall.output = existingResult.is_error ? {
                                        error: existingResult.content
                                    } : Array.isArray(existingResult.content) ? existingResult.content[0]?.text || existingResult.content : existingResult.content;
                                }
                                return toolCall;
                            });
                        }
                    } else {
                        // No tool uses at all - just text response
                        toolCalls = [];
                    }
                } catch (err) {
                    anthropicError = err;
                    useManualToolCalling = true;
                    const errorMsg = err.message || '';
                    const isMCPError = errorMsg.includes('Input should be an object') || errorMsg.includes('input_type=list') || errorMsg.includes('Internal server error') || err.status === 500;
                    console.error('❌ Anthropic API error:', {
                        status: err.status,
                        message: errorMsg,
                        mcpServers: mcpServers.map((s)=>({
                                name: s.name,
                                url: s.url,
                                hasAuth: !!s.authorization_token
                            })),
                        isMCPError
                    });
                    if (!isMCPError) {
                        throw err;
                    }
                    console.warn('⚠️ Anthropic MCP failed, falling back to manual tool execution. Error:', errorMsg.substring(0, 200));
                }
                if (useManualToolCalling) {
                    console.log('⚠️ Anthropic MCP failed, attempting manual tool calling for all tools...');
                    const allManualMcpTools = flattenedMcpTools;
                    const allFinalTools = [
                        ...standardTools.map((t)=>{
                            const openAITool = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$utils$2f$function_calling$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToOpenAITool"])(t);
                            return {
                                name: openAITool.function.name,
                                description: openAITool.function.description,
                                input_schema: openAITool.function.parameters
                            };
                        }),
                        ...allManualMcpTools.map((t)=>{
                            const openAITool = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertMcpToOpenAiTool"])(t);
                            return {
                                name: openAITool.function.name,
                                description: openAITool.function.description,
                                input_schema: openAITool.function.parameters
                            };
                        })
                    ];
                    const initialResponse = await client.messages.create({
                        model: modelName,
                        max_tokens: maxTokens,
                        messages: messages,
                        tools: allFinalTools
                    });
                    const toolUses = initialResponse.content.filter((item)=>item.type === 'tool_use');
                    const textBlocks = initialResponse.content.filter((item)=>item.type === 'text');
                    responseText = textBlocks.map((item)=>item.text).join('\n');
                    usage = initialResponse.usage || {};
                    if (toolUses.length > 0) {
                        const toolResults = await Promise.all(toolUses.map(async (tu)=>{
                            const tool = standardTools.find((t)=>t.name === tu.name);
                            if (tool) {
                                try {
                                    const result = await tool.invoke(tu.input);
                                    const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                    };
                                } catch (e) {
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: JSON.stringify({
                                            error: e instanceof Error ? e.message : 'Unknown error'
                                        }),
                                        is_error: true
                                    };
                                }
                            }
                            const mcpTool = allManualMcpTools.find((t)=>(t.name || t.toolName) === tu.name);
                            if (mcpTool) {
                                try {
                                    const mcpServer = {
                                        name: mcpTool.serverName,
                                        url: mcpTool.serverUrl,
                                        authToken: mcpTool.serverAuthToken
                                    };
                                    const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, tu.name, tu.input, apiKeys);
                                    const stringResult = JSON.stringify(result.result || result);
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                    };
                                } catch (e) {
                                    return {
                                        type: 'tool_result',
                                        tool_use_id: tu.id,
                                        content: JSON.stringify({
                                            error: e instanceof Error ? e.message : 'Unknown error'
                                        }),
                                        is_error: true
                                    };
                                }
                            }
                            return {
                                type: 'tool_result',
                                tool_use_id: tu.id,
                                content: JSON.stringify({
                                    error: `Tool not found: ${tu.name}`
                                }),
                                is_error: true
                            };
                        }));
                        // Implement agentic loop for fallback manual tool calling
                        let currentMessages = [
                            ...messages,
                            {
                                role: 'assistant',
                                content: initialResponse.content
                            },
                            {
                                role: 'user',
                                content: toolResults
                            }
                        ];
                        let iterations = 0;
                        const MAX_ITERATIONS = 10;
                        toolCalls = toolUses.map((item, idx)=>{
                            const mcpTool = allManualMcpTools.find((t)=>(t.name || t.toolName) === item.name);
                            return {
                                type: item.type,
                                name: item.name,
                                server_name: mcpTool ? mcpTool.serverName : undefined,
                                arguments: item.input,
                                tool_use_id: item.id,
                                output: toolResults[idx] ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["parseToolCallResult"])(toolResults[idx].content) : null
                            };
                        });
                        while(iterations < MAX_ITERATIONS){
                            const nextResponse = await client.messages.create({
                                model: modelName,
                                max_tokens: maxTokens,
                                messages: currentMessages,
                                tools: allFinalTools
                            });
                            console.log(`[Agent] Anthropic fallback agentic loop iteration ${iterations + 1}:`, {
                                stopReason: nextResponse.stop_reason,
                                contentTypes: nextResponse.content.map((c)=>c.type),
                                toolUsesCount: nextResponse.content.filter((c)=>c.type === 'tool_use').length
                            });
                            // Accumulate usage
                            usage = {
                                input_tokens: (usage.input_tokens || 0) + (nextResponse.usage?.input_tokens || 0),
                                output_tokens: (usage.output_tokens || 0) + (nextResponse.usage?.output_tokens || 0),
                                total_tokens: (usage.total_tokens || 0) + ((nextResponse.usage?.input_tokens || 0) + (nextResponse.usage?.output_tokens || 0))
                            };
                            const nextToolUses = nextResponse.content.filter((item)=>item.type === 'tool_use');
                            // If no more tool calls, we're done
                            if (nextToolUses.length === 0) {
                                const finalTextBlocks = nextResponse.content.filter((item)=>item.type === 'text');
                                responseText = finalTextBlocks.map((item)=>item.text).join('\n');
                                break;
                            }
                            // Execute next batch of tool calls
                            const nextToolResults = await Promise.all(nextToolUses.map(async (tu)=>{
                                const tool = standardTools.find((t)=>t.name === tu.name);
                                if (tool) {
                                    try {
                                        const result = await tool.invoke(tu.input);
                                        const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                        return {
                                            type: 'tool_result',
                                            tool_use_id: tu.id,
                                            content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                        };
                                    } catch (e) {
                                        return {
                                            type: 'tool_result',
                                            tool_use_id: tu.id,
                                            content: JSON.stringify({
                                                error: e instanceof Error ? e.message : 'Unknown error'
                                            }),
                                            is_error: true
                                        };
                                    }
                                }
                                const mcpTool = allManualMcpTools.find((t)=>(t.name || t.toolName) === tu.name);
                                if (mcpTool) {
                                    try {
                                        const mcpServer = {
                                            name: mcpTool.serverName,
                                            url: mcpTool.serverUrl,
                                            authToken: mcpTool.serverAuthToken
                                        };
                                        console.log(`🔧 Fallback agentic loop MCP tool call: ${tu.name}`, {
                                            iteration: iterations + 1,
                                            input: tu.input
                                        });
                                        const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, tu.name, tu.input, apiKeys);
                                        const stringResult = JSON.stringify(result.result || result);
                                        return {
                                            type: 'tool_result',
                                            tool_use_id: tu.id,
                                            content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                        };
                                    } catch (e) {
                                        return {
                                            type: 'tool_result',
                                            tool_use_id: tu.id,
                                            content: JSON.stringify({
                                                error: e instanceof Error ? e.message : 'Unknown error'
                                            }),
                                            is_error: true
                                        };
                                    }
                                }
                                return {
                                    type: 'tool_result',
                                    tool_use_id: tu.id,
                                    content: JSON.stringify({
                                        error: `Tool not found: ${tu.name}`
                                    }),
                                    is_error: true
                                };
                            }));
                            // Accumulate tool calls
                            toolCalls = [
                                ...toolCalls,
                                ...nextToolUses.map((item, idx)=>{
                                    const mcpTool = allManualMcpTools.find((t)=>(t.name || t.toolName) === item.name);
                                    return {
                                        type: item.type,
                                        name: item.name,
                                        server_name: mcpTool ? mcpTool.serverName : undefined,
                                        arguments: item.input,
                                        tool_use_id: item.id,
                                        output: nextToolResults[idx] ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["parseToolCallResult"])(nextToolResults[idx].content) : null
                                    };
                                })
                            ];
                            currentMessages = [
                                ...currentMessages,
                                {
                                    role: 'assistant',
                                    content: nextResponse.content
                                },
                                {
                                    role: 'user',
                                    content: nextToolResults
                                }
                            ];
                            iterations++;
                        }
                        if (iterations >= MAX_ITERATIONS) {
                            console.warn(`⚠️ Anthropic fallback agent reached max iterations (${MAX_ITERATIONS})`);
                        }
                    }
                }
            } else {
                const response = await client.messages.create({
                    model: modelName,
                    max_tokens: maxTokens,
                    messages: messages
                });
                responseText = response.content[0].type === 'text' ? response.content[0].text : '';
                usage = response.usage || {};
            }
        } else if (provider === 'openai' && apiKeys?.openai || provider === 'groq' && apiKeys?.groq) {
            // --- OpenAI / Groq Implementation ---
            const OpenAI = (await __turbopack_context__.A("[project]/node_modules/openai/index.mjs [app-route] (ecmascript, async loader)")).default;
            const client = new OpenAI({
                apiKey: provider === 'groq' ? apiKeys.groq : apiKeys.openai,
                baseURL: provider === 'groq' ? 'https://api.groq.com/openai/v1' : undefined
            });
            if (hasMcpTools || standardTools.length > 0) {
                // Convert tools and filter out any that failed conversion
                const mcpConvertedTools = flattenedMcpTools.map((tool)=>{
                    try {
                        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertMcpToOpenAiTool"])(tool);
                    } catch (error) {
                        console.error(`Failed to convert MCP tool ${tool?.name || 'unknown'}:`, error);
                        return null;
                    }
                }).filter(Boolean);
                const standardConvertedTools = standardTools.map((tool)=>{
                    try {
                        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$utils$2f$function_calling$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToOpenAITool"])(tool);
                    } catch (error) {
                        console.error(`Failed to convert standard tool ${tool?.name || 'unknown'}:`, error);
                        return null;
                    }
                }).filter(Boolean);
                const tools = [
                    ...mcpConvertedTools,
                    ...standardConvertedTools
                ];
                // Only include tools if the array is not empty after conversion
                const hasValidTools = tools.length > 0;
                console.log(`[Agent] Tool conversion: ${flattenedMcpTools.length} MCP + ${standardTools.length} standard → ${tools.length} valid tools`);
                if (!hasValidTools) {
                    console.warn('[Agent] No valid tools after conversion - running without tools');
                }
                const response = await client.chat.completions.create({
                    model: modelName,
                    messages: messages,
                    ...hasValidTools ? {
                        tools,
                        tool_choice: "auto"
                    } : {},
                    ...data.tokenLimit ? {
                        max_tokens: maxTokens
                    } : {}
                });
                const message = response.choices[0].message;
                usage = response.usage || {};
                if (message.tool_calls && message.tool_calls.length > 0) {
                    const toolResults = await Promise.all(message.tool_calls.map(async (call)=>{
                        try {
                            const mcpTool = flattenedMcpTools.find((t)=>(t.name || t.toolName) === call.function.name);
                            if (mcpTool) {
                                const args = JSON.parse(call.function.arguments);
                                const mcpServer = {
                                    name: mcpTool.serverName,
                                    url: mcpTool.serverUrl,
                                    authToken: mcpTool.serverAuthToken
                                };
                                console.log(`🔧 Calling MCP tool: ${call.function.name} with args: `, JSON.stringify(args, null, 2));
                                const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, call.function.name, args, apiKeys);
                                console.log(`✅ MCP tool ${call.function.name} returned: `, JSON.stringify(result, null, 2).substring(0, 500));
                                return {
                                    tool_call_id: call.id,
                                    role: "tool",
                                    content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(JSON.stringify(result.result || result))
                                };
                            }
                            const standardTool = standardTools.find((t)=>t.name === call.function.name);
                            if (standardTool) {
                                const args = JSON.parse(call.function.arguments);
                                const result = await standardTool.invoke(args);
                                const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                return {
                                    tool_call_id: call.id,
                                    role: "tool",
                                    content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                };
                            }
                            throw new Error(`Tool not found: ${call.function.name} `);
                        } catch (error) {
                            console.error(`❌ Tool execution failed for ${call.function.name}:`, error);
                            return {
                                tool_call_id: call.id,
                                role: "tool",
                                content: JSON.stringify({
                                    error: error instanceof Error ? error.message : 'Unknown error'
                                })
                            };
                        }
                    }));
                    // Implement agentic loop for OpenAI/Groq
                    let currentMessages = [
                        ...messages,
                        message,
                        ...toolResults
                    ];
                    let iterations = 0;
                    const MAX_ITERATIONS = 10;
                    toolCalls = message.tool_calls.map((call, idx)=>({
                            id: call.id,
                            name: call.function.name,
                            arguments: JSON.parse(call.function.arguments),
                            output: toolResults[idx] ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["parseToolCallResult"])(toolResults[idx].content) : null
                        }));
                    while(iterations < MAX_ITERATIONS){
                        const nextResponse = await client.chat.completions.create({
                            model: modelName,
                            messages: currentMessages,
                            tools: tools,
                            tool_choice: "auto",
                            ...data.tokenLimit ? {
                                max_tokens: maxTokens
                            } : {}
                        });
                        const nextMessage = nextResponse.choices[0].message;
                        console.log(`[Agent] OpenAI/Groq agentic loop iteration ${iterations + 1}:`, {
                            finishReason: nextResponse.choices[0].finish_reason,
                            hasToolCalls: !!nextMessage.tool_calls,
                            toolCallsCount: nextMessage.tool_calls?.length || 0
                        });
                        // Accumulate usage
                        usage = {
                            prompt_tokens: (usage.prompt_tokens || 0) + (nextResponse.usage?.prompt_tokens || 0),
                            completion_tokens: (usage.completion_tokens || 0) + (nextResponse.usage?.completion_tokens || 0),
                            total_tokens: (usage.total_tokens || 0) + (nextResponse.usage?.total_tokens || 0)
                        };
                        // If no more tool calls, we're done
                        if (!nextMessage.tool_calls || nextMessage.tool_calls.length === 0) {
                            responseText = nextMessage.content || '';
                            break;
                        }
                        // Execute next batch of tool calls
                        const nextToolResults = await Promise.all(nextMessage.tool_calls.map(async (call)=>{
                            try {
                                const mcpTool = flattenedMcpTools.find((t)=>(t.name || t.toolName) === call.function.name);
                                if (mcpTool) {
                                    const args = JSON.parse(call.function.arguments);
                                    const mcpServer = {
                                        name: mcpTool.serverName,
                                        url: mcpTool.serverUrl,
                                        authToken: mcpTool.serverAuthToken
                                    };
                                    console.log(`🔧 Agentic loop MCP tool call: ${call.function.name}`, {
                                        iteration: iterations + 1,
                                        args
                                    });
                                    const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, call.function.name, args, apiKeys);
                                    return {
                                        tool_call_id: call.id,
                                        role: "tool",
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(JSON.stringify(result.result || result))
                                    };
                                }
                                const standardTool = standardTools.find((t)=>t.name === call.function.name);
                                if (standardTool) {
                                    const args = JSON.parse(call.function.arguments);
                                    const result = await standardTool.invoke(args);
                                    const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                    return {
                                        tool_call_id: call.id,
                                        role: "tool",
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                    };
                                }
                                throw new Error(`Tool not found: ${call.function.name}`);
                            } catch (error) {
                                console.error(`❌ Tool execution failed for ${call.function.name}:`, error);
                                return {
                                    tool_call_id: call.id,
                                    role: "tool",
                                    content: JSON.stringify({
                                        error: error instanceof Error ? error.message : 'Unknown error'
                                    })
                                };
                            }
                        }));
                        // Accumulate tool calls
                        toolCalls = [
                            ...toolCalls,
                            ...nextMessage.tool_calls.map((call, idx)=>({
                                    id: call.id,
                                    name: call.function.name,
                                    arguments: JSON.parse(call.function.arguments),
                                    output: nextToolResults[idx] ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["parseToolCallResult"])(nextToolResults[idx].content) : null
                                }))
                        ];
                        currentMessages = [
                            ...currentMessages,
                            nextMessage,
                            ...nextToolResults
                        ];
                        iterations++;
                    }
                    if (iterations >= MAX_ITERATIONS) {
                        console.warn(`⚠️ OpenAI/Groq agent reached max iterations (${MAX_ITERATIONS})`);
                    }
                } else {
                    responseText = message.content || '';
                }
            } else {
                const response = await client.chat.completions.create({
                    model: modelName,
                    messages: messages,
                    ...data.tokenLimit ? {
                        max_tokens: maxTokens
                    } : {}
                });
                responseText = response.choices[0].message.content || '';
                usage = response.usage || {};
            }
        } else if (provider === 'google' && apiKeys?.google) {
            // --- Google Gemini Implementation ---
            const { ChatGoogleGenerativeAI } = await __turbopack_context__.A("[project]/node_modules/@langchain/google-genai/dist/index.js [app-route] (ecmascript, async loader)");
            if (hasMcpTools || standardTools.length > 0) {
                const model = new ChatGoogleGenerativeAI({
                    apiKey: apiKeys.google,
                    model: modelName,
                    maxOutputTokens: data.tokenLimit
                });
                const tools = [
                    ...flattenedMcpTools.map(__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertMcpToOpenAiTool"]),
                    ...standardTools.map((tool)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$langchain$2f$core$2f$dist$2f$utils$2f$function_calling$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["convertToOpenAITool"])(tool))
                ];
                const response = await model.invoke(messages, {
                    tools: tools
                });
                usage = response.response_metadata?.usage || {};
                if (response.tool_calls && response.tool_calls.length > 0) {
                    const toolResults = await Promise.all(response.tool_calls.map(async (call)=>{
                        try {
                            const mcpTool = flattenedMcpTools.find((t)=>(t.name || t.toolName) === call.name);
                            if (mcpTool) {
                                // Construct a server object for executeMcpTool
                                const mcpServer = {
                                    name: mcpTool.serverName,
                                    url: mcpTool.serverUrl,
                                    authToken: mcpTool.serverAuthToken
                                };
                                console.log(`🔧 Calling MCP tool: ${call.name} with args: `, JSON.stringify(call.args, null, 2));
                                const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, call.name, call.args, apiKeys);
                                console.log(`✅ MCP tool ${call.name} returned: `, JSON.stringify(result, null, 2).substring(0, 500));
                                return {
                                    tool_call_id: call.id,
                                    role: "tool",
                                    content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(JSON.stringify(result.result || result))
                                };
                            }
                            const standardTool = standardTools.find((t)=>t.name === call.name);
                            if (standardTool) {
                                const result = await standardTool.invoke(call.args);
                                const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                return {
                                    tool_call_id: call.id,
                                    role: "tool",
                                    content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                };
                            }
                            return {
                                tool_call_id: call.id,
                                role: "tool",
                                content: JSON.stringify({
                                    error: 'Tool not found'
                                })
                            };
                        } catch (error) {
                            return {
                                tool_call_id: call.id,
                                role: "tool",
                                content: JSON.stringify({
                                    error: error instanceof Error ? error.message : 'Unknown error'
                                })
                            };
                        }
                    }));
                    // Implement agentic loop for Google Gemini
                    let currentMessages = [
                        ...messages,
                        response,
                        ...toolResults
                    ];
                    let iterations = 0;
                    const MAX_ITERATIONS = 10;
                    toolCalls = response.tool_calls.map((call, idx)=>({
                            id: call.id,
                            name: call.name,
                            arguments: call.args,
                            output: toolResults[idx] ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["parseToolCallResult"])(toolResults[idx].content) : null
                        }));
                    while(iterations < MAX_ITERATIONS){
                        const nextResponse = await model.invoke(currentMessages, {
                            tools: tools
                        });
                        console.log(`[Agent] Gemini agentic loop iteration ${iterations + 1}:`, {
                            hasToolCalls: !!nextResponse.tool_calls,
                            toolCallsCount: nextResponse.tool_calls?.length || 0
                        });
                        // Accumulate usage
                        usage = {
                            prompt_tokens: (usage.prompt_tokens || 0) + (nextResponse.response_metadata?.usage?.prompt_tokens || 0),
                            completion_tokens: (usage.completion_tokens || 0) + (nextResponse.response_metadata?.usage?.completion_tokens || 0),
                            total_tokens: (usage.total_tokens || 0) + (nextResponse.response_metadata?.usage?.total_tokens || 0)
                        };
                        // If no more tool calls, we're done
                        if (!nextResponse.tool_calls || nextResponse.tool_calls.length === 0) {
                            responseText = nextResponse.content;
                            break;
                        }
                        // Execute next batch of tool calls
                        const nextToolResults = await Promise.all(nextResponse.tool_calls.map(async (call)=>{
                            try {
                                const mcpTool = flattenedMcpTools.find((t)=>(t.name || t.toolName) === call.name);
                                if (mcpTool) {
                                    const mcpServer = {
                                        name: mcpTool.serverName,
                                        url: mcpTool.serverUrl,
                                        authToken: mcpTool.serverAuthToken
                                    };
                                    console.log(`🔧 Agentic loop MCP tool call: ${call.name}`, {
                                        iteration: iterations + 1,
                                        args: call.args
                                    });
                                    const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMcpTool"])(mcpServer, call.name, call.args, apiKeys);
                                    return {
                                        tool_call_id: call.id,
                                        role: "tool",
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(JSON.stringify(result.result || result))
                                    };
                                }
                                const standardTool = standardTools.find((t)=>t.name === call.name);
                                if (standardTool) {
                                    const result = await standardTool.invoke(call.args);
                                    const stringResult = typeof result === 'string' ? result : JSON.stringify(result);
                                    return {
                                        tool_call_id: call.id,
                                        role: "tool",
                                        content: (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["truncateContent"])(stringResult)
                                    };
                                }
                                return {
                                    tool_call_id: call.id,
                                    role: "tool",
                                    content: JSON.stringify({
                                        error: 'Tool not found'
                                    })
                                };
                            } catch (error) {
                                return {
                                    tool_call_id: call.id,
                                    role: "tool",
                                    content: JSON.stringify({
                                        error: error instanceof Error ? error.message : 'Unknown error'
                                    })
                                };
                            }
                        }));
                        // Accumulate tool calls
                        toolCalls = [
                            ...toolCalls,
                            ...nextResponse.tool_calls.map((call, idx)=>({
                                    id: call.id,
                                    name: call.name,
                                    arguments: call.args,
                                    output: nextToolResults[idx] ? (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tool$2d$utils$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["parseToolCallResult"])(nextToolResults[idx].content) : null
                                }))
                        ];
                        currentMessages = [
                            ...currentMessages,
                            nextResponse,
                            ...nextToolResults
                        ];
                        iterations++;
                    }
                    if (iterations >= MAX_ITERATIONS) {
                        console.warn(`⚠️ Gemini agent reached max iterations (${MAX_ITERATIONS})`);
                    }
                } else {
                    responseText = response.content;
                }
            } else {
                const model = new ChatGoogleGenerativeAI({
                    apiKey: apiKeys.google,
                    model: modelName,
                    maxOutputTokens: data.tokenLimit
                });
                const response = await model.invoke(messages);
                responseText = response.content;
                usage = response.response_metadata?.usage || {};
            }
        } else {
            throw new Error(`No API key available for provider: ${provider} `);
        }
        // 10. Return Result
        const serverChatUpdates = data.includeChatHistory ? [
            {
                role: 'user',
                content: data.instructions || ''
            },
            {
                role: 'assistant',
                content: responseText
            }
        ] : [];
        let output = responseText;
        if (data.outputFormat === 'JSON') {
            try {
                output = JSON.parse(responseText);
            } catch (e) {
                console.warn('Could not parse JSON output, using raw text');
            }
        }
        return {
            __agentValue: output,
            __agentToolCalls: toolCalls,
            __chatHistoryUpdates: serverChatUpdates,
            __variableUpdates: {
                lastOutput: output
            },
            __usage: {
                input_tokens: usage.input_tokens || usage.prompt_tokens || 0,
                output_tokens: usage.output_tokens || usage.completion_tokens || 0,
                total_tokens: usage.total_tokens || (usage.input_tokens || usage.prompt_tokens || 0) + (usage.output_tokens || usage.completion_tokens || 0)
            }
        };
    } catch (error) {
        console.error('Agent execution error:', error);
        const errorMessage = error instanceof Error ? error.message : 'Unknown error';
        throw new Error(`Agent execution failed: ${errorMessage} `);
    }
}
}),
"[externals]/util [external] (util, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("util", () => require("util"));

module.exports = mod;
}),
"[externals]/http [external] (http, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http", () => require("http"));

module.exports = mod;
}),
"[externals]/https [external] (https, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("https", () => require("https"));

module.exports = mod;
}),
"[externals]/url [external] (url, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("url", () => require("url"));

module.exports = mod;
}),
"[externals]/fs [external] (fs, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("fs", () => require("fs"));

module.exports = mod;
}),
"[externals]/http2 [external] (http2, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("http2", () => require("http2"));

module.exports = mod;
}),
"[externals]/assert [external] (assert, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("assert", () => require("assert"));

module.exports = mod;
}),
"[externals]/tty [external] (tty, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("tty", () => require("tty"));

module.exports = mod;
}),
"[externals]/os [external] (os, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("os", () => require("os"));

module.exports = mod;
}),
"[externals]/zlib [external] (zlib, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("zlib", () => require("zlib"));

module.exports = mod;
}),
"[externals]/events [external] (events, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("events", () => require("events"));

module.exports = mod;
}),
"[project]/lib/api/config.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Server-side API configuration utilities
 * Use this for getting API keys in API routes and server components
 */ __turbopack_context__.s([
    "getServerAPIKeys",
    ()=>getServerAPIKeys,
    "hasServerAPIKeys",
    ()=>hasServerAPIKeys
]);
function getServerAPIKeys() {
    return {
        anthropic: process.env.ANTHROPIC_API_KEY,
        groq: process.env.GROQ_API_KEY,
        openai: process.env.OPENAI_API_KEY,
        google: process.env.GOOGLE_API_KEY,
        firecrawl: process.env.FIRECRAWL_API_KEY,
        arcade: process.env.ARCADE_API_KEY,
        e2b: process.env.E2B_API_KEY,
        tavily: process.env.TAVILY_API_KEY,
        serper: process.env.SERPER_API_KEY,
        serpapi: process.env.SERPAPI_API_KEY,
        scraperapi: process.env.SCRAPERAPI_API_KEY,
        browserless: process.env.BROWSERLESS_API_KEY
    };
}
function hasServerAPIKeys() {
    const hasLLMKey = !!(process.env.ANTHROPIC_API_KEY || process.env.GROQ_API_KEY || process.env.OPENAI_API_KEY || process.env.GOOGLE_API_KEY);
    return hasLLMKey && !!process.env.FIRECRAWL_API_KEY;
}
}),
"[project]/lib/workflow/executors/mcp.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeMCPNode",
    ()=>executeMCPNode
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$mendable$2f$firecrawl$2d$js$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@mendable/firecrawl-js/dist/index.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/api/config.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mcp$2f$resolver$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/mcp/resolver.ts [app-route] (ecmascript)");
;
;
;
;
/**
 * Extract specific field from Firecrawl response
 */ function extractField(data, field, customPath) {
    if (field === 'full') return data;
    if (field === 'custom' && customPath) {
        return getNestedValue(data, customPath);
    }
    // Predefined field mappings
    switch(field){
        case 'markdown':
            return data.markdown || data;
        case 'html':
            return data.html || data;
        case 'metadata':
            return data.metadata || {};
        case 'results':
            return data.results || data;
        case 'urls':
            if (Array.isArray(data.results)) {
                return data.results.map((r)=>r.url);
            }
            if (Array.isArray(data.urls)) {
                return data.urls;
            }
            if (Array.isArray(data.links)) {
                return data.links;
            }
            return data;
        case 'first':
            return data.results?.[0] || data[0] || data;
        case 'json':
            // For JSON mode in scrape/crawl
            return data.json || data.data || data;
        default:
            return data[field] || data;
    }
}
/**
 * Get nested value from object using dot notation
 */ function getNestedValue(obj, path) {
    const parts = path.split('.');
    let current = obj;
    for (const part of parts){
        // Handle array indexing
        const arrayMatch = part.match(/^(\w+)\[(\d+)\]$/);
        if (arrayMatch) {
            const [, arrayName, index] = arrayMatch;
            current = current[arrayName]?.[parseInt(index)];
        } else {
            current = current?.[part];
        }
        if (current === undefined) break;
    }
    return current;
}
/**
 * Execute a generic MCP server (DeepWiki, etc.)
 */ async function executeGenericMCPServer(serverConfig, state) {
    const input = state.variables?.input || state.variables?.lastOutput;
    const serverName = serverConfig.name.toLowerCase();
    // For DeepWiki
    if (serverName.includes('deepwiki') || serverName.includes('devin')) {
        // Parse the input to determine which tool to use
        const inputText = typeof input === 'string' ? input : JSON.stringify(input);
        // Determine which DeepWiki tool to use based on input
        let tool = 'ask_question'; // Default
        let params = {};
        if (inputText.includes('wiki structure') || inputText.includes('topics')) {
            tool = 'read_wiki_structure';
            // Extract repo from input (e.g., "anthropics/anthropic-sdk-python")
            const repoMatch = inputText.match(/([a-zA-Z0-9-]+\/[a-zA-Z0-9-]+)/);
            params.repository = repoMatch ? repoMatch[1] : 'anthropics/anthropic-sdk-python';
        } else if (inputText.includes('wiki content') || inputText.includes('documentation')) {
            tool = 'read_wiki_contents';
            const repoMatch = inputText.match(/([a-zA-Z0-9-]+\/[a-zA-Z0-9-]+)/);
            params.repository = repoMatch ? repoMatch[1] : 'anthropics/anthropic-sdk-python';
        } else {
            tool = 'ask_question';
            // Extract repo if mentioned
            const repoMatch = inputText.match(/([a-zA-Z0-9-]+\/[a-zA-Z0-9-]+)/);
            params.repository = repoMatch ? repoMatch[1] : 'anthropics/anthropic-sdk-python';
            params.question = inputText;
        }
        return {
            tool,
            data: {
                server: 'DeepWiki',
                tool,
                params,
                note: 'DeepWiki MCP execution is not yet implemented. This is a placeholder response.',
                input: inputText,
                suggestedImplementation: 'Call the DeepWiki MCP server API at ' + serverConfig.url
            }
        };
    }
    // Generic fallback for unknown MCP servers
    throw new Error(`MCP server "${serverConfig.name}" execution not yet implemented. Server URL: ${serverConfig.url}`);
}
async function executeMCPNode(node, state, apiKey) {
    const { data } = node;
    // MCP executor always runs on server side in LangGraph context
    // No client-side detection needed
    const nodeName = data.nodeName?.toLowerCase() || '';
    const nodeData = data;
    const lastOutput = state.variables?.lastOutput;
    // Resolve MCP server configuration
    let mcpServers = nodeData.mcpServers || [];
    // If using new format with server ID, resolve it
    if (nodeData.mcpServerId) {
        const resolvedServer = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$mcp$2f$resolver$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["resolveMCPServer"])(nodeData.mcpServerId);
        if (resolvedServer) {
            mcpServers = [
                resolvedServer
            ];
        } else {
            console.warn(`Could not resolve MCP server ID: ${nodeData.mcpServerId}`);
        }
    }
    if (!mcpServers || mcpServers.length === 0) {
        return {
            error: 'No MCP servers configured or could not resolve server'
        };
    }
    const results = [];
    for (const serverConfig of mcpServers){
        // For all servers (including Firecrawl), use API routes
        if (serverConfig.name.toLowerCase().includes('firecrawl')) {
            // Server-side Firecrawl execution - use Firecrawl SDK directly
            console.log('🖥️ MCP executor running Firecrawl on server side');
            const serverApiKeys = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getServerAPIKeys"])();
            const firecrawlKey = apiKey || serverApiKeys.firecrawl;
            if (!firecrawlKey) {
                throw new Error('FIRECRAWL_API_KEY not configured. Add it to your .env.local file or configure it in Settings.');
            }
            const firecrawl = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$mendable$2f$firecrawl$2d$js$2f$dist$2f$index$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["default"]({
                apiKey: firecrawlKey
            });
            // Get the action and parameters from the node data
            const nodeData = data;
            const action = nodeData.mcpAction || 'scrape';
            // Get URL from input or previous step
            const getUrl = ()=>{
                const explicitUrl = nodeData.scrapeUrl || nodeData.mapUrl || nodeData.crawlUrl;
                if (explicitUrl) {
                    const substituted = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(explicitUrl, state);
                    if (substituted && substituted.startsWith('http')) {
                        return substituted;
                    }
                }
                const lastOutput = state.variables?.lastOutput;
                if (typeof lastOutput === 'string' && lastOutput.startsWith('http')) {
                    return lastOutput;
                }
                if (lastOutput?.url && typeof lastOutput.url === 'string') {
                    return lastOutput.url;
                }
                if (typeof state.variables?.input === 'string' && state.variables.input.startsWith('http')) {
                    return state.variables.input;
                }
                return 'https://example.com';
            };
            // Get search query
            const getSearchQuery = ()=>{
                if (nodeData.searchQuery) {
                    const substituted = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(nodeData.searchQuery, state);
                    if (substituted) {
                        return substituted;
                    }
                }
                const lastOutput = state.variables?.lastOutput;
                if (typeof lastOutput === 'string' && !lastOutput.startsWith('http')) {
                    return lastOutput;
                }
                if (typeof state.variables?.input === 'string' && !state.variables.input.startsWith('http')) {
                    return state.variables.input;
                }
                return 'latest tech news';
            };
            let result;
            try {
                switch(action){
                    case 'scrape':
                        result = await firecrawl.scrape(getUrl(), {
                            formats: nodeData.useJsonMode ? [
                                'json'
                            ] : [
                                'markdown',
                                'html'
                            ]
                        });
                        break;
                    case 'search':
                        result = await firecrawl.search(getSearchQuery(), {
                            limit: nodeData.searchLimit || 5
                        });
                        break;
                    case 'map':
                        result = await firecrawl.map(getUrl());
                        break;
                    case 'crawl':
                        result = await firecrawl.crawl(getUrl(), {
                            limit: nodeData.crawlLimit || 10
                        });
                        break;
                    default:
                        throw new Error(`Unknown Firecrawl action: ${action}`);
                }
                console.log('✅ MCP Firecrawl server-side execution completed successfully');
                // Extract specific field based on configuration
                let outputData = result;
                if (nodeData.outputField && nodeData.outputField !== 'full') {
                    outputData = extractField(result, nodeData.outputField, nodeData.customOutputPath);
                }
                // Update state
                state.variables.lastOutput = outputData;
                return {
                    results: [
                        {
                            server: 'Firecrawl',
                            tool: action,
                            success: true,
                            data: result
                        }
                    ],
                    extractedField: nodeData.outputField,
                    output: outputData,
                    mcpServers: [
                        'Firecrawl'
                    ],
                    toolCalls: [
                        {
                            name: `firecrawl_${action}`,
                            arguments: {
                                action,
                                url: getUrl(),
                                query: getSearchQuery()
                            },
                            output: result
                        }
                    ]
                };
            } catch (error) {
                console.error('❌ MCP Firecrawl server-side execution failed:', error);
                throw new Error(`Firecrawl ${action} failed: ${error instanceof Error ? error.message : 'Unknown error'}`);
            }
        } else {
            // Generic MCP server support (DeepWiki, etc.)
            try {
                const result = await executeGenericMCPServer(serverConfig, state);
                results.push({
                    server: serverConfig.name,
                    tool: result.tool || 'unknown',
                    success: true,
                    data: result.data
                });
                state.variables.lastOutput = result.data;
            } catch (error) {
                const errorMessage = error instanceof Error ? error.message : 'Unknown error';
                console.error(`${serverConfig.name} execution error:`, error);
                results.push({
                    server: serverConfig.name,
                    error: errorMessage,
                    success: false
                });
            }
        }
    }
    return {
        results,
        mcpServers: mcpServers.map((s)=>s.name)
    };
}
}),
"[project]/lib/workflow/executors/logic.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeLogicNode",
    ()=>executeLogicNode
]);
async function executeLogicNode(node, state) {
    const { type, data } = node;
    switch(type){
        case 'if-else':
            return await executeIfElse(data, state);
        case 'while':
            return await executeWhile(node, data, state);
        case 'user-approval':
            return await executeUserApproval(data, state);
        default:
            throw new Error(`Unknown logic node type: ${type}`);
    }
}
async function executeIfElse(data, state) {
    // Simulate delay
    await new Promise((resolve)=>setTimeout(resolve, 200));
    const conditionExpr = data.condition || 'true';
    try {
        // Get input from previous node, but prefer original classification data for conditions
        let input = state.variables['lastOutput'] || state.variables['input'] || {};
        // For classification-based conditions, use the original classification data
        if (conditionExpr.includes('classification') && state.variables.originalClassification) {
            input = state.variables.originalClassification;
            console.log('Using original classification data for condition evaluation');
        }
        console.log('Evaluating If/Else condition:', conditionExpr);
        console.log('Available input:', input);
        // Import safe evaluator
        const { safeEvaluate } = await __turbopack_context__.A("[project]/lib/workflow/safe-expression-evaluator.ts [app-route] (ecmascript, async loader)");
        // Create evaluation context with input, state, and lastOutput
        const context = {
            input,
            state: state.variables,
            lastOutput: state.variables['lastOutput'],
            // Also expose individual state variables for convenience
            ...state.variables
        };
        // Use safe evaluator (no Function constructor, no eval)
        const result = safeEvaluate(conditionExpr, context);
        console.log('Condition result:', result);
        return {
            condition: Boolean(result),
            branch: result ? 'if' : 'else',
            evaluatedCondition: conditionExpr
        };
    } catch (error) {
        console.error('Failed to evaluate condition:', error);
        // Default to false if evaluation fails
        return {
            condition: false,
            branch: 'else',
            evaluatedCondition: conditionExpr,
            error: error instanceof Error ? error.message : 'Evaluation failed'
        };
    }
}
async function executeWhile(node, data, state) {
    // This function is deprecated and should not be used
    // While loops are now handled by LangGraph's executeWhileNode method
    // This is kept for backward compatibility but will throw an error if called
    throw new Error('While loop execution via custom executor is deprecated. Use LangGraph executor instead.');
}
/**
 * User Approval - Pauses workflow execution pending approval
 */ async function executeUserApproval(data, state) {
    const { generateApprovalId } = await __turbopack_context__.A("[project]/lib/approval/approval-store.ts [app-route] (ecmascript, async loader)");
    const { substituteVariables } = await __turbopack_context__.A("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript, async loader)");
    const approvalId = generateApprovalId();
    const rawMessage = data.approvalMessage || 'Approval required';
    // Substitute variables in the approval message
    // This converts {{lastOutput.task}} to the actual task value
    const message = substituteVariables(rawMessage, state);
    console.log('User Approval - Raw message:', rawMessage);
    console.log('User Approval - Substituted message:', message);
    console.log('User Approval - State variables:', Object.keys(state.variables));
    // Return special response to signal workflow should pause
    return {
        __pendingApproval: true,
        approvalId,
        message,
        status: 'pending',
        createdAt: new Date().toISOString()
    };
} /**
 * TODO: Real implementation
 *
 * For if-else:
 * - Parse and evaluate condition string
 * - Support variable references from state
 * - Return boolean result
 *
 * For while:
 * - Evaluate condition in loop
 * - Execute loop body (connected nodes)
 * - Update state and check condition again
 *
 * For user-approval:
 * - Pause execution
 * - Show approval UI to user
 * - Wait for user response
 * - Resume with approval result
 */ 
}),
"[externals]/@e2b/code-interpreter [external] (@e2b/code-interpreter, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("@e2b/code-interpreter", () => require("@e2b/code-interpreter"));

module.exports = mod;
}),
"[project]/lib/workflow/executors/data.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeDataNode",
    ()=>executeDataNode
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f40$e2b$2f$code$2d$interpreter__$5b$external$5d$__$2840$e2b$2f$code$2d$interpreter$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/@e2b/code-interpreter [external] (@e2b/code-interpreter, cjs)");
;
async function executeDataNode(node, state) {
    const { data } = node;
    const nodeType = data.nodeType || node.type;
    try {
        switch(nodeType){
            case 'transform':
            case 'data-transform':
                return await executeTransform(data, state);
            case 'set-state':
            case 'set state':
                return await executeSetState(data, state);
            case 'export':
                return await executeExport(data, state);
            default:
                throw new Error(`Unknown data node type: ${nodeType}`);
        }
    } catch (error) {
        // Log error with context
        console.error(`Data node ${node.id} (${nodeType}) failed:`, error);
        // Re-throw with more context
        throw new Error(`Node ${node.id} execution failed: ${error instanceof Error ? error.message : 'Unknown error'}`);
    }
}
/**
 * Execute transform using E2B sandbox (REQUIRED FOR SECURITY)
 */ async function executeTransform(data, state) {
    // Get the transform script from node data
    // Support both transformScript (UI) and transformation (templates)
    const transformScript = data.transformScript || data.transformation;
    // If no transform script, just pass through the input
    if (!transformScript || transformScript.trim() === '') {
        console.log('⚠️ No transform script provided, passing through input');
        return state.variables.lastOutput || {};
    }
    // E2B sandbox is REQUIRED for security
    if (!process.env?.E2B_API_KEY) {
        throw new Error('E2B_API_KEY is required for secure code execution. ' + 'Get your key at https://e2b.dev and set it in your environment variables. ' + 'Transform nodes execute user-provided code and must run in a secure sandbox.');
    }
    return await executeTransformE2B(transformScript, state);
}
/**
 * Execute transform using E2B CodeInterpreter (SECURE)
 * Now uses JavaScript/TypeScript execution
 */ async function executeTransformE2B(transformScript, state) {
    console.log('🔒 Executing transform in E2B sandbox...');
    // Prepare the data for the sandbox
    const sandboxedInput = JSON.parse(JSON.stringify(state.variables.lastOutput || {}));
    const sandboxedState = {
        variables: JSON.parse(JSON.stringify(state.variables))
    };
    // Create E2B sandbox
    const sandbox = await __TURBOPACK__imported__module__$5b$externals$5d2f40$e2b$2f$code$2d$interpreter__$5b$external$5d$__$2840$e2b$2f$code$2d$interpreter$2c$__cjs$29$__["default"].create({
        apiKey: process.env.E2B_API_KEY
    });
    try {
        // Prepare the code to execute using JavaScript
        // We wrap the user's code in a function that provides the context
        const codeToExecute = `
const input = ${JSON.stringify(sandboxedInput)};
const lastOutput = ${JSON.stringify(sandboxedInput)};
const state = ${JSON.stringify(sandboxedState)};

// Execute user's transform code
const transform = () => {
${transformScript.split('\n').map((line)=>'  ' + line).join('\n')}
};

const result = transform();

// Output result as JSON
console.log(JSON.stringify(result));
`;
        // Execute in the sandbox using JavaScript
        const execution = await sandbox.runCode(codeToExecute);
        // Check for errors
        if (execution.error) {
            throw new Error(`E2B execution error: ${execution.error.name}: ${execution.error.value}`);
        }
        // Parse the result from stdout
        const resultText = execution.logs.stdout.join('\n');
        let result;
        try {
            result = JSON.parse(resultText);
        } catch (e) {
            console.error('Failed to parse E2B execution result:', resultText);
            throw new Error(`Transform returned invalid JSON: ${e instanceof Error ? e.message : 'Parse error'}`);
        }
        console.log('✅ E2B execution successful:', result);
        // Update state with the result
        state.variables['lastOutput'] = result;
        return result;
    } finally{
        // Always close the sandbox
        await sandbox.kill();
    }
}
/**
 * REMOVED: Insecure fallback execution using Function constructor
 *
 * This function has been removed for security reasons. It used the Function constructor
 * to execute user code, which is inherently unsafe even with regex-based filtering.
 *
 * Regex patterns can be bypassed using:
 * - String concatenation: 'req' + 'uire'
 * - Array/object access: global['process']
 * - Unicode escapes: \u0072equire
 * - Computed property access: this[String.fromCharCode(112,114,111,99,101,115,115)]
 *
 * All code execution now REQUIRES E2B sandbox for security.
 * See executeTransformE2B() for the secure implementation.
 */ async function executeSetState(data, state) {
    const key = data.stateKey || 'variable';
    try {
        let rawValue = data.stateValue || null;
        const valueType = data.valueType || 'string';
        console.log('🔧 Set State - Key:', key);
        console.log('🔧 Set State - Raw Value:', rawValue);
        console.log('🔧 Set State - Type:', valueType);
        // Import variable substitution
        const { substituteVariables } = await __turbopack_context__.A("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript, async loader)");
        // Substitute variables in the value (e.g., {{lastOutput.price}})
        if (typeof rawValue === 'string') {
            rawValue = substituteVariables(rawValue, state);
            console.log('🔧 Set State - After substitution:', rawValue);
        }
        // Parse value based on type
        let finalValue;
        switch(valueType){
            case 'number':
                finalValue = parseFloat(rawValue);
                if (isNaN(finalValue)) {
                    throw new Error(`Cannot convert "${rawValue}" to number`);
                }
                break;
            case 'boolean':
                if (typeof rawValue === 'boolean') {
                    finalValue = rawValue;
                } else {
                    const str = String(rawValue).toLowerCase();
                    finalValue = str === 'true' || str === '1' || str === 'yes';
                }
                break;
            case 'json':
                try {
                    finalValue = typeof rawValue === 'string' ? JSON.parse(rawValue) : rawValue;
                } catch (e) {
                    throw new Error(`Invalid JSON: ${e instanceof Error ? e.message : 'Parse error'}`);
                }
                break;
            case 'expression':
                // Evaluate JavaScript expression using safe evaluator
                try {
                    const { safeEvaluate } = await __turbopack_context__.A("[project]/lib/workflow/safe-expression-evaluator.ts [app-route] (ecmascript, async loader)");
                    // Create evaluation context
                    const context = {
                        input: state.variables.input,
                        lastOutput: state.variables.lastOutput,
                        state: state.variables,
                        // Also expose individual state variables
                        ...state.variables
                    };
                    finalValue = safeEvaluate(rawValue, context);
                } catch (e) {
                    throw new Error(`Expression evaluation failed: ${e instanceof Error ? e.message : 'Unknown error'}`);
                }
                break;
            default:
                // String - just use the substituted value
                finalValue = rawValue;
        }
        console.log('🔧 Set State - Final Value:', finalValue);
        // Set the state variable
        state.variables[key] = finalValue;
        return {
            key,
            value: finalValue,
            valueType,
            stateUpdated: true
        };
    } catch (error) {
        // Provide context about what failed
        throw new Error(`Failed to set state variable '${key}': ${error instanceof Error ? error.message : 'Unknown error'}`);
    }
}
async function executeExport(data, state) {
    // Simulate delay
    await new Promise((resolve)=>setTimeout(resolve, 100));
    // Get the data to export from lastOutput
    const input = state.variables['lastOutput'] || {};
    // For now, just return the data formatted for export
    // In a real implementation, this would handle different export formats
    return {
        exportData: input,
        exported: true
    };
} /**
 * TODO: Real implementation
 *
 * For transform:
 * - Execute JavaScript transform script safely (sandboxed)
 * - Support common transformation libraries (lodash, etc)
 * - Handle errors gracefully
 * - Support async transformations
 *
 * For set-state:
 * - Parse value expressions
 * - Support references to other variables
 * - Validate types
 * - Support nested object paths
 */ 
}),
"[project]/lib/workflow/executors/tools.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeToolsNode",
    ()=>executeToolsNode
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$server$2d$only$2f$empty$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/compiled/server-only/empty.js [app-route] (ecmascript)");
;
async function executeToolsNode(node, state) {
    const { type, data } = node;
    const nodeType = data.nodeType || type;
    if (nodeType.includes('file') || nodeType === 'file-search') {
        return await executeFileSearch(data, state);
    }
    if (nodeType.includes('guardrail')) {
        return await executeGuardrails(data, state);
    }
    throw new Error(`Unknown tools node type: ${nodeType}`);
}
async function executeFileSearch(data, state) {
    // Simulate delay
    await new Promise((resolve)=>setTimeout(resolve, 800));
    // Mock file search results
    const mockFiles = [
        {
            path: 'src/components/Button.tsx',
            matches: 3,
            content: includeContent(data) ? 'export default function Button() { ... }' : undefined
        },
        {
            path: 'src/utils/helpers.ts',
            matches: 2,
            content: includeContent(data) ? 'export function formatData() { ... }' : undefined
        },
        {
            path: 'app/page.tsx',
            matches: 1,
            content: includeContent(data) ? 'export default function Home() { ... }' : undefined
        }
    ];
    const maxResults = parseInt(data.maxResults || '10');
    const results = mockFiles.slice(0, maxResults);
    return {
        query: data.searchQuery || '',
        pattern: data.filePattern || '*.ts,*.tsx',
        results,
        totalMatches: results.length,
        filesSearched: 156
    };
}
function includeContent(data) {
    return data.includeContent !== false;
}
async function executeGuardrails(data, state) {
    const inputText = state.variables['lastOutput'] || state.variables['input'] || '';
    const textToCheck = typeof inputText === 'string' ? inputText : JSON.stringify(inputText);
    // Check which guardrails are enabled
    const enabledChecks = [];
    if (data.piiEnabled) enabledChecks.push('PII');
    if (data.moderationEnabled) enabledChecks.push('Moderation');
    if (data.jailbreakEnabled) enabledChecks.push('Jailbreak');
    if (data.hallucinationEnabled) enabledChecks.push('Hallucination');
    console.log('🛡️ Guardrails check:', enabledChecks.join(', '));
    console.log('📝 Checking text:', textToCheck.substring(0, 100));
    // Server-side implementation: basic checks
    // TODO: Integrate with actual content moderation APIs
    const badWords = [
        'hack',
        'exploit',
        'attack',
        'spam'
    ];
    const lowerText = textToCheck.toLowerCase();
    const hasBadWord = badWords.some((word)=>lowerText.includes(word));
    if (hasBadWord && data.actionOnViolation === 'block') {
        throw new Error(`Guardrail violation: Potentially harmful content detected`);
    }
    return {
        passed: !hasBadWord,
        checks_run: enabledChecks,
        violations: hasBadWord ? [
            'Harmful content detected'
        ] : [],
        message: hasBadWord ? 'Content flagged' : `All guardrails passed (${enabledChecks.length} checks)`
    };
} /**
 * TODO: Real implementation
 *
 * For file-search:
 * - Use ripgrep or similar fast search
 * - Support glob patterns
 * - Index codebase for faster searching
 * - Return file paths and line numbers
 *
 * For guardrails:
 * - Integrate with content moderation APIs
 * - Use regex patterns for PII detection
 * - Custom rule engine for business logic
 * - Rate limiting and abuse prevention
 */ 
}),
"[externals]/dns [external] (dns, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("dns", () => require("dns"));

module.exports = mod;
}),
"[project]/lib/workflow/ssrf-protection.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * SSRF (Server-Side Request Forgery) Protection
 *
 * Prevents HTTP nodes from accessing:
 * - Private IP addresses (internal networks)
 * - Localhost/loopback addresses
 * - Cloud metadata endpoints
 * - Link-local addresses
 */ __turbopack_context__.s([
    "getAllowedDomains",
    ()=>getAllowedDomains,
    "isAllowedDomain",
    ()=>isAllowedDomain,
    "validateURLForSSRF",
    ()=>validateURLForSSRF,
    "validateURLForSSRFSync",
    ()=>validateURLForSSRFSync
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$url__$5b$external$5d$__$28$url$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/url [external] (url, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$dns__$5b$external$5d$__$28$dns$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/dns [external] (dns, cjs)");
var __TURBOPACK__imported__module__$5b$externals$5d2f$util__$5b$external$5d$__$28$util$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/util [external] (util, cjs)");
;
;
;
const dnsLookup = (0, __TURBOPACK__imported__module__$5b$externals$5d2f$util__$5b$external$5d$__$28$util$2c$__cjs$29$__["promisify"])(__TURBOPACK__imported__module__$5b$externals$5d2f$dns__$5b$external$5d$__$28$dns$2c$__cjs$29$__["lookup"]);
// Private IP ranges (RFC 1918)
const PRIVATE_IP_RANGES = [
    /^10\./,
    /^172\.(1[6-9]|2[0-9]|3[01])\./,
    /^192\.168\./,
    /^127\./,
    /^169\.254\./,
    /^0\./,
    /^255\.255\.255\.255$/
];
// Blocked hostnames
const BLOCKED_HOSTS = [
    'localhost',
    'localhost.localdomain',
    'ip6-localhost',
    'ip6-loopback',
    '0.0.0.0',
    '::1',
    'metadata.google.internal',
    'metadata',
    'instance-data'
];
// Cloud metadata endpoints (AWS, GCP, Azure, Oracle)
const METADATA_IP_PATTERNS = [
    /^169\.254\.169\.254$/,
    /^169\.254\.169\.253$/,
    /^fd00:ec2::254$/,
    /^100\.100\.100\.200$/
];
/**
 * Check if an IP address is private/internal
 */ function isPrivateIP(ip) {
    // Check IPv4 private ranges
    for (const pattern of PRIVATE_IP_RANGES){
        if (pattern.test(ip)) {
            return true;
        }
    }
    // Check IPv6 private ranges
    if (ip.startsWith('fe80:') || ip.startsWith('fc00:') || ip.startsWith('fd00:')) {
        return true; // Link-local and ULA
    }
    if (ip === '::1' || ip === '::') {
        return true; // Loopback and unspecified
    }
    return false;
}
/**
 * Check if an IP is a cloud metadata endpoint
 */ function isMetadataIP(ip) {
    for (const pattern of METADATA_IP_PATTERNS){
        if (pattern.test(ip)) {
            return true;
        }
    }
    return false;
}
/**
 * Check if a hostname is blocked
 */ function isBlockedHostname(hostname) {
    const normalizedHost = hostname.toLowerCase();
    return BLOCKED_HOSTS.some((blocked)=>normalizedHost === blocked || normalizedHost.endsWith(`.${blocked}`));
}
async function validateURLForSSRF(url) {
    try {
        // Parse the URL
        const parsedUrl = new __TURBOPACK__imported__module__$5b$externals$5d2f$url__$5b$external$5d$__$28$url$2c$__cjs$29$__["URL"](url);
        // Only allow HTTP/HTTPS
        if (parsedUrl.protocol !== 'http:' && parsedUrl.protocol !== 'https:') {
            return {
                valid: false,
                reason: `Protocol '${parsedUrl.protocol}' not allowed. Only HTTP and HTTPS are permitted.`
            };
        }
        const hostname = parsedUrl.hostname.toLowerCase();
        // Check blocked hostnames
        if (isBlockedHostname(hostname)) {
            return {
                valid: false,
                reason: `Hostname '${hostname}' is blocked for security reasons.`
            };
        }
        // Check if hostname is already an IP address
        const isIPAddress = /^\d+\.\d+\.\d+\.\d+$/.test(hostname) || hostname.includes(':');
        if (isIPAddress) {
            // Direct IP access - check if it's private
            if (isPrivateIP(hostname)) {
                return {
                    valid: false,
                    reason: `Direct access to private IP addresses is not allowed.`
                };
            }
            if (isMetadataIP(hostname)) {
                return {
                    valid: false,
                    reason: `Access to cloud metadata endpoints is blocked for security.`
                };
            }
        } else {
            // Hostname - resolve to IP and check
            try {
                const { address, family } = await dnsLookup(hostname);
                // Check if resolved IP is private
                if (isPrivateIP(address)) {
                    return {
                        valid: false,
                        reason: `Hostname '${hostname}' resolves to a private IP address (${address}).`
                    };
                }
                // Check if resolved IP is metadata endpoint
                if (isMetadataIP(address)) {
                    return {
                        valid: false,
                        reason: `Hostname '${hostname}' resolves to a cloud metadata endpoint (${address}).`
                    };
                }
            } catch (dnsError) {
                // DNS lookup failed - might be a DNS rebinding attack
                return {
                    valid: false,
                    reason: `Unable to resolve hostname '${hostname}'. DNS lookup failed.`
                };
            }
        }
        // Check for unusual ports (optional - you may want to allow custom ports)
        const port = parsedUrl.port;
        if (port) {
            const portNum = parseInt(port, 10);
            // Block common internal service ports
            const blockedPorts = [
                22,
                23,
                25,
                3306,
                5432,
                6379,
                27017,
                9200,
                11211
            ];
            if (blockedPorts.includes(portNum)) {
                return {
                    valid: false,
                    reason: `Port ${portNum} is blocked for security reasons.`
                };
            }
        }
        // URL is safe
        return {
            valid: true
        };
    } catch (error) {
        return {
            valid: false,
            reason: `Invalid URL: ${error instanceof Error ? error.message : 'Parse error'}`
        };
    }
}
function validateURLForSSRFSync(url) {
    try {
        const parsedUrl = new __TURBOPACK__imported__module__$5b$externals$5d2f$url__$5b$external$5d$__$28$url$2c$__cjs$29$__["URL"](url);
        // Only allow HTTP/HTTPS
        if (parsedUrl.protocol !== 'http:' && parsedUrl.protocol !== 'https:') {
            return {
                valid: false,
                reason: `Protocol '${parsedUrl.protocol}' not allowed.`
            };
        }
        const hostname = parsedUrl.hostname.toLowerCase();
        // Check blocked hostnames
        if (isBlockedHostname(hostname)) {
            return {
                valid: false,
                reason: `Hostname '${hostname}' is blocked.`
            };
        }
        // Check if it's an IP address
        const isIPAddress = /^\d+\.\d+\.\d+\.\d+$/.test(hostname) || hostname.includes(':');
        if (isIPAddress) {
            if (isPrivateIP(hostname) || isMetadataIP(hostname)) {
                return {
                    valid: false,
                    reason: `Access to private/internal IP addresses is not allowed.`
                };
            }
        }
        return {
            valid: true
        };
    } catch (error) {
        return {
            valid: false,
            reason: `Invalid URL: ${error instanceof Error ? error.message : 'Unknown error'}`
        };
    }
}
function getAllowedDomains() {
    const domains = process.env.ALLOWED_HTTP_DOMAINS;
    if (!domains) return null;
    return domains.split(',').map((d)=>d.trim()).filter((d)=>d.length > 0);
}
function isAllowedDomain(url, allowedDomains) {
    try {
        const parsedUrl = new __TURBOPACK__imported__module__$5b$externals$5d2f$url__$5b$external$5d$__$28$url$2c$__cjs$29$__["URL"](url);
        const hostname = parsedUrl.hostname.toLowerCase();
        for (const allowed of allowedDomains){
            // Wildcard support: *.example.com
            if (allowed.startsWith('*.')) {
                const domain = allowed.slice(2).toLowerCase();
                if (hostname === domain || hostname.endsWith(`.${domain}`)) {
                    return true;
                }
            } else {
                // Exact match
                if (hostname === allowed.toLowerCase()) {
                    return true;
                }
            }
        }
        return false;
    } catch  {
        return false;
    }
}
}),
"[project]/lib/workflow/executors/http.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeHTTPNode",
    ()=>executeHTTPNode
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$ssrf$2d$protection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/ssrf-protection.ts [app-route] (ecmascript)");
;
;
async function executeHTTPNode(node, state) {
    const { data } = node;
    const nodeData = data;
    try {
        // Substitute variables in URL
        const url = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(nodeData.httpUrl || '', state);
        const method = nodeData.httpMethod || 'GET';
        // SSRF Protection: Validate URL
        const validation = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$ssrf$2d$protection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["validateURLForSSRF"])(url);
        if (!validation.valid) {
            throw new Error(`SSRF Protection: ${validation.reason}`);
        }
        // Optional: Check domain whitelist if configured
        const allowedDomains = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$ssrf$2d$protection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getAllowedDomains"])();
        if (allowedDomains && allowedDomains.length > 0) {
            if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$ssrf$2d$protection$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["isAllowedDomain"])(url, allowedDomains)) {
                throw new Error(`Domain not in whitelist. Allowed domains: ${allowedDomains.join(', ')}`);
            }
        }
        // Build headers
        const headers = {};
        if (nodeData.httpHeaders && Array.isArray(nodeData.httpHeaders)) {
            nodeData.httpHeaders.forEach((h)=>{
                if (h.key && h.value) {
                    headers[h.key] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(h.value, state);
                }
            });
        }
        // Add authentication
        if (nodeData.httpAuthType === 'bearer' && nodeData.httpAuthToken) {
            headers['Authorization'] = `Bearer ${nodeData.httpAuthToken}`;
        } else if (nodeData.httpAuthType === 'api-key' && nodeData.httpAuthToken) {
            headers['X-API-Key'] = nodeData.httpAuthToken;
        } else if (nodeData.httpAuthType === 'basic' && nodeData.httpAuthToken) {
            headers['Authorization'] = `Basic ${btoa(nodeData.httpAuthToken)}`;
        }
        // Build request body
        let body = undefined;
        if ((method === 'POST' || method === 'PUT' || method === 'PATCH') && nodeData.httpBody) {
            body = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(nodeData.httpBody, state);
        }
        console.log('HTTP Request:', {
            method,
            url,
            headers,
            body
        });
        // Make the request
        const response = await fetch(url, {
            method,
            headers,
            body
        });
        const responseData = await response.json().catch(()=>response.text());
        if (!response.ok) {
            throw new Error(`HTTP ${response.status}: ${response.statusText}`);
        }
        // Convert headers to plain object
        const headersObj = {};
        response.headers.forEach((value, key)=>{
            headersObj[key] = value;
        });
        return {
            status: response.status,
            statusText: response.statusText,
            headers: headersObj,
            data: responseData,
            url,
            method
        };
    } catch (error) {
        console.error('HTTP request error:', error);
        throw new Error(`HTTP request failed: ${error instanceof Error ? error.message : 'Unknown error'}`);
    }
}
}),
"[project]/lib/workflow/executors/extract.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeExtractNode",
    ()=>executeExtractNode
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$server$2d$only$2f$empty$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/compiled/server-only/empty.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/api/models.ts [app-route] (ecmascript)");
;
;
;
async function executeExtractNode(node, state, apiKeys) {
    const { data } = node;
    try {
        // Substitute variables in instructions
        const instructions = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(data.instructions || 'Extract information from the input', state);
        // Build context from previous node output
        const lastOutput = state.variables?.lastOutput;
        // Validate API keys are provided
        if (!apiKeys) {
            throw new Error('API keys are required for server-side execution');
        }
        // Server-side execution only
        const OpenAI = (await __turbopack_context__.A("[project]/node_modules/openai/index.mjs [app-route] (ecmascript, async loader)")).default;
        const client = new OpenAI({
            apiKey: apiKeys?.openai
        });
        // Build full prompt with context
        let fullPrompt = instructions;
        if (lastOutput) {
            const contextData = typeof lastOutput === 'string' ? lastOutput : JSON.stringify(lastOutput, null, 2);
            const maxLength = 10000;
            const truncated = contextData.substring(0, maxLength);
            const wasTruncated = contextData.length > maxLength;
            fullPrompt = `${fullPrompt}\n\nData to extract from:\n${truncated}`;
            if (wasTruncated) {
                const originalLength = contextData.length;
                console.warn(`[Extract] Input truncated from ${originalLength} to ${maxLength} characters`);
                fullPrompt += `\n\n[Note: Input was truncated from ${originalLength} to ${maxLength} characters due to length limits]`;
            }
        }
        // Parse JSON schema
        let schema;
        try {
            schema = typeof data.jsonSchema === 'string' ? JSON.parse(data.jsonSchema) : data.jsonSchema;
        } catch (e) {
            throw new Error(`Invalid JSON schema: ${e instanceof Error ? e.message : 'Parse error'}`);
        }
        // If MCP tools are configured, use Responses API
        if (data.mcpTools && data.mcpTools.length > 0) {
            const tools = data.mcpTools.map((mcp)=>({
                    type: 'mcp',
                    server_label: mcp.name,
                    server_url: mcp.url && mcp.url.includes('{FIRECRAWL_API_KEY}') ? mcp.url.replace('{FIRECRAWL_API_KEY}', encodeURIComponent(apiKeys?.firecrawl || '')) : mcp.url || '',
                    authorization: mcp.accessToken ? `Bearer ${mcp.accessToken}` : undefined,
                    require_approval: 'never'
                }));
            const response = await client.responses.create({
                model: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DEFAULT_MODELS"].openai,
                tools,
                input: fullPrompt,
                text: {
                    format: {
                        type: 'json_schema',
                        name: 'extraction',
                        schema,
                        strict: true
                    }
                }
            });
            let extractedData;
            try {
                extractedData = JSON.parse(response.output_text || '{}');
            } catch (e) {
                console.error('Failed to parse LLM extraction response:', response.output_text);
                throw new Error(`LLM returned invalid JSON: ${e instanceof Error ? e.message : 'Parse error'}`);
            }
            return {
                extractedData,
                model: __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DEFAULT_MODELS"].openai,
                tokensUsed: response.usage?.total_tokens || 0,
                mcpToolsUsed: response.output.filter((item)=>item.type === 'mcp_call').length,
                __variableUpdates: {
                    lastOutput: extractedData
                }
            };
        }
        // No MCP - use regular Chat Completions with JSON mode
        const completion = await client.chat.completions.create({
            model: data.model || __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DEFAULT_MODELS"].openai,
            messages: [
                {
                    role: 'user',
                    content: fullPrompt
                }
            ],
            response_format: {
                type: 'json_schema',
                json_schema: {
                    name: 'extraction',
                    schema,
                    strict: true
                }
            }
        });
        let extractedData;
        try {
            extractedData = JSON.parse(completion.choices[0].message.content || '{}');
        } catch (e) {
            console.error('Failed to parse LLM extraction response:', completion.choices[0].message.content);
            throw new Error(`LLM returned invalid JSON: ${e instanceof Error ? e.message : 'Parse error'}`);
        }
        return {
            extractedData,
            model: data.model,
            tokensUsed: completion.usage?.total_tokens || 0,
            __variableUpdates: {
                lastOutput: extractedData
            }
        };
    } catch (error) {
        console.error('Extract execution error:', error);
        throw new Error(`Failed to execute extract: ${error instanceof Error ? error.message : 'Unknown error'}`);
    }
}
}),
"[externals]/punycode [external] (punycode, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("punycode", () => require("punycode"));

module.exports = mod;
}),
"[externals]/node:stream/web [external] (node:stream/web, cjs)", ((__turbopack_context__, module, exports) => {

const mod = __turbopack_context__.x("node:stream/web", () => require("node:stream/web"));

module.exports = mod;
}),
"[project]/lib/workflow/executors/arcade.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "executeArcadeNode",
    ()=>executeArcadeNode
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$server$2d$only$2f$empty$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/compiled/server-only/empty.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/variable-substitution.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$arcadeai$2f$arcadejs$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/@arcadeai/arcadejs/index.mjs [app-route] (ecmascript) <locals>");
;
;
;
async function executeArcadeNode(node, state, apiKey) {
    const { data } = node;
    try {
        // Get Arcade configuration
        const arcadeTool = data.arcadeTool; // e.g., "GoogleDocs.CreateDocumentFromText@4.3.1"
        // Support both arcadeInput (templates) and arcadeParams (UI panel)
        const arcadeInput = data.arcadeInput || data.arcadeParams || {};
        const arcadeUserId = data.arcadeUserId || 'workflow-builder';
        if (!arcadeTool) {
            throw new Error('Arcade tool not configured. Please open the node settings and select a tool from the Arcade.dev catalog.');
        }
        // Substitute variables in the input
        const substitutedInput = {};
        for (const [key, value] of Object.entries(arcadeInput)){
            if (typeof value === 'string') {
                substitutedInput[key] = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$variable$2d$substitution$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["substituteVariables"])(value, state);
            } else {
                substitutedInput[key] = value;
            }
        }
        console.log('📝 Arcade Input (after substitution):', JSON.stringify(substitutedInput, null, 2));
        const arcadeApiKey = apiKey ?? process.env.ARCADE_API_KEY;
        if (!arcadeApiKey) {
            throw new Error('ARCADE_API_KEY not configured in .env.local');
        }
        console.log('🎮 Arcade Node Execution:', {
            tool: arcadeTool,
            userId: arcadeUserId
        });
        // Initialize Arcade client
        const client = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$arcadeai$2f$arcadejs$2f$index$2e$mjs__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["default"]({
            apiKey: arcadeApiKey
        });
        // Step 1: Authorize the tool
        const auth = await client.tools.authorize({
            tool_name: arcadeTool,
            user_id: arcadeUserId
        });
        console.log('🔐 Arcade Auth Status:', auth.status);
        if (!auth?.id) {
            throw new Error('Authorization failed: missing authorization id from Arcade');
        }
        // Check if authorization is completed
        if (auth.status === 'failed') {
            throw new Error(`Authorization failed for ${arcadeTool}`);
        }
        if (auth.status !== 'completed') {
            console.log('⚠️ Authorization required:', auth.url);
            return {
                __arcadePendingAuth: true,
                authUrl: auth.url,
                authId: auth.id,
                toolName: arcadeTool,
                userId: arcadeUserId,
                message: `Authorization required for ${arcadeTool}`,
                pendingInput: substitutedInput
            };
        }
        console.log('✅ Authorization successful!');
        // Step 2: Execute the tool
        const result = await client.tools.execute({
            tool_name: arcadeTool,
            input: substitutedInput,
            user_id: arcadeUserId
        });
        console.log('🎯 Arcade Result:', JSON.stringify(result, null, 2).substring(0, 500));
        // Extract the output value
        const output = result.output?.value || result.output || result;
        return {
            success: true,
            result: output,
            fullResult: result,
            toolUsed: arcadeTool,
            userId: arcadeUserId,
            __variableUpdates: {
                lastOutput: output
            }
        };
    } catch (error) {
        console.error('❌ Arcade execution error:', error);
        throw new Error(`Arcade execution failed: ${error.message || 'Unknown error'}`);
    }
}
}),
"[project]/lib/arcade/auth-store.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "createOrUpdateArcadeAuthRecord",
    ()=>createOrUpdateArcadeAuthRecord,
    "deleteArcadeAuthRecord",
    ()=>deleteArcadeAuthRecord,
    "getArcadeAuthRecord",
    ()=>getArcadeAuthRecord,
    "listArcadeAuthRecords",
    ()=>listArcadeAuthRecords,
    "updateArcadeAuthRecord",
    ()=>updateArcadeAuthRecord
]);
function getMemoryStore() {
    if (!globalThis.__arcadeAuthStore) {
        globalThis.__arcadeAuthStore = new Map();
    }
    return globalThis.__arcadeAuthStore;
}
function cloneRecord(record) {
    return JSON.parse(JSON.stringify(record));
}
async function createOrUpdateArcadeAuthRecord(data) {
    const now = new Date().toISOString();
    let existing = null;
    // Get existing record from memory store
    existing = getMemoryStore().get(data.authId) || null;
    const record = {
        authId: data.authId,
        executionId: data.executionId,
        workflowId: data.workflowId,
        nodeId: data.nodeId,
        toolName: data.toolName,
        authUrl: data.authUrl ?? existing?.authUrl ?? undefined,
        status: data.status ?? existing?.status ?? 'pending',
        userId: data.userId ?? existing?.userId,
        pendingInput: data.pendingInput ?? existing?.pendingInput,
        createdAt: existing?.createdAt ?? now,
        updatedAt: now
    };
    // Use in-memory store
    getMemoryStore().set(record.authId, record);
    return cloneRecord(record);
}
async function getArcadeAuthRecord(authId) {
    // Use in-memory store
    const record = getMemoryStore().get(authId);
    return record ? cloneRecord(record) : null;
}
async function updateArcadeAuthRecord(authId, updates) {
    const existing = await getArcadeAuthRecord(authId);
    if (!existing) {
        return null;
    }
    const next = {
        ...existing,
        ...updates,
        updatedAt: new Date().toISOString()
    };
    // Use in-memory store
    getMemoryStore().set(authId, next);
    return cloneRecord(next);
}
async function deleteArcadeAuthRecord(authId) {
    // Delete from memory store
    getMemoryStore().delete(authId);
}
function listArcadeAuthRecords() {
    // This only returns memory store records
    // For Redis, we'd need a different approach (e.g., scan with pattern)
    return Array.from(getMemoryStore().values()).map(cloneRecord);
}
}),
"[project]/lib/langsmith/config.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * LangSmith Tracing Configuration
 *
 * Automatically enables LangSmith tracing when environment variables are set.
 * LangSmith provides monitoring and debugging for LLM applications.
 *
 * Setup:
 * 1. Sign up at https://smith.langchain.com/
 * 2. Get your API key from Settings → API Keys
 * 3. Set environment variables in .env.local:
 *    LANGCHAIN_TRACING_V2=true
 *    LANGCHAIN_API_KEY=lsv2_pt_...
 *    LANGCHAIN_PROJECT=open-agent-builder
 *    LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
 */ /**
 * Check if LangSmith tracing is enabled
 */ __turbopack_context__.s([
    "getLangGraphConfig",
    ()=>getLangGraphConfig,
    "getLangSmithConfig",
    ()=>getLangSmithConfig,
    "isLangSmithEnabled",
    ()=>isLangSmithEnabled
]);
function isLangSmithEnabled() {
    return process.env.LANGCHAIN_TRACING_V2 === 'true' && !!process.env.LANGCHAIN_API_KEY;
}
function getLangSmithConfig() {
    if (!isLangSmithEnabled()) {
        return null;
    }
    return {
        project: process.env.LANGCHAIN_PROJECT || 'open-agent-builder',
        apiKey: process.env.LANGCHAIN_API_KEY,
        endpoint: process.env.LANGCHAIN_ENDPOINT || 'https://api.smith.langchain.com'
    };
}
function getLangGraphConfig(baseConfig) {
    const config = {
        ...baseConfig
    };
    // LangSmith tracing is automatically enabled by environment variables
    // No explicit callbacks needed - LangChain handles this internally
    // when LANGCHAIN_TRACING_V2=true
    if (isLangSmithEnabled()) {
        console.log('[LangSmith] Tracing enabled for project:', process.env.LANGCHAIN_PROJECT || 'open-agent-builder');
    }
    return config;
}
/**
 * Log LangSmith status on module load
 */ if ("TURBOPACK compile-time truthy", 1) {
    // Server-side only
    if (isLangSmithEnabled()) {
        const config = getLangSmithConfig();
        console.log('✅ LangSmith tracing enabled');
        console.log('   Project:', config?.project);
        console.log('   Endpoint:', config?.endpoint);
    } else {
        console.log('⚠️  LangSmith tracing disabled');
        if (!process.env.LANGCHAIN_TRACING_V2) {
            console.log('   Set LANGCHAIN_TRACING_V2=true in .env.local to enable');
        }
        if (!process.env.LANGCHAIN_API_KEY) {
            console.log('   Set LANGCHAIN_API_KEY in .env.local (get from https://smith.langchain.com/)');
        }
    }
}
}),
"[project]/lib/workflow/langgraph.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

/**
 * LangGraph Integration for Open Agent Builder
 *
 * This module provides LangGraph-powered workflow execution with:
 * - StateGraph-based orchestration
 * - Conditional routing
 * - State persistence
 * - Streaming support
 * - Export/Import capabilities
 * Server-side only - should not be imported in client code
 */ __turbopack_context__.s([
    "LangGraphExecutor",
    ()=>LangGraphExecutor,
    "WorkflowStateAnnotation",
    ()=>WorkflowStateAnnotation,
    "createExampleLangGraphWorkflow",
    ()=>createExampleLangGraphWorkflow,
    "langGraphJSONToWorkflow",
    ()=>langGraphJSONToWorkflow,
    "workflowToLangGraphCode",
    ()=>workflowToLangGraphCode,
    "workflowToLangGraphJSON",
    ()=>workflowToLangGraphJSON
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$compiled$2f$server$2d$only$2f$empty$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/compiled/server-only/empty.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__ = __turbopack_context__.i("[externals]/@langchain/langgraph [external] (@langchain/langgraph, esm_import)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$safe$2d$expression$2d$evaluator$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/safe-expression-evaluator.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/agent.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/mcp.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$logic$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/logic.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$data$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/data.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tools$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/tools.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$http$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/http.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$extract$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/extract.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$arcade$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/executors/arcade.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$arcade$2f$auth$2d$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/arcade/auth-store.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/api/models.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$langsmith$2f$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/langsmith/config.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__
]);
[__TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
;
;
;
;
;
;
;
;
;
const WorkflowStateAnnotation = __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"].Root({
    // Core workflow variables
    variables: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])({
        reducer: (left, right)=>({
                ...left,
                ...right
            }),
        default: ()=>({
                input: '',
                lastOutput: ''
            })
    }),
    // Chat history for conversational workflows
    chatHistory: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])({
        reducer: (left, right)=>[
                ...left || [],
                ...right || []
            ],
        default: ()=>[]
    }),
    // Execution metadata
    currentNodeId: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])({
        reducer: (_, right)=>right,
        default: ()=>''
    }),
    // Node results for tracking
    nodeResults: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])({
        reducer: (left, right)=>({
                ...left,
                ...right
            }),
        default: ()=>({})
    }),
    // Pending authorization state (for Arcade auth, user approval, etc.)
    pendingAuth: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])({
        reducer: (_, right)=>right,
        default: ()=>null
    }),
    // Loop results accumulator (array that accumulates across iterations)
    loopResults: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])({
        reducer: (left, right)=>[
                ...left || [],
                ...right || []
            ],
        default: ()=>[]
    })
});
class LangGraphExecutor {
    workflow;
    graph;
    apiKeys;
    onNodeUpdate;
    checkpointer;
    parallelNodeIds = new Set();
    activeThreadId;
    activeExecutionId;
    pendingAuth = null;
    lastStreamState = null;
    edgesBySource = new Map();
    constructor(workflow, onNodeUpdate, apiKeys){
        this.workflow = workflow;
        this.onNodeUpdate = onNodeUpdate;
        this.apiKeys = apiKeys;
        // Initialize checkpointer for state persistence
        // - Required for human-in-the-loop (interrupts)
        // - Time-travel debugging
        // Note: MemorySaver is lightweight and doesn't impact performance
        this.checkpointer = new __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["MemorySaver"]();
        // Build the LangGraph StateGraph
        this.graph = this.buildGraph();
    }
    /**
   * Build LangGraph StateGraph from workflow definition
   */ buildGraph() {
        console.log('Building LangGraph from workflow:', {
            workflowId: this.workflow.id,
            workflowName: this.workflow.name,
            nodes: this.workflow.nodes.length,
            edges: this.workflow.edges.length,
            nodeIds: this.workflow.nodes.map((n)=>n.id),
            nodeTypes: this.workflow.nodes.map((n)=>n.data?.nodeType || n.type),
            edgeSources: this.workflow.edges.map((e)=>e.source),
            edgeTargets: this.workflow.edges.map((e)=>e.target),
            fullNodes: this.workflow.nodes,
            fullEdges: this.workflow.edges
        });
        const builder = new __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["StateGraph"](WorkflowStateAnnotation);
        this.parallelNodeIds.clear();
        this.edgesBySource = new Map();
        // Create a set of valid node IDs for edge validation
        const validNodeIds = new Set(this.workflow.nodes.map((n)=>n.id));
        // Build edge map, skipping edges with invalid source/target
        for (const edge of this.workflow.edges){
            // Validate that both source and target nodes exist
            if (!validNodeIds.has(edge.source)) {
                console.warn(`⚠️ Skipping edge ${edge.id}: source node '${edge.source}' does not exist`);
                continue;
            }
            if (!validNodeIds.has(edge.target)) {
                console.warn(`⚠️ Skipping edge ${edge.id}: target node '${edge.target}' does not exist`);
                continue;
            }
            if (!this.edgesBySource.has(edge.source)) {
                this.edgesBySource.set(edge.source, []);
            }
            this.edgesBySource.get(edge.source).push(edge);
        }
        console.log('Edges by source:', Object.fromEntries(this.edgesBySource));
        // Add nodes to the graph
        for (const node of this.workflow.nodes){
            const nodeType = node.data?.nodeType || node.type;
            // Skip note nodes entirely - they are visual-only sticky notes
            if (nodeType === 'note') {
                console.log(`Skipping note node ${node.id} (visual only)`);
                continue;
            }
            // Skip end nodes - LangGraph has built-in END
            // But we still want to execute them for UI feedback
            if (nodeType === 'end') {
                // Create a special end node executor that notifies UI before transitioning to END
                const endNodeExecutor = this.createNodeExecutor(node);
                builder.addNode(node.id, endNodeExecutor);
                continue;
            }
            // Handle start nodes - use regular executor but still connect to LangGraph's START
            if (nodeType === 'start') {
                const startNodeExecutor = this.createNodeExecutor(node);
                builder.addNode(node.id, startNodeExecutor);
                continue;
            }
            // Create node executor function
            const nodeExecutor = this.createNodeExecutor(node);
            const outgoingEdges = this.edgesBySource.get(node.id) || [];
            const shouldParallelize = this.shouldUseParallelRouting(nodeType, outgoingEdges);
            if (shouldParallelize) {
                this.parallelNodeIds.add(node.id);
            }
            builder.addNode(node.id, nodeExecutor, shouldParallelize ? {
                ends: this.getParallelEnds(outgoingEdges)
            } : undefined);
        }
        // Add edges (skip edges connected to note nodes)
        const conditionalNodes = new Set();
        const whileLoopNodes = new Set();
        for (const [sourceId, sourceEdges] of Array.from(this.edgesBySource.entries())){
            const sourceNode = this.workflow.nodes.find((n)=>n.id === sourceId);
            const sourceType = sourceNode?.data?.nodeType || sourceNode?.type;
            // Skip edges from note nodes (notes are visual only)
            if (sourceType === 'note') {
                console.log(`Skipping edges from note node ${sourceId}`);
                continue;
            }
            if (sourceType === 'while') {
                if (!whileLoopNodes.has(sourceId)) {
                    const routingFunction = this.createWhileLoopRouter(sourceId);
                    const pathMap = this.buildWhilePathMap(sourceId, sourceEdges);
                    builder.addConditionalEdges(sourceId, routingFunction, pathMap);
                    whileLoopNodes.add(sourceId);
                }
                continue;
            }
            if (sourceType === 'if-else' || sourceType === 'if / else') {
                if (!conditionalNodes.has(sourceId)) {
                    const routingFunction = this.createConditionalRouter(sourceId);
                    const pathMap = {};
                    for (const edge of sourceEdges){
                        const handle = edge.sourceHandle || 'default';
                        pathMap[handle] = edge.target;
                    }
                    builder.addConditionalEdges(sourceId, routingFunction, pathMap);
                    conditionalNodes.add(sourceId);
                }
                continue;
            }
            // For regular nodes, add their outgoing edges
            for (const edge of sourceEdges){
                // Verify target node exists
                const targetNode = this.workflow.nodes.find((n)=>n.id === edge.target);
                if (!targetNode) {
                    console.warn(`⚠️ Skipping edge ${edge.id}: target node '${edge.target}' not found`);
                    continue;
                }
                // Skip edges that connect to note nodes (notes are visual only)
                const targetType = targetNode.data?.nodeType || targetNode.type;
                if (targetType === 'note') {
                    console.log(`Skipping edge to note node ${edge.target}`);
                    continue;
                }
                // Use the actual target node ID for all edges, including end nodes
                // Since we're adding end nodes as actual nodes, we don't need to redirect to END
                builder.addEdge(sourceId, edge.target);
            }
        }
        // Connect LangGraph's START to our start node
        console.log('Connecting LangGraph START to workflow start node...');
        const startNode = this.workflow.nodes.find((n)=>{
            const nodeType = n.data?.nodeType || n.type;
            return nodeType === 'start';
        });
        if (startNode) {
            console.log(`Connecting LangGraph START to start node: ${startNode.id}`);
            builder.addEdge(__TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["START"], startNode.id);
        }
        // Connect all end nodes to LangGraph's END
        const endNodes = this.workflow.nodes.filter((n)=>{
            const nodeType = n.data?.nodeType || n.type;
            return nodeType === 'end';
        });
        for (const endNode of endNodes){
            console.log(`Connecting end node ${endNode.id} to LangGraph END`);
            builder.addEdge(endNode.id, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["END"]);
        }
        // Compile the graph WITH checkpointing (required for interrupts/approvals)
        try {
            return builder.compile({
                checkpointer: this.checkpointer
            });
        } catch (error) {
            // Provide descriptive error messages for common issues
            if (error instanceof Error && error.message.includes('is not reachable')) {
                // Extract the unreachable node ID from the error
                const match = error.message.match(/Node `([^`]+)` is not reachable/);
                const unreachableNodeId = match ? match[1] : 'unknown';
                const unreachableNode = this.workflow.nodes.find((n)=>n.id === unreachableNodeId);
                const nodeName = unreachableNode?.data?.nodeName || unreachableNode?.type || unreachableNodeId;
                // Find what nodes ARE reachable to help debug
                const allNodeIds = new Set(this.workflow.nodes.map((n)=>n.id));
                const connectedFromStart = new Set();
                // BFS from start node to find all reachable nodes
                const startNode = this.workflow.nodes.find((n)=>{
                    const nodeType = n.data?.nodeType || n.type;
                    return nodeType === 'start';
                });
                if (startNode) {
                    const queue = [
                        startNode.id
                    ];
                    connectedFromStart.add(startNode.id);
                    while(queue.length > 0){
                        const currentId = queue.shift();
                        const outgoingEdges = this.edgesBySource.get(currentId) || [];
                        for (const edge of outgoingEdges){
                            if (!connectedFromStart.has(edge.target)) {
                                connectedFromStart.add(edge.target);
                                queue.push(edge.target);
                            }
                        }
                    }
                }
                const unreachableNodes = Array.from(allNodeIds).filter((id)=>!connectedFromStart.has(id));
                throw new Error(`Workflow configuration error: Node "${nodeName}" (${unreachableNodeId}) is not connected to the workflow.\n\n` + `Unreachable nodes: ${unreachableNodes.map((id)=>{
                    const node = this.workflow.nodes.find((n)=>n.id === id);
                    const name = node?.data?.nodeName || node?.type || id;
                    return `"${name}" (${id})`;
                }).join(', ')}\n\n` + `To fix this:\n` + `1. Connect the Start node to your first agent/action node\n` + `2. Ensure all nodes have incoming connections (except Start)\n` + `3. Check that all edges point to valid nodes\n\n` + `Tip: Use the canvas to drag connections between node handles`);
            }
            // Re-throw other errors
            throw error;
        }
    }
    /**
   * Create node executor function for LangGraph (CLEAN VERSION)
   */ createNodeExecutor(node) {
        return async (state)=>{
            console.log(`Executing node: ${node.id}`);
            // Notify UI
            const result = {
                nodeId: node.id,
                status: 'running',
                startedAt: new Date().toISOString()
            };
            this.onNodeUpdate?.(node.id, result);
            try {
                // Execute the node (pure, server-side)
                const output = await this.executeNodePure(node, state);
                // Log full output shape for debugging
                console.log(`Node ${node.id} output shape:`, {
                    outputType: typeof output,
                    outputKeys: output && typeof output === 'object' && output !== null ? Object.keys(output) : [],
                    hasAgentValue: output && typeof output === 'object' && output !== null && '__agentValue' in output,
                    hasAgentToolCalls: output && typeof output === 'object' && output !== null && '__agentToolCalls' in output
                });
                // Check if output is a pending approval (user-approval node)
                if (output && typeof output === 'object' && output !== null && '__pendingApproval' in output) {
                    console.log(`Detected pending approval from node ${node.id}`);
                    return await this.handlePendingApproval(node, result, output);
                }
                // Extract tool calls and chat history if this is an agent node
                let actualOutput = output;
                let toolCalls = undefined;
                let chatHistoryUpdates = [];
                let variableUpdates = {};
                let usage = undefined;
                if (output && typeof output === 'object' && output !== null && '__agentValue' in output) {
                    actualOutput = output.__agentValue;
                    toolCalls = output.__agentToolCalls;
                    chatHistoryUpdates = output.__chatHistoryUpdates || [];
                    variableUpdates = output.__variableUpdates || {};
                    usage = output.__usage;
                    console.log(`Extracted from agent output:`, {
                        actualOutput: typeof actualOutput === 'string' ? actualOutput.substring(0, 100) : actualOutput,
                        toolCallsCount: Array.isArray(toolCalls) ? toolCalls.length : 0,
                        toolCalls: toolCalls,
                        chatHistoryUpdates: chatHistoryUpdates.length,
                        variableUpdates: Object.keys(variableUpdates),
                        usage: usage
                    });
                }
                // Update result
                result.output = actualOutput;
                result.toolCalls = toolCalls;
                result.usage = usage;
                result.status = 'completed';
                result.completedAt = new Date().toISOString();
                this.onNodeUpdate?.(node.id, result);
                // For while loops, extract the iteration counter from output
                // Merge with any agent-provided variable updates
                // Use node name for variable storage, fallback to node ID
                const nodeKey = node.data?.nodeName || node.data?.name || node.id;
                const mergedVariableUpdates = {
                    lastOutput: actualOutput,
                    [nodeKey]: actualOutput,
                    [node.id]: actualOutput,
                    ...variableUpdates
                };
                // Check if output wants to append to loop results
                let loopResultsUpdate;
                if (actualOutput && typeof actualOutput === 'object' && '__appendToLoopResults' in actualOutput) {
                    const newResult = actualOutput.__appendToLoopResults;
                    loopResultsUpdate = [
                        newResult
                    ]; // Will be appended by reducer
                    console.log(`Appending result to loopResults array`);
                    // Remove the signal from lastOutput
                    const { __appendToLoopResults, ...cleanOutput } = actualOutput;
                    mergedVariableUpdates.lastOutput = cleanOutput;
                    mergedVariableUpdates[node.id] = cleanOutput;
                }
                // If this is a while loop, save the iteration counter to variables
                const nodeType = node.data?.nodeType || node.type;
                if (nodeType === 'while' && actualOutput && typeof actualOutput === 'object') {
                    const iterationKey = `${node.id}__iterationCount`;
                    if (iterationKey in actualOutput) {
                        mergedVariableUpdates[iterationKey] = actualOutput[iterationKey];
                        console.log(`Saving iteration counter: ${iterationKey} = ${actualOutput[iterationKey]}`);
                    }
                }
                // Return LangGraph state update (immutable, use reducer-friendly format)
                return {
                    variables: mergedVariableUpdates,
                    chatHistory: chatHistoryUpdates.length > 0 ? chatHistoryUpdates : [],
                    currentNodeId: node.id,
                    nodeResults: {
                        [node.id]: result
                    },
                    pendingAuth: null,
                    ...loopResultsUpdate ? {
                        loopResults: loopResultsUpdate
                    } : {}
                };
            } catch (error) {
                result.status = 'failed';
                result.error = error instanceof Error ? error.message : 'Unknown error';
                result.completedAt = new Date().toISOString();
                this.onNodeUpdate?.(node.id, result);
                throw error;
            }
        };
    }
    /**
   * Pure node execution (no state mutation)
   */ async executeNodePure(node, state) {
        const nodeType = node.data?.nodeType || node.type;
        const data = node.data;
        switch(nodeType){
            case 'start':
                // Return the input object properly without spreading strings
                const input = state.variables.input;
                // If input is a JSON string, parse it
                let parsedInput = input;
                if (typeof input === 'string') {
                    try {
                        parsedInput = JSON.parse(input);
                    } catch  {
                        // Not JSON, keep as string
                        parsedInput = input;
                    }
                }
                return {
                    message: 'Workflow started',
                    ...typeof parsedInput === 'object' && parsedInput !== null ? parsedInput : {
                        input: parsedInput
                    }
                };
            case 'agent':
                {
                    // Use the proper executeAgentNode which handles MCP tools
                    console.log(`Agent node ${node.id} configuration:`, {
                        hasMcpTools: !!data.mcpTools,
                        mcpToolsCount: data.mcpTools?.length || 0,
                        mcpTools: data.mcpTools
                    });
                    const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeAgentNode"])(node, state, this.apiKeys);
                    return result;
                }
            case 'mcp':
                {
                    return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMCPNode"])(node, state, this.apiKeys?.firecrawl);
                }
            case 'transform':
            case 'data-transform':
                {
                    // SECURITY FIX: Use E2B sandbox instead of Function() constructor
                    // Function() allows arbitrary code execution and bypasses security
                    // Import and use the secure data executor instead
                    const { executeDataTransformNode } = await __turbopack_context__.A("[project]/lib/workflow/executors/data.ts [app-route] (ecmascript, async loader)");
                    return await executeDataTransformNode(node, state, this.apiKeys);
                }
            case 'http':
                {
                    const url = data.httpUrl || '';
                    const method = data.httpMethod || 'GET';
                    const response = await fetch(url, {
                        method
                    });
                    return await response.json();
                }
            case 'if-else':
                {
                    // SECURITY FIX: Use safe evaluator (mathjs-based) instead of Function() constructor
                    const condition = data.condition || 'true';
                    try {
                        const result = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$safe$2d$expression$2d$evaluator$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["safeEvaluate"])(condition, {
                            input: state.variables.input,
                            state: state,
                            lastOutput: state.variables.lastOutput
                        });
                        return {
                            condition: Boolean(result),
                            branch: result ? 'if' : 'else'
                        };
                    } catch (error) {
                        console.error(`If-else condition evaluation error:`, error);
                        // Default to false branch on error
                        return {
                            condition: false,
                            branch: 'else'
                        };
                    }
                }
            case 'while':
                {
                    // Execute while loop check using the proper method
                    const tempState = {
                        variables: state.variables,
                        chatHistory: state.chatHistory
                    };
                    return await this.executeWhileNode(node, tempState, state);
                }
            case 'end':
                return {
                    message: 'Workflow completed',
                    finalOutput: state.variables.lastOutput
                };
            default:
                // For node types not in executeNodePure, fall back to the full executeNode implementation
                console.log(`WARNING: Node type '${nodeType}' not in executeNodePure, using executeNode fallback`);
                const tempState = {
                    variables: state.variables,
                    chatHistory: state.chatHistory
                };
                return await this.executeNode(node, tempState);
        }
    }
    /**
   * Create conditional router for if-else nodes
   */ createConditionalRouter(nodeId) {
        return async (state)=>{
            const node = this.workflow.nodes.find((n)=>n.id === nodeId);
            if (!node) return 'default';
            // Execute the if-else condition
            const tempState = {
                variables: state.variables,
                chatHistory: state.chatHistory
            };
            const result = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$logic$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeLogicNode"])(node, tempState);
            // Return the branch handle ('if' or 'else')
            return result.branch || 'else';
        };
    }
    shouldUseParallelRouting(nodeType, edges) {
        if (!edges || edges.length <= 1) {
            return false;
        }
        if (!nodeType) {
            return false;
        }
        const normalized = nodeType.toLowerCase();
        if ([
            'if-else',
            'if / else',
            'while',
            'user-approval',
            'user approval'
        ].includes(normalized)) {
            return false;
        }
        if (edges.some((edge)=>edge.sourceHandle)) {
            return false;
        }
        const uniqueTargets = new Set();
        let hasNonTerminalTarget = false;
        for (const edge of edges){
            if (!edge.target) continue;
            uniqueTargets.add(edge.target);
            const targetNode = this.workflow.nodes.find((n)=>n.id === edge.target);
            const targetType = targetNode?.data?.nodeType || targetNode?.type;
            if (targetType !== 'end') {
                hasNonTerminalTarget = true;
            }
        }
        return uniqueTargets.size > 1 && hasNonTerminalTarget;
    }
    getParallelEnds(edges) {
        const ends = new Set();
        for (const edge of edges){
            if (!edge.target) continue;
            const targetNode = this.workflow.nodes.find((n)=>n.id === edge.target);
            const targetType = targetNode?.data?.nodeType || targetNode?.type;
            if (targetType === 'end') {
                continue;
            }
            ends.add(edge.target);
        }
        return Array.from(ends);
    }
    buildWhilePathMap(nodeId, edges) {
        const pathMap = {};
        for (const edge of edges){
            const handle = (edge.sourceHandle || edge.label || '').toLowerCase();
            if ([
                'continue',
                'true',
                'yes',
                'loop',
                'next'
            ].includes(handle)) {
                pathMap['continue'] = edge.target;
            } else if ([
                'break',
                'false',
                'no',
                'exit',
                'stop',
                'end',
                'complete'
            ].includes(handle)) {
                pathMap['break'] = edge.target;
            } else if (!pathMap['continue']) {
                pathMap['continue'] = edge.target;
            } else if (!pathMap['break']) {
                pathMap['break'] = edge.target;
            }
        }
        if (!pathMap['continue'] && edges[0]) {
            pathMap['continue'] = edges[0].target;
        }
        if (!pathMap['break']) {
            console.warn(`While loop ${nodeId}: no explicit break edge found, defaulting to END`);
            pathMap['break'] = __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["END"];
        }
        return pathMap;
    }
    /**
   * Create while loop router for iteration control
   */ createWhileLoopRouter(nodeId) {
        return async (state)=>{
            const node = this.workflow.nodes.find((n)=>n.id === nodeId);
            if (!node) {
                console.warn(`While loop router: node ${nodeId} not found, breaking`);
                return 'break';
            }
            const iterationKey = `${nodeId}__iterationCount`;
            const result = state.nodeResults?.[nodeId];
            const loopOutput = result?.output || state.variables?.[node.id];
            const maxIterations = this.parseMaxIterations(node.data?.maxIterations);
            const currentIteration = Number(state.variables?.[iterationKey] || 0);
            console.log(`While loop router for ${nodeId}:`, {
                currentIteration,
                maxIterations,
                loopOutputCondition: loopOutput?.condition,
                stoppedReason: loopOutput?.stoppedReason
            });
            // Check if loop already decided to break
            if (loopOutput?.condition === false || loopOutput?.stoppedReason === 'condition_false') {
                console.log(`While loop ${nodeId}: loop output says break (condition=false)`);
                return 'break';
            }
            // Check for max_iterations reason
            if (loopOutput?.stoppedReason === 'max_iterations') {
                console.log(`While loop ${nodeId}: loop output says break (max_iterations reached)`);
                return 'break';
            }
            if (currentIteration >= maxIterations) {
                console.log(`While loop ${nodeId}: iteration count (${currentIteration}) >= max (${maxIterations}), breaking`);
                return 'break';
            }
            const shouldContinue = this.getWhileCondition(node, state, currentIteration, loopOutput);
            if (shouldContinue) {
                console.log(`While loop ${nodeId}: condition true, continuing`);
                return 'continue';
            }
            console.log(`While loop ${nodeId}: condition false, breaking`);
            return 'break';
        };
    }
    getWhileCondition(node, state, currentIteration, loopOutput) {
        if (loopOutput && typeof loopOutput === 'object' && 'condition' in loopOutput) {
            if (loopOutput.condition === true && loopOutput.stoppedReason === 'max_iterations') {
                return false;
            }
            return Boolean(loopOutput.condition);
        }
        const conditionExpr = node.data?.whileCondition || node.data?.condition || 'false';
        try {
            return Boolean((0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$safe$2d$expression$2d$evaluator$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["safeEvaluate"])(conditionExpr, {
                input: state.variables.input,
                state: state,
                lastOutput: state.variables.lastOutput,
                iteration: currentIteration
            }));
        } catch (error) {
            console.error(`ERROR: While loop ${node.id}: condition evaluation error`, error);
            return false;
        }
    }
    parseMaxIterations(rawValue) {
        const ABSOLUTE_MAX = 100; // Hard limit to prevent infinite loops
        const DEFAULT = 10;
        const parsed = Number.parseInt(rawValue ?? DEFAULT.toString(), 10);
        if (!Number.isFinite(parsed) || Number.isNaN(parsed) || parsed <= 0) {
            return DEFAULT;
        }
        // Enforce absolute maximum
        if (parsed > ABSOLUTE_MAX) {
            console.warn(`While loop max iterations ${parsed} exceeds limit, capping at ${ABSOLUTE_MAX}`);
            return ABSOLUTE_MAX;
        }
        return parsed;
    }
    async executeWhileNode(node, state, langGraphState) {
        const data = node.data || {};
        const ABSOLUTE_MAX = 100; // Hard safety limit
        const conditionExpr = data.whileCondition || data.condition || 'false';
        const maxIterations = Math.min(this.parseMaxIterations(data.maxIterations), ABSOLUTE_MAX);
        const iterationKey = `${node.id}__iterationCount`;
        const previousIteration = Number(langGraphState.variables?.[iterationKey] || 0);
        // Add absolute safety check
        if (previousIteration > ABSOLUTE_MAX) {
            console.error(`While loop ${node.id} exceeded absolute limit of ${ABSOLUTE_MAX}`);
            return {
                condition: false,
                iteration: previousIteration,
                nextIteration: previousIteration,
                maxIterations: ABSOLUTE_MAX,
                stoppedReason: 'absolute_limit_exceeded',
                error: `Loop terminated: exceeded absolute limit of ${ABSOLUTE_MAX} iterations`,
                timestamp: new Date().toISOString()
            };
        }
        if (previousIteration > maxIterations) {
            return {
                condition: false,
                iteration: previousIteration,
                nextIteration: previousIteration,
                maxIterations,
                stoppedReason: 'max_iterations',
                timestamp: new Date().toISOString()
            };
        }
        try {
            const evaluationIteration = previousIteration + 1;
            // SECURITY FIX: Use safe evaluator (mathjs-based) instead of Function() constructor
            const shouldContinue = Boolean((0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$safe$2d$expression$2d$evaluator$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["safeEvaluate"])(conditionExpr, {
                input: state.variables.input,
                state: langGraphState,
                lastOutput: state.variables.lastOutput,
                iteration: evaluationIteration
            }));
            const nextIteration = shouldContinue ? evaluationIteration : previousIteration;
            // Get accumulated loop results
            // NOTE: We return immutable updates - the reducer will merge [iterationKey] into state.variables
            const loopResultsKey = `${node.id}__loopResults`;
            const accumulatedResults = langGraphState.variables[loopResultsKey] || [];
            if (!shouldContinue) {
                console.log(`Loop breaking, passing ${accumulatedResults.length} results to next node`);
            }
            return {
                condition: shouldContinue,
                iteration: shouldContinue ? evaluationIteration : previousIteration,
                previousIteration,
                nextIteration,
                maxIterations,
                stoppedReason: shouldContinue ? 'condition_true' : 'condition_false',
                timestamp: new Date().toISOString(),
                conditionExpression: conditionExpr,
                evaluationIteration,
                [iterationKey]: nextIteration,
                loopResults: accumulatedResults
            };
        } catch (error) {
            const message = error instanceof Error ? error.message : 'Unknown error';
            console.error(`ERROR: While loop ${node.id}: condition evaluation failed`, error);
            throw new Error(`While loop ${node.id} condition evaluation failed: ${message}`);
        }
    }
    async handleArcadePendingAuth(node, result, pending, state) {
        const message = pending.message ?? `Authorization required for ${pending.toolName}`;
        const pendingAuth = {
            authId: pending.authId,
            nodeId: node.id,
            toolName: pending.toolName,
            authUrl: pending.authUrl,
            status: 'pending',
            userId: pending.userId,
            message,
            threadId: this.activeThreadId,
            executionId: this.activeExecutionId
        };
        result.status = 'pending-authorization';
        result.output = message;
        result.pendingAuth = pendingAuth;
        result.completedAt = new Date().toISOString();
        this.pendingAuth = pendingAuth;
        this.onNodeUpdate?.(node.id, result);
        const executionRef = this.activeExecutionId ?? this.activeThreadId ?? `exec_${Date.now()}`;
        await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$arcade$2f$auth$2d$store$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createOrUpdateArcadeAuthRecord"])({
            authId: pending.authId,
            executionId: executionRef,
            workflowId: this.workflow.id,
            nodeId: node.id,
            toolName: pending.toolName,
            authUrl: pending.authUrl,
            status: 'pending',
            userId: pending.userId,
            pendingInput: pending.pendingInput
        });
        // TODO: Save arcade auth state to database when running on server
        // This should be handled by the API route that calls this executor
        const resumeValue = (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["interrupt"])({
            type: 'arcade-auth',
            workflowId: this.workflow.id,
            nodeId: node.id,
            pendingAuth
        });
        if (resumeValue && typeof resumeValue === 'object' && 'status' in resumeValue) {
            const normalized = String(resumeValue.status).toLowerCase();
            if (normalized !== 'completed' && normalized !== 'authorized' && normalized !== 'approved') {
                throw new Error('Arcade authorization not completed yet. Please finish authorization before resuming.');
            }
        }
        this.pendingAuth = null;
        result.status = 'running';
        result.pendingAuth = undefined;
        result.output = undefined;
        delete result.completedAt;
        this.onNodeUpdate?.(node.id, result);
        return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$arcade$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeArcadeNode"])(node, state, this.apiKeys?.arcade);
    }
    async handlePendingApproval(node, result, pending) {
        const message = pending.message || 'Approval required';
        const pendingAuth = {
            authId: pending.approvalId,
            nodeId: node.id,
            toolName: 'user-approval',
            status: 'pending',
            message,
            threadId: this.activeThreadId,
            executionId: this.activeExecutionId
        };
        result.status = 'pending-approval';
        result.output = message;
        result.pendingAuth = pendingAuth;
        result.completedAt = new Date().toISOString();
        this.pendingAuth = pendingAuth;
        this.onNodeUpdate?.(node.id, result);
        const executionRef = this.activeExecutionId ?? this.activeThreadId ?? `exec_${Date.now()}`;
        // TODO: Save approval state to database
        // This should be handled by the API route that calls this executor
        // The API route should save to Convex/Redis as needed
        const resumeValue = (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["interrupt"])({
            type: 'user-approval',
            workflowId: this.workflow.id,
            nodeId: node.id,
            pendingAuth
        });
        this.pendingAuth = null;
        result.status = 'running';
        result.pendingAuth = undefined;
        result.output = undefined;
        delete result.completedAt;
        this.onNodeUpdate?.(node.id, result);
        return resumeValue ?? {
            approved: true
        };
    }
    /**
   * Execute individual node using existing executors
   */ async executeNode(node, state) {
        const nodeType = node.data.nodeType || node.type;
        switch(nodeType){
            case 'start':
                await new Promise((resolve)=>setTimeout(resolve, 500));
                // Pass through the initial input variables so they're available to next nodes
                return {
                    message: 'Workflow started',
                    ...state.variables.input || {}
                };
            case 'agent':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeAgentNode"])(node, state, this.apiKeys);
            case 'extract':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$extract$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeExtractNode"])(node, state, this.apiKeys);
            case 'arcade':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$arcade$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeArcadeNode"])(node, state, this.apiKeys?.arcade);
            case 'mcp':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$mcp$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeMCPNode"])(node, state, this.apiKeys?.firecrawl);
            case 'if-else':
            case 'if / else':
            case 'while':
            case 'user-approval':
            case 'user approval':
            case 'approval':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$logic$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeLogicNode"])(node, state);
            case 'transform':
            case 'data-transform':
            case 'set-state':
            case 'set state':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$data$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeDataNode"])(node, state);
            case 'file-search':
            case 'file search':
            case 'guardrails':
            case 'guardrail':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$tools$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeToolsNode"])(node, state);
            case 'http':
            case 'http-request':
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$http$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeHTTPNode"])(node, state);
            case 'note':
                return {
                    message: 'Note node (visual only)'
                };
            case 'end':
                return {
                    message: 'Workflow completed'
                };
            default:
                return await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$executors$2f$agent$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["executeAgentNode"])(node, state);
        }
    }
    /**
   * Execute workflow with streaming support
   */ async executeStream(input, config) {
        const threadId = config?.threadId || `thread_${Date.now()}`;
        this.activeThreadId = threadId;
        if (config?.executionId) {
            this.activeExecutionId = config.executionId;
        }
        this.pendingAuth = null;
        const initialState = {
            variables: {
                input: typeof input === 'string' ? input : input,
                lastOutput: typeof input === 'string' ? input : ''
            },
            chatHistory: [],
            currentNodeId: '',
            nodeResults: {},
            pendingAuth: null
        };
        this.lastStreamState = initialState;
        const rawStream = await this.graph.stream(initialState, (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$langsmith$2f$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getLangGraphConfig"])({
            configurable: {
                thread_id: threadId
            },
            streamMode: "values",
            recursionLimit: 100
        }));
        return this.wrapStreamWithInterruptHandling(rawStream, initialState);
    }
    async resumeFromAuth(threadId, resumeValue, options) {
        this.activeThreadId = threadId;
        if (options?.executionId) {
            this.activeExecutionId = options.executionId;
        }
        const command = new __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Command"]({
            resume: resumeValue
        });
        const rawStream = await this.graph.stream(command, (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$langsmith$2f$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getLangGraphConfig"])({
            configurable: {
                thread_id: threadId
            },
            streamMode: "values",
            recursionLimit: 100
        }));
        const fallback = this.lastStreamState ?? {
            variables: {},
            nodeResults: {},
            pendingAuth: null,
            currentNodeId: ''
        };
        return this.wrapStreamWithInterruptHandling(rawStream, fallback);
    }
    /**
   * Execute workflow (non-streaming)
   */ async execute(input, config) {
        const threadId = config?.threadId || `thread_${Date.now()}`;
        const initialState = {
            variables: {
                input: typeof input === 'string' ? input : input,
                lastOutput: typeof input === 'string' ? input : ''
            },
            chatHistory: [],
            currentNodeId: '',
            nodeResults: {}
        };
        const result = await this.graph.invoke(initialState, (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$langsmith$2f$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getLangGraphConfig"])({
            configurable: {
                thread_id: threadId
            },
            recursionLimit: 100
        }));
        return {
            id: `exec_${Date.now()}`,
            workflowId: this.workflow.id,
            status: 'completed',
            nodeResults: result.nodeResults || {},
            startedAt: new Date().toISOString(),
            completedAt: new Date().toISOString()
        };
    }
    wrapStreamWithInterruptHandling(rawStream, fallbackState) {
        const self = this;
        return async function*() {
            let latestState = fallbackState;
            try {
                for await (const chunk of rawStream){
                    if ((0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["isInterrupted"])(chunk)) {
                        const interruptRecord = chunk.__interrupt__?.[0] ?? null;
                        const pendingAuth = self.pendingAuth ?? interruptRecord?.value?.pendingAuth ?? null;
                        const pauseState = {
                            ...latestState ?? {},
                            pendingAuth,
                            currentNodeId: interruptRecord?.value?.nodeId ?? latestState?.currentNodeId ?? '',
                            nodeResults: latestState?.nodeResults ?? {}
                        };
                        self.lastStreamState = pauseState;
                        yield pauseState;
                        return;
                    }
                    const enrichedChunk = {
                        ...chunk,
                        pendingAuth: self.pendingAuth
                    };
                    latestState = enrichedChunk;
                    self.lastStreamState = enrichedChunk;
                    yield enrichedChunk;
                }
            } catch (streamError) {
                console.error('ERROR: Stream iteration error in wrapStreamWithInterruptHandling:', streamError);
                // Yield error state instead of throwing
                const errorState = {
                    ...latestState ?? {},
                    error: streamError instanceof Error ? streamError.message : 'Stream error',
                    status: 'failed'
                };
                self.lastStreamState = errorState;
                yield errorState;
                return;
            }
            self.pendingAuth = null;
            self.lastStreamState = latestState;
        }();
    }
    /**
   * Export workflow as LangGraph JSON
   */ exportToLangGraph() {
        return {
            nodes: this.workflow.nodes.map((node)=>({
                    id: node.id,
                    type: node.data?.nodeType || node.type,
                    data: node.data
                })),
            edges: this.workflow.edges.map((edge)=>({
                    source: edge.source,
                    target: edge.target,
                    sourceHandle: edge.sourceHandle
                })),
            state: {
                schema: {
                    variables: 'object',
                    chatHistory: 'array',
                    currentNodeId: 'string',
                    nodeResults: 'object'
                }
            }
        };
    }
    /**
   * Get LangGraph mermaid diagram
   */ async getMermaidDiagram() {
        try {
            const graphDrawable = this.graph.getGraph();
            return graphDrawable.drawMermaid();
        } catch (error) {
            console.error('Failed to generate Mermaid diagram:', error);
            return '';
        }
    }
    /**
   * Get state checkpoints for a thread
   */ async getCheckpoints(threadId) {
        const config = {
            configurable: {
                thread_id: threadId
            }
        };
        const checkpoints = [];
        for await (const checkpoint of this.checkpointer.list(config)){
            checkpoints.push(checkpoint);
        }
        return checkpoints;
    }
    /**
   * Resume from a specific checkpoint
   */ async resumeFromCheckpoint(threadId, checkpointId, input) {
        const config = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$langsmith$2f$config$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getLangGraphConfig"])({
            configurable: {
                thread_id: threadId,
                checkpoint_id: checkpointId
            },
            recursionLimit: 100
        });
        return await this.graph.invoke(input, config);
    }
}
function workflowToLangGraphJSON(workflow) {
    return {
        name: workflow.name,
        description: workflow.description,
        nodes: workflow.nodes.map((node)=>({
                id: node.id,
                type: node.data?.nodeType || node.type,
                position: node.position,
                data: {
                    ...node.data,
                    // Clean up any UI-specific data
                    nodeType: node.data?.nodeType || node.type
                }
            })),
        edges: workflow.edges.map((edge)=>({
                id: edge.id,
                source: edge.source,
                target: edge.target,
                sourceHandle: edge.sourceHandle,
                label: edge.label
            })),
        metadata: {
            createdAt: workflow.createdAt,
            updatedAt: workflow.updatedAt,
            version: '1.0.0',
            framework: 'langgraph'
        }
    };
}
function workflowToLangGraphCode(workflow) {
    const nodes = workflow.nodes;
    const edges = workflow.edges;
    // Generate node executors
    const nodeExecutors = nodes.filter((n)=>{
        const nodeType = n.data?.nodeType || n.type;
        return nodeType !== 'start' && nodeType !== 'end';
    }).map((node)=>{
        const nodeType = node.data?.nodeType || node.type;
        const data = node.data;
        if (nodeType === 'agent') {
            return `// ${data.nodeName || node.id} - AI Agent
const ${node.id} = async (state: typeof StateAnnotation.State) => {
  const response = await anthropic.messages.create({
    model: "${data.model || __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$models$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["DEFAULT_MODELS"].anthropic}",
    max_tokens: 4096,
    messages: [{
      role: 'user',
      content: \`${data.instructions || 'Process the input'}\${state.variables.lastOutput ? '\\n\\nContext: ' + JSON.stringify(state.variables.lastOutput) : ''}\`
    }]
  });

  return {
    ...state.variables,
    lastOutput: response.content[0].type === 'text' ? response.content[0].text : ''
  };
};`;
        }
        if (nodeType === 'transform') {
            return `// ${data.nodeName || node.id} - Transform Data
const ${node.id} = async (state: typeof StateAnnotation.State) => {
  // Transform logic here
  return {
    ...state.variables,
    lastOutput: state.variables.lastOutput // Add your transformation
  };
};`;
        }
        return `// ${node.id} - ${nodeType}
const ${node.id} = async (state: typeof StateAnnotation.State) => state.variables;`;
    }).join('\n\n');
    // Generate graph building code
    const startNode = nodes.find((n)=>(n.data?.nodeType || n.type) === 'start');
    const inputVars = startNode ? startNode.data?.inputVariables || [] : [];
    const graphBuilder = `const graph = new StateGraph(StateAnnotation)
${nodes.filter((n)=>{
        const t = n.data?.nodeType || n.type;
        return t !== 'start' && t !== 'end';
    }).map((n)=>`  .addNode("${n.id}", ${n.id})`).join('\n')}
  .addNode("end", async (state) => state.variables)
${edges.map((e)=>`  .addEdge("${e.source}", "${e.target}")`).join('\n')}
  .addEdge(START, "${startNode?.id || 'start'}")
  .addEdge("end", END)
  .compile();`;
    return `import { Anthropic } from "@anthropic-ai/sdk";
import { StateGraph, START, END, Annotation } from "@langchain/langgraph";
import { MemorySaver } from "@langchain/langgraph";

// Initialize Anthropic client
const anthropic = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY
});

// Define state structure
const StateAnnotation = Annotation.Root({
  variables: Annotation<{
    ${inputVars.map((v)=>`${v.name}: string;`).join('\n    ')}
    lastOutput?: any;
  }>(),
});

${nodeExecutors}

// Build the graph
${graphBuilder}

// Execute the workflow
async function main() {
  const result = await graph.invoke({
    variables: {
      ${inputVars.map((v)=>`${v.name}: "${v.defaultValue || ''}",`).join('\n      ')}
    }
  });

  console.log("Workflow Result:", result);
}

main();`;
}
function langGraphJSONToWorkflow(json) {
    return {
        id: json.metadata?.workflowId || `workflow_${Date.now()}`,
        name: json.name,
        description: json.description,
        nodes: json.nodes.map((node)=>({
                id: node.id,
                type: node.type,
                position: node.position || {
                    x: 0,
                    y: 0
                },
                data: node.data
            })),
        edges: json.edges.map((edge)=>({
                id: edge.id,
                source: edge.source,
                target: edge.target,
                sourceHandle: edge.sourceHandle,
                label: edge.label
            })),
        createdAt: json.metadata?.createdAt || new Date().toISOString(),
        updatedAt: new Date().toISOString()
    };
}
async function createExampleLangGraphWorkflow() {
    // Example: Query classifier with conditional routing
    const ExampleStateAnnotation = __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"].Root({
        query: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])(),
        category: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])(),
        result: (0, __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["Annotation"])()
    });
    // Router node - classifies the query
    const classifierNode = async (state)=>{
        const query = state.query.toLowerCase();
        let category = 'general';
        if (query.includes('stock') || query.includes('price') || /[A-Z]{2,5}/.test(state.query)) {
            category = 'finance';
        } else if (query.includes('product') || query.includes('buy') || query.includes('price comparison')) {
            category = 'shopping';
        } else if (query.includes('news') || query.includes('article')) {
            category = 'news';
        }
        return {
            category
        };
    };
    // Finance node
    const financeNode = async (state)=>{
        return {
            result: `Finance data for: ${state.query}`
        };
    };
    // Shopping node
    const shoppingNode = async (state)=>{
        return {
            result: `Shopping results for: ${state.query}`
        };
    };
    // News node
    const newsNode = async (state)=>{
        return {
            result: `News articles for: ${state.query}`
        };
    };
    // General node
    const generalNode = async (state)=>{
        return {
            result: `General info for: ${state.query}`
        };
    };
    // Conditional router
    const routeByCategory = (state)=>{
        switch(state.category){
            case 'finance':
                return 'finance';
            case 'shopping':
                return 'shopping';
            case 'news':
                return 'news';
            default:
                return 'general';
        }
    };
    // Build the graph
    const graph = new __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["StateGraph"](ExampleStateAnnotation).addNode('classifier', classifierNode).addNode('finance', financeNode).addNode('shopping', shoppingNode).addNode('news', newsNode).addNode('general', generalNode).addEdge(__TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["START"], 'classifier').addConditionalEdges('classifier', routeByCategory, {
        finance: 'finance',
        shopping: 'shopping',
        news: 'news',
        general: 'general'
    }).addEdge('finance', __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["END"]).addEdge('shopping', __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["END"]).addEdge('news', __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["END"]).addEdge('general', __TURBOPACK__imported__module__$5b$externals$5d2f40$langchain$2f$langgraph__$5b$external$5d$__$2840$langchain$2f$langgraph$2c$__esm_import$29$__["END"]).compile();
    return graph;
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
"[project]/lib/api/auth.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * API Authentication Utilities
 * Validates API keys for workflow execution endpoints
 * Supports both Clerk (UI) and API Key (external) authentication
 */ __turbopack_context__.s([
    "createUnauthorizedResponse",
    ()=>createUnauthorizedResponse,
    "validateApiKey",
    ()=>validateApiKey
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2f$client$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/lib/convex/client.ts [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$clerk$2f$nextjs$2f$dist$2f$esm$2f$app$2d$router$2f$server$2f$auth$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@clerk/nextjs/dist/esm/app-router/server/auth.js [app-route] (ecmascript)");
;
;
async function validateApiKey(request) {
    try {
        // First, check for Clerk authentication (UI users)
        const { userId } = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$clerk$2f$nextjs$2f$dist$2f$esm$2f$app$2d$router$2f$server$2f$auth$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["auth"])();
        if (userId) {
            return {
                authenticated: true,
                userId,
                authType: 'clerk'
            };
        }
        // If no Clerk auth, check for API key (external API users)
        const authHeader = request.headers.get('Authorization');
        if (!authHeader) {
            return {
                authenticated: false,
                error: 'Authentication required. Please sign in or provide an API key with format: "Bearer YOUR_API_KEY"'
            };
        }
        // Parse Bearer token
        const parts = authHeader.split(' ');
        if (parts.length !== 2 || parts[0] !== 'Bearer') {
            return {
                authenticated: false,
                error: 'Invalid Authorization header format. Expected: "Bearer YOUR_API_KEY"'
            };
        }
        const apiKey = parts[1];
        if (!apiKey || apiKey.length < 10) {
            return {
                authenticated: false,
                error: 'Invalid API key format'
            };
        }
        // Verify key with Convex
        const convex = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2f$client$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getConvexClient"])();
        const result = await convex.mutation(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].apiKeys.verify, {
            key: apiKey
        });
        if (!result.valid) {
            return {
                authenticated: false,
                error: result.error || 'Invalid API key'
            };
        }
        return {
            authenticated: true,
            userId: result.userId,
            authType: 'api-key'
        };
    } catch (error) {
        console.error('API key validation error:', error);
        return {
            authenticated: false,
            error: 'API key validation failed'
        };
    }
}
function createUnauthorizedResponse(error) {
    return new Response(JSON.stringify({
        error: 'Unauthorized',
        message: error,
        hint: 'Generate an API key from Settings and include it in the Authorization header'
    }), {
        status: 401,
        headers: {
            'Content-Type': 'application/json',
            'WWW-Authenticate': 'Bearer realm="API Key Required"'
        }
    });
}
}),
"[project]/src/lib/api/distributed-rate-limiter.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * Distributed Rate Limiter using Convex
 *
 * Replaces the in-memory Map-based rate limiting that doesn't scale
 * across multiple serverless instances.
 *
 * Usage:
 *   import { checkRateLimit, RATE_LIMITS } from '@/src/lib/api/distributed-rate-limiter';
 *
 *   const rateLimitResponse = await checkRateLimit(
 *     `user:${userId}:workflow-execution`,
 *     RATE_LIMITS.WORKFLOW_EXECUTION
 *   );
 *
 *   if (rateLimitResponse) return rateLimitResponse;
 */ __turbopack_context__.s([
    "RATE_LIMITS",
    ()=>RATE_LIMITS,
    "checkRateLimit",
    ()=>checkRateLimit,
    "getRateLimitIdentifier",
    ()=>getRateLimitIdentifier,
    "getRateLimitKey",
    ()=>getRateLimitKey,
    "resetRateLimit",
    ()=>resetRateLimit
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$index$2d$node$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/index-node.js [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/convex/dist/esm/browser/http_client.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
;
;
const RATE_LIMITS = {
    WORKFLOW_EXECUTION: {
        windowMs: 60 * 1000,
        maxRequests: 100
    },
    API_GENERAL: {
        windowMs: 60 * 1000,
        maxRequests: 600
    },
    API_HEAVY: {
        windowMs: 60 * 1000,
        maxRequests: 100
    }
};
async function checkRateLimit(identifier, config) {
    try {
        // Initialize Convex client
        const convex = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ConvexHttpClient"](("TURBOPACK compile-time value", "https://sensible-ermine-579.convex.cloud"));
        // Check rate limit via Convex mutation
        // Check rate limit via Convex mutation
        const result = await convex.mutation(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].rateLimits.check.checkRateLimit, {
            key: identifier,
            limit: config.maxRequests,
            windowMs: config.windowMs
        });
        // If not allowed, return 429 response
        if (!result.allowed) {
            const retryAfterSeconds = Math.ceil((result.resetAt - Date.now()) / 1000);
            return new Response(JSON.stringify({
                error: "Rate limit exceeded",
                message: `Too many requests. Please try again in ${retryAfterSeconds} seconds.`,
                retryAfter: retryAfterSeconds
            }), {
                status: 429,
                headers: {
                    "Content-Type": "application/json",
                    "Retry-After": String(retryAfterSeconds),
                    "X-RateLimit-Limit": String(config.maxRequests),
                    "X-RateLimit-Remaining": String(result.remaining),
                    "X-RateLimit-Reset": new Date(result.resetAt).toISOString()
                }
            });
        }
        // Allowed - return null to continue
        return null;
    } catch (error) {
        console.error("[Rate Limiter] Error checking rate limit:", error);
        // On error, allow the request (fail open)
        // This prevents rate limiter failures from blocking all requests
        return null;
    }
}
function getRateLimitKey(userId, action) {
    return `user:${userId}:${action}`;
}
function getRateLimitIdentifier(request, userId) {
    if (userId) {
        return `user:${userId}`;
    }
    // Fallback to IP-based rate limiting
    const forwarded = request.headers.get("x-forwarded-for");
    const ip = forwarded ? forwarded.split(",")[0].trim() : "unknown";
    return `ip:${ip}`;
}
async function resetRateLimit(identifier) {
    try {
        const convex = new __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$convex$2f$dist$2f$esm$2f$browser$2f$http_client$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["ConvexHttpClient"](("TURBOPACK compile-time value", "https://sensible-ermine-579.convex.cloud"));
        await convex.mutation(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].rateLimits.check.resetRateLimit, {
            key: identifier
        });
    } catch (error) {
        console.error("[Rate Limiter] Error resetting rate limit:", error);
        throw error;
    }
}
}),
"[project]/lib/api/validation-schemas.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * SECURITY FIX: Zod validation schemas for API input validation
 * Prevents injection attacks and ensures data integrity
 */ __turbopack_context__.s([
    "ApiKeySchema",
    ()=>ApiKeySchema,
    "HttpUrlSchema",
    ()=>HttpUrlSchema,
    "McpServerSchema",
    ()=>McpServerSchema,
    "PaginationSchema",
    ()=>PaginationSchema,
    "TransformScriptSchema",
    ()=>TransformScriptSchema,
    "WorkflowExecutionSchema",
    ()=>WorkflowExecutionSchema,
    "WorkflowIdSchema",
    ()=>WorkflowIdSchema,
    "WorkflowInputSchema",
    ()=>WorkflowInputSchema,
    "WorkflowResumeSchema",
    ()=>WorkflowResumeSchema,
    "createValidationErrorResponse",
    ()=>createValidationErrorResponse,
    "safeValidate",
    ()=>safeValidate
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__ = __turbopack_context__.i("[project]/node_modules/zod/v3/external.js [app-route] (ecmascript) <export * as z>");
;
// Maximum lengths to prevent DoS attacks
const MAX_STRING_LENGTH = 10000;
const MAX_ARRAY_LENGTH = 100;
const MAX_WORKFLOW_NAME_LENGTH = 255;
const MAX_NODE_COUNT = 100;
/**
 * Workflow node schema
 */ const WorkflowNodeSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
    id: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255),
    type: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].enum([
        'start',
        'agent',
        'mcp',
        'extract',
        'http',
        'transform',
        'if-else',
        'while',
        'user-approval',
        'end',
        'set-state',
        'guardrails',
        'arcade',
        'note',
        'data-transform'
    ]),
    position: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
        x: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].number(),
        y: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].number()
    }).optional(),
    data: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].record(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].any()).optional()
}).passthrough(); // Allow additional fields for node-specific data
/**
 * Workflow edge schema
 */ const WorkflowEdgeSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
    id: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255),
    source: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255),
    target: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255),
    sourceHandle: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255).optional(),
    targetHandle: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255).optional(),
    label: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255).optional(),
    type: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(50).optional()
}).passthrough();
const WorkflowExecutionSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
    input: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].union([
        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(MAX_STRING_LENGTH),
        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].record(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].any()),
        __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].null()
    ]).optional(),
    workflow: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
        id: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255).optional(),
        name: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(MAX_WORKFLOW_NAME_LENGTH).optional(),
        description: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(1000).optional(),
        nodes: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].array(WorkflowNodeSchema).max(MAX_NODE_COUNT),
        edges: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].array(WorkflowEdgeSchema).max(MAX_NODE_COUNT * 3),
        customId: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255).optional(),
        userId: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255).optional()
    }).passthrough()
});
const WorkflowInputSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].union([
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(MAX_STRING_LENGTH),
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].record(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].any()),
    __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].null()
]).optional();
const WorkflowIdSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255).regex(/^[a-zA-Z0-9_-]+$/, 'Invalid workflow ID format');
const WorkflowResumeSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
    threadId: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255),
    approved: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].boolean(),
    reason: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(1000).optional(),
    modifiedData: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].record(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].any()).optional()
});
const ApiKeySchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
    provider: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].enum([
        'anthropic',
        'openai',
        'groq',
        'google',
        'firecrawl',
        'e2b',
        'tavily-search',
        'serper-search',
        'serpapi-search',
        'scraperapi',
        'browserless',
        'arcade'
    ]),
    key: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().min(10, 'API key too short').max(500, 'API key too long').regex(/^[A-Za-z0-9_-]+$/, 'Invalid API key format')
});
const McpServerSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
    name: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(255),
    url: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().url().max(1000),
    type: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].enum([
        'sse',
        'stdio',
        'http',
        'websocket'
    ]),
    config: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].record(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].any()).optional()
});
const HttpUrlSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().url('Invalid URL format').max(2000).refine((url)=>{
    // Block localhost and private IPs to prevent SSRF
    const hostname = new URL(url).hostname;
    const privateRanges = [
        /^localhost$/i,
        /^127\./,
        /^10\./,
        /^172\.(1[6-9]|2[0-9]|3[01])\./,
        /^192\.168\./,
        /^169\.254\./,
        /^\[::1\]$/,
        /^\[fc00:/
    ];
    return !privateRanges.some((pattern)=>pattern.test(hostname));
}, {
    message: 'Private IP addresses and localhost are not allowed (SSRF protection)'
});
const TransformScriptSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].string().max(10000, 'Script too long').refine((script)=>{
    // Block dangerous patterns
    const dangerousPatterns = [
        /eval\s*\(/i,
        /Function\s*\(/i,
        /setTimeout\s*\(/i,
        /setInterval\s*\(/i,
        /__proto__/i,
        /constructor\s*\[/i
    ];
    return !dangerousPatterns.some((pattern)=>pattern.test(script));
}, {
    message: 'Script contains potentially dangerous code patterns'
});
const PaginationSchema = __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].object({
    limit: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].number().int().min(1).max(100).default(20),
    offset: __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$zod$2f$v3$2f$external$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$export__$2a$__as__z$3e$__["z"].number().int().min(0).default(0)
});
function safeValidate(schema, data) {
    try {
        const result = schema.safeParse(data);
        if (result.success) {
            return {
                success: true,
                data: result.data
            };
        } else {
            const errorMessage = result.error.errors.map((e)=>`${e.path.join('.')}: ${e.message}`).join(', ');
            return {
                success: false,
                error: errorMessage
            };
        }
    } catch (error) {
        return {
            success: false,
            error: error instanceof Error ? error.message : 'Validation failed'
        };
    }
}
function createValidationErrorResponse(error) {
    return {
        error: 'Validation failed',
        details: error,
        status: 400
    };
}
}),
"[project]/app/api/workflows/[workflowId]/execute-stream/route.ts [app-route] (ecmascript)", ((__turbopack_context__) => {
"use strict";

return __turbopack_context__.a(async (__turbopack_handle_async_dependencies__, __turbopack_async_result__) => { try {

__turbopack_context__.s([
    "POST",
    ()=>POST,
    "dynamic",
    ()=>dynamic
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2f$client$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__ = __turbopack_context__.i("[project]/lib/convex/client.ts [app-route] (ecmascript) <locals>");
var __TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/convex/_generated/api.js [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$langgraph$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/workflow/langgraph.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/api/auth.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2f$distributed$2d$rate$2d$limiter$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/src/lib/api/distributed-rate-limiter.ts [app-route] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$validation$2d$schemas$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/lib/api/validation-schemas.ts [app-route] (ecmascript)");
var __turbopack_async_dependencies__ = __turbopack_handle_async_dependencies__([
    __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$langgraph$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__
]);
[__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$langgraph$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__] = __turbopack_async_dependencies__.then ? (await __turbopack_async_dependencies__)() : __turbopack_async_dependencies__;
;
;
;
;
;
const dynamic = 'force-dynamic';
async function POST(request, { params }) {
    // Validate API key
    const authResult = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["validateApiKey"])(request);
    if (!authResult.authenticated) {
        return (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$auth$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["createUnauthorizedResponse"])(authResult.error || 'Authentication required');
    }
    // Rate limiting - Using distributed Convex-based rate limiting
    const rateLimitKey = (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2f$distributed$2d$rate$2d$limiter$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["getRateLimitKey"])(authResult.userId || 'anonymous', 'workflow-execution');
    const rateLimitResponse = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2f$distributed$2d$rate$2d$limiter$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["checkRateLimit"])(rateLimitKey, __TURBOPACK__imported__module__$5b$project$5d2f$src$2f$lib$2f$api$2f$distributed$2d$rate$2d$limiter$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["RATE_LIMITS"].WORKFLOW_EXECUTION);
    if (rateLimitResponse) {
        return rateLimitResponse;
    }
    const { workflowId } = await params;
    // SECURITY FIX: Validate workflow ID
    const idValidation = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$validation$2d$schemas$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["safeValidate"])(__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$validation$2d$schemas$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["WorkflowIdSchema"], workflowId);
    if (!idValidation.success) {
        return new Response(JSON.stringify({
            error: 'Validation failed',
            details: idValidation.error || 'Invalid workflow ID'
        }), {
            status: 400,
            headers: {
                'Content-Type': 'application/json'
            }
        });
    }
    // Create SSE stream
    const encoder = new TextEncoder();
    let isStreamClosed = false;
    const stream = new ReadableStream({
        async start (controller) {
            const closeStream = ()=>{
                if (!isStreamClosed) {
                    isStreamClosed = true;
                    try {
                        controller.close();
                    } catch (e) {
                    // Ignore if already closed
                    }
                }
            };
            const sendEvent = (event, data)=>{
                if (isStreamClosed) return;
                try {
                    const message = `event: ${event}\ndata: ${JSON.stringify(data)}\n\n`;
                    controller.enqueue(encoder.encode(message));
                } catch (error) {
                    // If enqueue fails, it might be because the stream is closed/errored
                    if (error instanceof TypeError && (error.message.includes('closed') || error.message.includes('errored'))) {
                        isStreamClosed = true;
                        return; // Don't log expected closure errors
                    }
                    console.error('Failed to send SSE event:', error);
                }
            };
            try {
                // Get inputs from request body
                const body = await request.json();
                // SECURITY FIX: Validate inputs
                const inputValidation = (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$validation$2d$schemas$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["safeValidate"])(__TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$api$2f$validation$2d$schemas$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["WorkflowInputSchema"], body);
                if (!inputValidation.success) {
                    sendEvent('error', {
                        error: 'Invalid input format',
                        details: inputValidation.error
                    });
                    closeStream();
                    return;
                }
                const inputs = body || {};
                // Get workflow from Convex
                if (!(0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2f$client$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["isConvexConfigured"])()) {
                    sendEvent('error', {
                        error: 'Convex not configured',
                        workflowId
                    });
                    closeStream();
                    return;
                }
                const convex = await (0, __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$convex$2f$client$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__$3c$locals$3e$__["getAuthenticatedConvexClient"])();
                // Look up workflow - try customId first, then try as Convex ID
                let workflowDoc = await convex.query(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].workflows.getWorkflowByCustomId, {
                    customId: workflowId
                });
                // If not found by customId and looks like Convex ID, try direct lookup
                if (!workflowDoc && workflowId.startsWith('j')) {
                    try {
                        workflowDoc = await convex.query(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].workflows.getWorkflow, {
                            id: workflowId
                        });
                    } catch (e) {
                    // Not a valid Convex ID
                    }
                }
                if (!workflowDoc) {
                    sendEvent('error', {
                        error: `Workflow ${workflowId} not found`,
                        workflowId
                    });
                    closeStream();
                    return;
                }
                // Convert Convex document to workflow format
                const workflowData = {
                    ...workflowDoc,
                    id: workflowDoc.customId || workflowDoc._id
                };
                if ("TURBOPACK compile-time falsy", 0) //TURBOPACK unreachable
                ;
                const workflow = workflowData;
                const startTime = Date.now();
                // Send start event
                sendEvent('workflow_started', {
                    workflowId,
                    workflowName: workflow.name,
                    totalNodes: workflow.nodes.length,
                    timestamp: new Date().toISOString()
                });
                // Create a custom execution with progress callbacks
                const executionId = `exec_${Date.now()}`;
                const nodeResults = {};
                // Get API keys - check user keys first, then fall back to system keys from Convex
                const { getLLMApiKey, getToolApiKey } = await __turbopack_context__.A("[project]/lib/api/llm-keys.ts [app-route] (ecmascript, async loader)");
                const userId = authResult.userId;
                // Get system API keys from Convex environment
                const systemKeys = await convex.action(__TURBOPACK__imported__module__$5b$project$5d2f$convex$2f$_generated$2f$api$2e$js__$5b$app$2d$route$5d$__$28$ecmascript$29$__["api"].systemApiKeys.getAllSystemApiKeys);
                const apiKeys = {
                    anthropic: (userId ? await getLLMApiKey('anthropic', userId) : undefined) ?? systemKeys.anthropic,
                    groq: (userId ? await getLLMApiKey('groq', userId) : undefined) ?? systemKeys.groq,
                    openai: (userId ? await getLLMApiKey('openai', userId) : undefined) ?? systemKeys.openai,
                    google: (userId ? await getLLMApiKey('google', userId) : undefined) ?? systemKeys.google,
                    firecrawl: (userId ? await getToolApiKey('firecrawl', userId) : undefined) ?? systemKeys.firecrawl,
                    arcade: (userId ? await getToolApiKey('arcade', userId) : undefined) ?? systemKeys.arcade,
                    e2b: (userId ? await getToolApiKey('e2b', userId) : undefined) ?? systemKeys.e2b,
                    tavily: (userId ? await getToolApiKey('tavily-search', userId) : undefined) ?? systemKeys.tavily,
                    serper: (userId ? await getToolApiKey('serper-search', userId) : undefined) ?? systemKeys.serper,
                    serpapi: (userId ? await getToolApiKey('serpapi-search', userId) : undefined) ?? systemKeys.serpapi,
                    scraperapi: (userId ? await getToolApiKey('scraperapi', userId) : undefined) ?? systemKeys.scraperapi,
                    browserless: (userId ? await getToolApiKey('browserless', userId) : undefined) ?? systemKeys.browserless
                };
                console.log('[Route] Debug API Keys:', {
                    userId,
                    hasTavilyKey: !!apiKeys.tavily,
                    hasSerperKey: !!apiKeys.serper,
                    hasFirecrawlKey: !!apiKeys.firecrawl,
                    hasAnthropicKey: !!apiKeys.anthropic
                });
                // Prepare initial input - pass as object if it's an object, otherwise as string
                let initialInput = '';
                if (typeof inputs === 'object' && Object.keys(inputs).length > 0) {
                    // If the body has an "input" field, extract it (common pattern from curl/API calls)
                    // Otherwise use the body directly
                    initialInput = inputs.input || inputs;
                } else {
                    // Otherwise use url or input field
                    initialInput = inputs.url || inputs.input || '';
                }
                // LangGraph Execution Path
                const threadId = `thread_${workflowId}_${Date.now()}`;
                let executor;
                try {
                    executor = new __TURBOPACK__imported__module__$5b$project$5d2f$lib$2f$workflow$2f$langgraph$2e$ts__$5b$app$2d$route$5d$__$28$ecmascript$29$__["LangGraphExecutor"](workflow, (nodeId, result)=>{
                        nodeResults[nodeId] = result;
                        if (result.status === 'running') {
                            const node = workflow.nodes.find((n)=>n.id === nodeId);
                            sendEvent('node_started', {
                                nodeId,
                                nodeName: node?.data?.nodeName || node?.data?.label || nodeId,
                                nodeType: node?.type || 'unknown',
                                timestamp: new Date().toISOString()
                            });
                        } else if (result.status === 'completed') {
                            const node = workflow.nodes.find((n)=>n.id === nodeId);
                            sendEvent('node_completed', {
                                nodeId,
                                nodeName: node?.data?.nodeName || node?.data?.label || nodeId,
                                result,
                                timestamp: new Date().toISOString()
                            });
                        } else if (result.status === 'failed') {
                            const node = workflow.nodes.find((n)=>n.id === nodeId);
                            sendEvent('node_failed', {
                                nodeId,
                                nodeName: node?.data?.nodeName || node?.data?.label || nodeId,
                                error: result.error,
                                timestamp: new Date().toISOString()
                            });
                        } else if (result.status === 'pending-authorization' || result.status === 'pending-approval') {
                            const node = workflow.nodes.find((n)=>n.id === nodeId);
                            sendEvent('node_paused', {
                                nodeId,
                                nodeName: node?.data?.nodeName || node?.data?.label || nodeId,
                                status: result.status,
                                timestamp: new Date().toISOString()
                            });
                        }
                    }, apiKeys);
                } catch (graphBuildError) {
                    console.error('❌ Failed to build LangGraph:', graphBuildError);
                    sendEvent('error', {
                        error: graphBuildError instanceof Error ? graphBuildError.message : 'Graph compilation failed',
                        timestamp: new Date().toISOString()
                    });
                    closeStream();
                    return;
                }
                // Execute with streaming
                const executionStream = await executor.executeStream(initialInput, {
                    threadId,
                    executionId
                });
                let finalState = null;
                // Proper async iteration with error handling
                try {
                    for await (const stateUpdate of executionStream){
                        const mergedState = {
                            ...stateUpdate,
                            nodeResults: {
                                ...stateUpdate.nodeResults,
                                ...nodeResults
                            }
                        };
                        finalState = mergedState;
                        sendEvent('state_update', {
                            nodeResults: mergedState.nodeResults,
                            currentNodeId: mergedState.currentNodeId,
                            pendingAuth: mergedState.pendingAuth,
                            timestamp: new Date().toISOString()
                        });
                        // Check for pending auth/approval
                        if (mergedState.pendingAuth) {
                            sendEvent('workflow_paused', {
                                reason: 'pending_authorization',
                                pendingAuth: mergedState.pendingAuth,
                                executionId,
                                threadId,
                                timestamp: new Date().toISOString()
                            });
                            // TODO: Save execution state to Convex for resume capability
                            // await convex.mutation(api.executions.createExecution, {...})
                            closeStream();
                            return;
                        }
                    }
                } catch (streamError) {
                    console.error('Stream iteration error:', streamError);
                    sendEvent('error', {
                        error: streamError instanceof Error ? streamError.message : 'Stream error',
                        timestamp: new Date().toISOString()
                    });
                    closeStream();
                    return;
                }
                // Send completion event
                const status = finalState?.pendingAuth ? 'waiting-auth' : 'completed';
                // Calculate metrics
                const endTime = Date.now();
                const totalTime = endTime - startTime;
                let totalTokens = 0;
                if (finalState?.nodeResults) {
                    Object.values(finalState.nodeResults).forEach((result)=>{
                        if (result.usage?.total_tokens) {
                            totalTokens += result.usage.total_tokens;
                        }
                    });
                }
                sendEvent('workflow_completed', {
                    workflowId,
                    executionId,
                    results: finalState?.nodeResults || {},
                    status,
                    timestamp: new Date().toISOString(),
                    metrics: {
                        totalTime,
                        totalTokens
                    }
                });
                // TODO: Save execution results to Convex
                // await convex.mutation(api.executions.completeExecution, {...})
                closeStream();
            } catch (error) {
                sendEvent('error', {
                    error: error instanceof Error ? error.message : 'Unknown error',
                    timestamp: new Date().toISOString()
                });
                closeStream();
            }
        },
        cancel () {
            isStreamClosed = true;
        }
    });
    return new Response(stream, {
        headers: {
            'Content-Type': 'text/event-stream',
            'Cache-Control': 'no-cache, no-transform',
            'Connection': 'keep-alive',
            'X-Accel-Buffering': 'no'
        }
    });
}
__turbopack_async_result__();
} catch(e) { __turbopack_async_result__(e); } }, false);}),
];

//# sourceMappingURL=%5Broot-of-the-server%5D__69b0f54a._.js.map